# 📋 CONTEXTE ACTUEL DU PROJET FINANCE COPILOT
# Exporté le: $(date +"%d/%m/%Y à %H:%M")
# Par: Script de sauvegarde automatique

## 🎯 ÉTAT ACTUEL DU PROJET

### Structure actuelle
```
$(find . -maxdepth 3 -type d | grep -E "(copilot-app|agent-stack-oss)" | sort | sed 's/^/├── /')
```

## 🚀 SERVICES ACTIFS

### État des services
$(get_service_status 8050 "Backend API")
$(get_service_status 5173 "Frontend UI")

## 🔧 PROCESSUS ACTIFS

$(get_active_processes)

## 📊 STATISTIQUES DU PROJET

$(get_file_stats)

## 🧪 ÉTAT DES ENDPOINTS API

$(test_api_endpoints)

## 📝 NOTES

- Dernière sauvegarde effectuée le $(date +"%d/%m/%Y à %H:%M")
- Pour restaurer ce contexte, suivez les instructions ci-dessous

## 🔄 INSTRUCTIONS DE RESTAURATION

1. Vérifier que tous les services sont arrêtés: `./stop.sh`
2. Vérifier la structure des dossiers
3. Restaurer les fichiers de configuration si nécessaire
4. Démarrer les services: `./start.sh`
5. Vérifier l'état: `./test_system.sh`

---
Exporté automatiquement par le script de sauvegarde le $(date +"%d/%m/%Y à %H:%M")
