# Batch-02 Gate Artifact

Timestamp: 2026-02-25 20:20 America/New_York
Batch: BATCH-02
Scope: T-A2.2 (`/api/stocks/prices` multi-ticker + missing ticker guard) + T-A3.1 (`/api/news/feed` normalized items)
QA_SIGNOFF: YES

DELTA:
- Implemented robust ticker parsing in `copilot-app/backend/src/api/main.py` via `_normalize_requested_tickers` (supports comma/space/semicolon tokenization such as `tickers=SPY,QQQ`).
- Normalized news item mapping in `_normalize_news_article` to guarantee stable fields `source` (fallback `unknown`) and `date` alias (`published_at`).
- Added contract coverage:
  - `copilot-app/backend/tests/test_stocks_prices_contract.py`
  - `copilot-app/backend/tests/test_news_feed_contract.py`

EVIDENCE:
- `cd copilot-app/backend && .venv/bin/python -m pytest -q tests/test_stocks_prices_contract.py tests/test_news_feed_contract.py tests/test_endpoint_cache_contracts.py` => `5 passed`.
- `bash scripts/backend_regression_gate.sh --no-live` => `VERDICT: PASS` (`33 passed`).
- `curl -sS http://localhost:8050/api/health | jq -c '{ok,status,service}'` => `{"ok":true,"status":"ok","service":null}`.
- `curl -sS "http://localhost:8050/api/stocks/prices?tickers=SPY,QQQ&timeframe=1y&interval=1d&downsample=200"` => `ok=true`, `resolved_tickers=["SPY","QQQ"]`, `requested_tickers=2`, `ticker_keys=["QQQ","SPY"]`.
- `curl -sS "http://localhost:8050/api/news/feed?tickers=SPY,QQQ&since=30d&score_min=0&limit=5"` => `ok=true`, `count=5`, `resolved_tickers=["SPY","QQQ"]`, sample item includes `title/url/source/date/published_at/tickers/score`.

RISKS:
- Non-blocking FastAPI/Pydantic deprecation warnings remain in the backend suite.
- Data-source variability can still affect article relevance/ordering, but contract shape is now locked by tests.

NEXT:
- Move to next prioritized batch (architecture/hardening) while keeping the same artifact discipline (`DELTA/EVIDENCE/RISKS/NEXT/VERDICT/BLOCKER_ID/NEXT_ACTION_UNIQUE`).
- Keep `scripts/backend_regression_gate.sh --no-live` in each implementation loop before QA signoff.

VERDICT: PASS
BLOCKER_ID: NONE
NEXT_ACTION_UNIQUE: CLOSE_BATCH02_AND_PREPARE_NEXT_PRIORITY_BATCH
