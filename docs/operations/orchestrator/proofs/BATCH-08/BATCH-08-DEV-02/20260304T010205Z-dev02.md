# BATCH-08-DEV-02 — UI wiring on stabilized decision contract

Date: 2026-03-04T01:02:05Z
Role: dev

## Delta
- Updated frontend copilot payload mapper (`buildCopilotJudgePayload`) to consume explicit contract fields:
  - reads `why` as reasoning source (fallback to `reasoning`/`answer`)
  - reads `risk` object (`level`, `caveat`) and keeps compatibility with `risk_level`/`risk_caveat`
  - exposes normalized `riskLevel`, `risk`, and `why` to UI payload consumers.

## File changed
- `apps/web/src/domains/forecasts/pages/app.js`

## INTEGRATION-APP-EENGINEER-RECOMMENDATIONS (reuse-first)
- Reused existing UI mapper (`buildCopilotJudgePayload`) and existing views.
- No new component, no new connector, no duplicate UI module.

## Verification
- `node --check apps/web/src/domains/forecasts/pages/app.js` -> PASS (local)
- `node --check apps/web/src/domains/forecasts/pages/app.js` -> PASS (VM)

## Risk / rollback
- Risk low: mapper remains backward-compatible with legacy fields.
- Rollback: one-file revert on `app.js`.
