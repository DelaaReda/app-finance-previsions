# BATCH-08-DEV-01 — Copilot decision contract stabilization

Date: 2026-03-04T01:00:43Z
Role: dev

## Delta
- Stabilized `copilot/ask` payload to always expose decision contract fields:
  - `verdict`, `confidence`, `why`, `risk` (+ retained existing keys `action`, `reasoning`, `risk_caveat`).
- Contract now remains never-empty on success and fallback/error paths.

## Files changed
- `apps/api/src/domains/copilot/application/copilot_service.py`
- `apps/api/src/domains/copilot/tests/test_copilot_service.py`

## INTEGRATION-APP-EENGINEER-RECOMMENDATIONS (reuse-first)
- Reused existing copilot domain service entrypoint (`build_ask_payload`) instead of creating a new endpoint/module.
- Preserved existing API envelope and frontend-compatible keys while adding explicit aliases (`why`, `risk`).
- No new service bridge, no new parallel API stack.

## Verification
- `python3 -m py_compile apps/api/src/domains/copilot/application/copilot_service.py` -> PASS
- local smoke (`build_ask_payload`) -> `SMOKE_OK buy medium 0.8`
- VM smoke (`build_ask_payload`) -> `VM_SMOKE_OK hold high insufficient_sources`
- `pytest` unavailable on VM image (`pytest: command not found`), covered by targeted smoke checks.

## Risk / rollback
- Risk: downstream clients relying only on `reasoning` remain compatible (field kept).
- Rollback: single-file revert on `copilot_service.py` + test file.
