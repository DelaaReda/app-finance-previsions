# BATCH-11 — Data Ingestion Core + Freshness SLO [PLAN]

## Vision alignment
- Référence: `docs/product/planning/PRODUCT_VISION.md`
- Objectif batch: garantir ingestion + propagation “freshness” mesurable (SLO) et affichage cohérent côté produit (pas de `UNKNOWN/NO_DATA` transitifs).

## Architecture plan ref
- `docs/architecture/ARCHITECTURE_MAP.md`
- API/runtime: `apps/api/runtime/`
- Domaines API: `apps/api/src/domains/*` (sans chemins legacy)

## Architecture audit (paths attendus)
- API: `apps/api/runtime/` + domaines ingestion/contrats dans `apps/api/src/domains/*`
- Web (consommation freshness): `apps/web/src` (surfaces qui affichent freshness/erreurs)

## Tracks & séquencement (single batch)
1) Backend/API: contrat freshness (timestamps, zones, fallback), normalisation payload, erreurs explicites.
2) Data/Ingestion: pipeline d’ingestion, idempotence, backfill minimal, métriques.
3) Infra/Obs: métriques freshness + alerting basique; corrélation req_id.

## Acceptance gate (DONE)
- SLO freshness défini + mesure disponible (métrique + seuil).
- Contrats API: champs freshness présents + defaults appliqués; pas de `NO_DATA` dû à mapping.
- Preuves: payload exemple + capture métrique freshness + scénario UI (erreur/retard).

## Dispatch attendu (prochaine étape)
- Créer tâches dev/backend/data/infra intra-batch (pas de dépendances top-level inter-batch).

