Summary: PASS. The `context_influence` payload is preserved through `buildCopilotStartState`, re-normalized in `resolveCopilotStartState`, and rendered in the existing hero metadata/lead path without introducing new UI wiring.

Artifact:
- reviewed commit: `bd568c13eeab0be4101a90b053d77930c27e54a7`
- delivery proof manifest: `docs/operations/orchestrator/proofs/VB-03/VB-03-DEV-02/20260311T032707Z-543.yaml`

Verify:
- `node --test apps/web/src/domains/forecasts/pages/app.test.js`
- result: `32/32` passing
- targeted assertions confirmed:
  - `sanitize/build` path preserves normalized `contextInfluence`
  - `resolve` path keeps the field available to raw and normalized callers
  - hero lead renders `Saved portfolio context applied.`
  - suggestion chips render `Context: portfolio aware portfolio • AAPL, MSFT`

Raw output ref:
- `docs/operations/orchestrator/proofs/VB-03/VB-03-DEV-02/qa-review-20260311-node-test.txt`
