# BATCH-28-DEV-03 — Release gate runbook and adoption analytics evidence

Date: 2026-03-07T23:07:31Z
Role: dev

## Delta
- Added a backend regression test for stable UI metadata on nominal and timeout/partial payload paths.
- Added a frontend smoke gate for the dynamic-state matrix used by the critical widget health banner.
- Captured the current batch closure verdict with explicit GO/NO-GO evidence instead of relying on implicit test drift.

## Files changed
- `apps/api/src/platform/tests/test_service_standard_metadata.py`
- `tests/smoke/batch_28_dev_03_release_gate_check.mjs`

## Runbook
1. Targeted backend contract regression:
   - `cd apps/api/src && PYTHONPATH=. pytest -q platform/tests/test_service_standard_metadata.py domains/market_data/tests/test_dashboard_metadata_contract.py`
   - Result: `PASS` (`4 passed`)
2. Frontend release-gate smoke:
   - `cd /home/venom/analyse-financiere && node tests/smoke/batch_28_dev_03_release_gate_check.mjs`
   - Result: `PASS batch_28_dev_03_release_gate_check`
3. Existing cache-contract regression (bounded check):
   - `cd apps/api/src && timeout 20s env PYTHONPATH=. pytest -q domains/market_data/tests/test_endpoint_cache_contracts.py`
   - Result: `TIMEOUT` (`rc=124`)
4. Existing backend regression gate (bounded check):
   - `cd /home/venom/analyse-financiere && timeout 60s bash platform/policies/backend_regression_gate.sh --no-live`
   - Result: `FAIL` during pytest collection with `ModuleNotFoundError: No module named 'packages'` from `domains/judge/tests/test_judge_template_hardening.py`

## Proof matrix
- Backend nominal metadata contract: `PASS`
  - `ok/status/error/freshness` stay mirrored between the top-level envelope and `data`.
- Backend timeout + partial payload normalization: `PASS`
  - `freshness=error` is normalized back to `generated_at`; `warnings` become a list; `status=degraded`.
- Frontend nominal state: `PASS`
  - No degraded banner is emitted when live data and API health are fresh.
- Frontend API timeout state: `PASS`
  - Timeout warnings map to `error`.
- Frontend incomplete payload state: `PASS`
  - `partial` / `missing` warnings plus fallback sources map to `degraded`.
- Frontend stale cache state: `PASS`
  - Aged freshness beyond the TTL window maps to `stale`.
- Desktop host proof: `PASS`
  - The banner resolves the visible glassmorphic hero host instead of a hidden legacy hero node.
- Compact/mobile hero host proof: `PASS`
  - The banner resolves `#hero-what-need-container` when earlier hero selectors are hidden.

## INTEGRATION-APP-EENGINEER-RECOMMENDATIONS (reuse-first)
- Reused `service_response_with_metadata` and the existing critical-widget health helpers from `apps/web/src/domains/forecasts/pages/app.js`.
- No duplicate runtime helper was introduced.
- Kept verification in the canonical repo test paths (`platform/tests`, `tests/smoke`).

## Verdict
- `NO-GO` for batch closure in the current environment.
- Reason 1: the existing broader backend regression gate is red before batch close because judge test collection cannot import `packages.contracts.judge_v1`.
- Reason 2: the existing cache-contract regression target does not complete within the bounded 20s check, so stale-cache closure evidence is not yet green via the legacy app bootstrap path.

## Recommended next step
- Fix the judge test import path so `platform/policies/backend_regression_gate.sh --no-live` can run green again.
- Triage the `domains/market_data/tests/test_endpoint_cache_contracts.py` hang or replace it with a narrower non-blocking cache-contract test.
- Once those two legacy blockers are green, rerun this runbook and let QA add browser-level desktop/mobile captures for final batch closure.
