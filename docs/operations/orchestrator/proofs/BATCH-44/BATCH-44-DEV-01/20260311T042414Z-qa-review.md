summary: QA found and fixed a local contract defect in `/api/forecasts/insider-behavior`.

issue:
- When `ownership_snapshot.json` was missing, the endpoint returned `signals=[]` and `provenance.fallback_used=false`, despite the slice being framed as a never-empty conservative fallback surface.

fix:
- Added conservative placeholder signal generation in `apps/api/src/domains/forecasts/application/global_signal_mesh_service.py`.
- Propagated top-level `fallback_used` so route contract normalization preserves `provenance.fallback_used=true`.
- Added regression coverage in `apps/api/src/domains/forecasts/tests/test_global_signal_mesh_route.py`.

verify:
- `pytest apps/api/src/domains/forecasts/tests/test_global_signal_mesh_route.py -q`
  - result: `11 passed`
- Direct probe from `apps/api/src`:
  - `GET /api/forecasts/insider-behavior?tickers=NVDA&limit=5`
  - result: `status_code=200`, `returned_signal_count=1`, `provenance.fallback_used=true`, warning includes `no_ownership_snapshot_rows`

raw_output_ref:
- prior delivery proof: `docs/operations/orchestrator/proofs/BATCH-44/BATCH-44-DEV-01/20260311T041849Z-743.yaml`
- QA proof: `docs/operations/orchestrator/proofs/BATCH-44/BATCH-44-DEV-01/20260311T042414Z-qa-review.md`

notes:
- The direct app probe also emitted unrelated legacy alerts-job noise (`platform/legacy/jobs/alerts.py` TypeError on `latest_direction.get("down") > 0`). This did not block the insider-behavior contract or targeted tests and was left out of scope for this bounded QA review.
