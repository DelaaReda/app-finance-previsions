# BATCH-28 — Frontend Dynamic UX Hardening (Freshness + Error States) [PLAN]

## Vision alignment
- Référence: `docs/product/planning/PRODUCT_VISION.md`
- Objectif batch: UX robuste sur données dynamiques (freshness visible, erreurs explicites et actionnables, états vides cohérents).

## Architecture plan ref
- `docs/architecture/ARCHITECTURE_MAP.md`
- Frontend: `apps/web/src`
- API/runtime (contrats/erreurs): `apps/api/runtime/` + `apps/api/src/domains/*`

## Architecture audit (paths impactés)
- `apps/web/src` (widgets, facettes, compos UI d’erreur/empty/loading, telemetry)
- `apps/api/runtime/` (middleware erreurs/normalisation)
- `apps/api/src/domains/*` (DTO/contrats exposés)
- `platform/automation/` (orchestration proofs/gates, pas de logique produit)

## Tracks & séquencement (single batch)
1) FE: standardiser états `loading/empty/error/no_data/freshness_stale` + composants partagés.
2) FE: “erreurs colorées” + CTA (retry, report) + instrumentation minimale.
3) API: normaliser payload/erreurs pour supprimer ambiguïtés (null vs vide) consommées par UI.
4) QA: scénarios e2e (data ok, empty legit, stale, erreur) + preuves.

## Acceptance gate (DONE)
- Les routes ciblées n’affichent plus `UNKNOWN/NO_DATA` transitif (mapping/contrat).
- Freshness visible et cohérente (stale/ok) + erreurs actionnables.
- Preuves: capture UI + exemples payload + métrique/telemetry.

## Dispatch attendu (prochaine étape)
- Découper en tâches dev FE/API intra-batch uniquement (pas de dépendances top-level inter-batch).

