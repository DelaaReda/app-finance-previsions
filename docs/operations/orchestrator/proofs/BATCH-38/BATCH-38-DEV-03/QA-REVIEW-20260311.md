# QA Review - BATCH-38-DEV-03

## Scope

Walk-forward forecast scoreboard contract and dashboard consumption.

## Result

- PASS for the delivered slice after one bounded frontend fix.
- Fixed a slice-local gap in the scenario widget so it now prefers `threshold_summary.walk_forward_direction_hit_rate` for hit-rate copy instead of depending on `rows`.

## Evidence

- Backend contract already exposes stable summary fields:
  - `apps/api/src/domains/forecasts/tests/test_walk_forward_scoreboard_route.py`
- Frontend scenario widget now consumes summary-first:
  - `apps/web/src/domains/forecasts/pages/app.js`
- Added targeted UI proof:
  - `apps/web/src/domains/forecasts/pages/app.test.js`

## Verify

- `pytest apps/api/src/domains/forecasts/tests/test_walk_forward_scoreboard_route.py -q`
  - PASS (`3 passed`)
- `node --test apps/web/src/domains/forecasts/contracts/apiConnector.test.js apps/web/src/domains/forecasts/pages/app.test.js`
  - PASS (`60 tests, 60 passed`)

## Regression Gate

- `platform/policies/backend_regression_gate.sh`
  - FAIL outside this slice.
  - Observed unrelated failures in:
    - `domains/copilot/tests/test_copilot_domain_router.py`
    - `domains/copilot/tests/test_copilot_service.py`
    - `domains/judge/tests/test_g4f_client_model_fallback.py`
    - `domains/market_data/tests/test_edge_contracts_runtime.py`
    - `domains/market_data/tests/test_endpoint_cache_contracts.py`
  - Notable blockers:
    - `domains/copilot/api/copilot.py`: `NameError` on undefined `fallback` in `/copilot/start` fallback path
    - `platform/main.py`: relative import failure when loaded by runtime edge-contract tests
    - `platform/main.py`: `/api/copilot/context` alias iterates over a `Query` object when invoked directly in test

