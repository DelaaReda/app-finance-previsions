# BATCH-57-PLAN — preuve (2026-03-06T07:22:03Z)

## Vision alignment
- Référence: `docs/product/planning/PRODUCT_VISION.md`
- Cible: fiabiliser le cycle orchestrateur (queue → workboard → claim) et éviter la dérive READY_DEV sans next_action exploitable.

## Architecture plan ref
- Map: `docs/architecture/ARCHITECTURE_MAP.md`
- Périmètre lecture/écriture autorisé: `docs/operations/orchestrator/parallel-workstreams.json`, `docs/product/planning/WORKSTATE.md`

## Architecture audit (chemins requis)
- `apps/api/src/domains/*` (impact attendu: contrats de domaine stables côté API)
- `apps/api/runtime/` (impact attendu: normalisation payload/contrats runtime)
- `apps/web/src/` (impact attendu: points d’intégration UI/analytics)

## Implementation tracks (minimal)
- Track A: réduire la désynchronisation queue_has_ready vs NO_READY_TASK (symptôme: READY_DEV + ready_next_actions=NONE).
- Track B: clarifier la génération des tâches planner vs dev lors de `sync-priority`.

## Integration reuse
- Réutiliser les garde-fous déjà appliqués: `platform/policies/role_contract_guard.py` (autofill artefacts, checks delivery context).

## Acceptance gate
- `queue_has_ready=1` ⇒ `claim --role planner` matérialise au moins 1 tâche READY planner (ou preuve explicite d’absence) après `sync-priority`.
- Les items READY_DEV incluent une next_action concrète (pas `NONE`) ou sont reclassés.

