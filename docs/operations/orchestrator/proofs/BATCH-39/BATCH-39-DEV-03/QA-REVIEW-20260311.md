QA review for `BATCH-39-DEV-03` confirmed the delivered forecast detail provenance/SLA slice on the success and not-found paths, then identified and fixed one remaining local contract gap on the route exception fallback for `/forecasts/{forecast_id}`.

Findings:
- Delivered change was correct for service-backed detail responses: `get_forecast_detail_payload()` now emits `updated_at`, `freshness_status`, `freshness_age`, and `provenance`.
- Remaining defect reproduced during QA: if `forecasts_service.get_forecast_detail_payload()` raised inside the route, the fallback response returned `updated_at=""` and `provenance={}`, which violated the batch requirement that the core forecast detail endpoint always publish canonical provenance/SLA metadata.
- Local bounded fix applied in `apps/api/src/domains/forecasts/api/forecasts.py` to add `updated_at`, `freshness_status`, `freshness_age`, and `provenance` on the route fallback, reusing the existing forecasts provenance helper.
- Regression lock added in `apps/api/src/domains/forecasts/tests/test_forecasts_template_parity.py` with `test_forecast_detail_route_fallback_keeps_provenance_contract`.

Verification:
- `PYTHONPATH=apps/api/src python3 -m pytest apps/api/src/domains/forecasts/tests/test_forecasts_template_parity.py -q -k "forecast_detail"` -> PASS
- `platform/policies/backend_regression_gate.sh --no-live domains/forecasts/tests/test_forecasts_template_parity.py` -> PASS

Raw output:
- `docs/operations/orchestrator/proofs/BATCH-39/BATCH-39-DEV-03/QA-REVIEW-20260311.raw.txt`
