{"status":"completed","summary":"Patched the forecasts facette controller so a minimal live slice now routes selected facette/tab combinations to existing story/news/trade/calendar/market-drivers widgets instead of the generic sample content, and re-renders the open facette when unified live payload updates arrive.","root_cause":"`loadFacetteContent()` in `apps/web/src/domains/forecasts/pages/app.js` always fell through to `generateFacetteContent()` on the nominal path, so facette tabs ignored the unified backend payload already applied by `applyLiveDashboardData()` and silently showed placeholder content.","fix_applied":"Added explicit facette/tab live routing, cloned existing widget templates with scoped IDs, reused the existing widget renderers against scoped roots, introduced live-API-unavailable fallback notices, updated the market pulse widget from story payload, and refreshed the active facette on `financecopilot:live-dashboard-updated`. Committed as `554932d6b8215db60ff9c802f6d94ad11f036a7b`.","artifact":"apps/web/src/domains/forecasts/pages/app.js:165-377 (live facette routing + scoped widget reuse), apps/web/src/domains/forecasts/pages/app.js:1847-1859 (dashboard story widget now bound to live payload), apps/web/src/domains/forecasts/pages/app.js:2043-2048 (active facette refresh on live event), apps/web/src/domains/forecasts/pages/app.js:3863-3984 (existing widget renderers now support scoped roots).","verify":"before=`loadFacetteContent()` always rendered `generateFacetteContent()` placeholders for facettes/tabs; after=`marche/Synthèse`, `marche/Actualités`, `economie/{Marché, Macro Économie, News Économiques}`, `news/*`, and `trading/Trade Ideas` branch to live widget templates backed by unified story/news/trade/calendar/market-drivers data with explicit unavailable fallback notices; test=`node --check apps/web/src/domains/forecasts/pages/app.js`","files_touched":"1 file: apps/web/src/domains/forecasts/pages/app.js","tests_run":"node --check apps/web/src/domains/forecasts/pages/app.js","commit_sha":"554932d6b8215db60ff9c802f6d94ad11f036a7b","architecture_check":"layer=frontend forecasts page controller, imports_ok=yes (no new module boundaries/imports introduced; reused existing widget/platform wiring in-place), path_target=apps/web/src/domains/forecasts/pages/app.js","vision_alignment":"batch=BATCH-27, target=FRONTEND facette/tab nominal path on unified live payload, impact=removes placeholder facette content for the targeted SRE pack slice while staying inside existing forecasts widgets and shared UI wiring","recommended_next":"Planner should queue a browser-level verification pass on the targeted facettes/tabs to confirm DOM-loaded widget templates are present before facette navigation and that the live contract warnings surface the fallback banner only when the API marks a slice unavailable.","blocking_issue":"none"}