# Roleflow Pilot (2026-02-26)

This folder contains pilot artifacts produced during an early parallel-workboard run.

Notes:
- Some artifacts reference legacy roles/steps (`po`, `scrum_master`, `PO_REVIEW`, `SCRUM_REVIEW`).
- Current baseline decommissions those always-on roles: governance is handled by `planner` via `GOV_REVIEW`.

Source of truth:
- Workboard: `docs/orchestrator-ops/parallel-workstreams.json`
- Template engine: `scripts/parallel_workstream.py`
- Coordination spec: `docs/ops/ORCHESTRATION_COORDINATION_SPEC.yaml`

