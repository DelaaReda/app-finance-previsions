# Daily Memory 2026-04-16

## AWS app runtime migration
- Finance Copilot app-serving runtime moved to AWS EC2 public endpoints.
- Canonical public endpoints:
  - frontend: `http://3.98.20.77/`
  - api: `http://3.98.20.77/api/...`
  - monitor: `http://3.98.20.77:8080/`
- The UTM VM stays orchestration-only. Backend/frontend/monitor must not be run locally there for normal work.
- VM-side app control path: `scripts/aws_remote_app_control.sh`
- Mac and the UTM VM share the same workspace view; this is local workspace sync only.
- Code publication path is a separate shared-workspace -> AWS step, manual by default.
- Canonical operator path is Mac-side publication; an explicit UTM-triggered publication still ships the same shared workspace snapshot, not VM-local orchestration state.
- VM agents must not assume auto-sync from local repo edits.
- Public API change reflection measured at about 5 seconds; full sync + restart + verification measured at about 20 to 30 seconds.
- EC2 app host auto-stops after 10 minutes without HTTP traffic; SSH does not keep it alive.

## 2026-04-16T22:11:00Z po-vision-batch-architect

- Audit infra delta confirmé: le runtime applicatif public vit désormais sur EC2 AWS; la VM UTM reste l’hôte d’orchestration uniquement.
- Impact sur la vision batch: aucun nouveau batch utile créé. `BATCH-89` reste le bon scope de fermeture; seul le protocole de preuve change.
- Nouvelle règle de preuve: VM wrappers pour la vérité runtime/orchestration, `scripts/aws_remote_app_control.sh` + endpoints publics EC2 pour la preuve backend/frontend/monitor.
- Vérification faite: `bash scripts/vm_ssh_exec.sh -- "bash scripts/runtime_host_check.sh && bash scripts/aws_remote_app_control.sh status"` confirme la VM canonique et un app stack EC2 en cours côté backend/frontend/monitor.
- Conséquence produit: ne plus considérer les listeners loopback VM comme preuve pour `brief/start/ask/open`; la réévaluation backlog doit rester focalisée sur la convergence SQLite/résidus historiques.

## 2026-04-16T22:18:00Z infra-alignment-loop

- Boucle de mise à jour effectuée sur les surfaces vivantes liées au changement infra.
- Code actif aligné: `platform/automation/product_priority_guard.py` prend désormais EC2 public par défaut; `platform/automation/doctor.py` réutilise le default public; `scripts/vm_wake_cleanup.sh` ne tente plus de redémarrer backend/frontend locaux sur la VM et passe par le contrôle AWS canonique.
- Guides mis à jour: `docs/DEV_TOOLS_GUIDE.md`, `docs/ops/reference/REFERENCE_DEV_TOOLS_GUIDE.md`, `docs/ops/reference/REFERENCE_AGENT_TOOLS_GUIDE.md` pointent désormais vers `http://3.98.20.77`.
- Historique laissé en place mais clarifié: `docs/ops/REMPISE_ORDRE_POST_MIGRATION.md` est explicitement marqué comme ancien modèle VM-local.
- Pas de nouveau batch créé; le delta est infrastructurel et documentaire, pas produit.

## 2026-04-16T22:26:00Z infra-alignment-loop-2

- Deuxième passe effectuée sur les surfaces actives runtime/watchdog/frontend.
- Corrigé vers EC2 public: `scripts/planner_delivery_watchdog.sh`, `platform/automation/dev_activation_readiness.py`, `scripts/dev_tools.sh`, `platform/policies/preflight_dispatch.sh`, `apps/monitor/services/status_service.py`.
- Corrigé côté frontend fallback: `apps/web/src/platform/js/sentry-init.js`, `apps/web/src/domains/forecasts/pages/app.js`, `apps/web/src/domains/forecasts/contracts/apiConnector.js`, `apps/web/src/domains/forecasts/pages/personal-finance-start.html`, `apps/web/src/domains/forecasts/components/widgets/copilot-panel.html`.
- Reste volontairement non modifié: tests, preuves historiques, jobs legacy, et usages loopback internes plausibles à l’hôte EC2.
- Décision inchangée: aucun nouveau batch; cette boucle sert seulement à réaligner l’infra vivante avec le modèle `EC2 app / VM orchestration`.

## Orchestration architect summary
- verdict: pas de nouveau progrès produit; produit EC2 up mais delivery/orchestration ne convergent pas proprement
- top priority: réconcilier `BATCH-95-DEV-02` entre snapshot planner local et vérité publique EC2 (`active_batch=null`)
- blocker: faux batch actif control-plane alors que le monitor public ne voit aucun batch ouvert
- next useful delivery: rétablir un `ranked_action` public non nul et scope-first sur `/api/copilot/start` et `/api/personal-finance/start`

## Orchestration architect summary
- verdict: régression publique EC2 en `502`; aucune valeur produit livrée maintenant malgré un `BATCH-95-DEV-03` projeté côté planner
- top priority: restaurer les endpoints publics EC2 (`/api/health`, `copilot/personal-finance/judge start`) avant tout nouveau claim de delivery
- blocker: `BATCH-95-DEV-03` est actif dans les projections mais absent de la vérité runtime prouvée, tandis que l’EC2 public est en panne `502`
- next useful delivery: remettre `BATCH-95-DEV-01/02` publiquement utilisables puis décider si `DEV-03` crée un vrai delta visible ou doit être classé résidu control-plane

## Orchestration architect summary
- verdict: produit public EC2 utilisable et batch fermé, mais guardian local recycle encore un faux READY au lieu d’accepter l’idleness canonique
- top priority: corriger `planner_guardian.py` pour ne plus déclencher `ready_but_*` quand SQLite dit `active_batch_ids=[]`
- blocker: résidu control-plane `task_id=NONE` dans `planner-guardian-latest.json` / `planner_guardian.py`, pas un batch produit actif
- next useful delivery: smoke public EC2 simple prouvant `judge -> copilot -> personal-finance` scope-first avec `ranked_action=open_nvda`

## Orchestration architect summary
- verdict: pas de nouveau progrès produit; EC2 public reste bon mais le planner a rouvert un faux batch `BATCH-96-ANALYSIS` sans delta utilisateur neuf
- top priority: empêcher l’autobatch planner-only quand la vérité publique/runtime est idle côté produit
- blocker: `BATCH-96-ANALYSIS` actif dans la VM (`priority-queue`/`parallel-workstreams`/guardian) alors que le monitor public expose `active_batch=null`
- next useful delivery: fermer `BATCH-96` comme résidu control-plane puis n’ouvrir qu’un batch avec preuve publique EC2 nouvelle

## 2026-04-17T00:22:52Z orchestration-architect

- EC2 public reste bon et produit utilisable (`/api/health`, `judge/copilot/personal-finance start` en `200`, `ranked_action=open_nvda`, story lines visibles).
- La vérité runtime VM confirme `product_done=true`, `ops_clean=false`, `next_batch_eligible=true`, mais le control-plane garde artificiellement `BATCH-96` ouvert.
- Les dernières preuves canoniques s’arrêtent à `BATCH-96-DEV-01` et `BATCH-96-DEV-02`; `BATCH-96-DEV-03` n’a pas de preuve publique fraîche et survit surtout via projections/contrats autofill.
- Priorité opérateur: backoff/close `DEV-03` comme résidu si aucune preuve EC2 neuve n’apparaît, puis ouvrir immédiatement le prochain batch portfolio/watchlist-first depuis la runtime truth.

## 2026-04-16T23:21:50Z orchestration-architect

- Public EC2 proof is still good and product-usable now: `/api/health`, `/api/copilot/start?tickers=NVDA`, `/api/personal-finance/start?tickers=NVDA`, and `/api/judge/personal-finance/start?tickers=NVDA` all return a real brief with `ranked_action=open_nvda`.
- New fact vs previous run: VM runtime truth now canonically contains `BATCH-96` in sqlite/event-store (`active_batch_ids=["BATCH-96"]`), so this is no longer projection-only.
- But `BATCH-96` has not shipped a new public delta yet: only `BATCH-96-ANALYSIS` is `IN_PROGRESS`, and its proof is a migration/proof-alignment memo.
- Delivery continuity is not broken because a batch is active, but orchestration efficiency is poor: downstream tasks are all `WAITING_DEP` and admin/dev lanes are emitting wait/no-signal churn.
- Immediate priority: either dispatch `BATCH-96` into one concrete EC2-visible user slice or back it off before it becomes durable token burn.

## 2026-04-16T23:43:50Z orchestration-architect

- EC2 public remains healthy and user-usable; the three starter endpoints still return a real brief with `ranked_action=open_nvda`.
- Runtime truth still has a real active batch: `BATCH-96`, now advanced to `BATCH-96-DEV-01` runnable/in progress after closure of `BATCH-96-ANALYSIS` and `BATCH-96-ARCH`.
- No new public user delta is visible yet for `BATCH-96`; the progress since the last run is structural only, not product-done.
- Main mismatch now: runtime truth says `BATCH-96` is active, while the public monitor still exposes `active_batch=null`; treat that as advisory residue, not canonical idleness.
- Immediate operator priority: finish `BATCH-96-DEV-01` or back off wait-only lanes; do not open a new batch while EC2 is healthy and this batch is still actionable.

## 2026-04-16T20:31:50-04:00 orchestration-architect summary

- Nouveau fait matériel: `BATCH-96-DEV-03` est maintenant `merged` dans la runtime truth SQLite et prouvé publiquement sur EC2; le judge starter scope `NVDA` et remplit enfin `what_changed_today` / `what_matters_now`.
- Le batch est donc `product_done=yes`, mais pas `ops_clean`: le monitor public expose déjà `active_batch=null` tandis que guardian/contrats VM continuent de poursuivre `DEV-03`.
- Priorité VM: fermer/backoff le résidu `BATCH-96` au lieu de demander plus de preuve décorative, puis ouvrir immédiatement le prochain batch éligible depuis la runtime truth.
- Plus petit delta suivant utile: rendre le starter réellement portfolio/watchlist-first sur EC2, pas seulement `market_wide` avec `tickers=NVDA`.

## 2026-04-17T00:08:27Z admin-unblock

Continuity
- previous_verdict: public EC2 product proof was green, but the public monitor lite surface still crashed and could misreport delivery continuity.
- changed_since_last_run: the EC2 app host was on stale monitor code, and a restart exposed a runtime lock inherited by detached child processes.

Verdict
- blocker: stale public monitor deployment plus inherited runtime lock on EC2 app control
- blocker_class: false authority / bad guard
- ec2_reachable: yes
- product_delivery_blocked: no
- delivery_continuity_restored: yes
- public_delivery_after_fix: yes
- runtime_can_resume: yes

Authority check
- public_proof_status: `api/health`, `copilot/start?tickers=NVDA`, and `monitor /api/status?lite=1` all return `200`
- runtime_truth_status: public monitor now reports `primary_status=ok` and `delivery_control.phase=idle_ready_for_next_batch`
- next_batch_eligible: yes
- projections_status: restored
- guardian_status: unchanged
- false_authority_detected: yes
- token_burn_detected: yes

Actions taken
- captured EC2 traceback proving `compute_health()` signature drift on the deployed monitor
- updated `apps/api/runtime/copilot.sh` so detached backend/frontend/monitor jobs close FD `9` and stop inheriting the runtime lock
- republished app-serving code to EC2 with `bash scripts/aws_app_sync_and_restart.sh`

Validation
- public `GET http://3.98.20.77/api/health` => `200`
- public `GET http://3.98.20.77:8080/api/status?lite=1` => `200`, `health=OK`, `primary_status=ok`
- public `GET http://3.98.20.77/api/copilot/start?tickers=NVDA` => valid starter payload
- remote `bash ./finance-copilot.sh status` => backend/frontend/monitor all `EN COURS`

Decision
- next_owner: VM planner/admin runtime
- next_action: resume from VM canonical truth; do not let restored public monitor projections reopen work by themselves.

## 2026-04-17T00:03:08Z role-prompt-engineer

Target
- role: planner
- prompt_source: `/Users/venom/Documents/analyse-financiere/platform/automation/cron_tmux_role_runner.sh`
- why_this_prompt_now: le bloc `ROLE=planner` restait la couche parent la plus rentable à corriger; il ne verrouillait pas encore explicitement l’autorité produit, la continuité EC2, ni le backoff des lanes stériles

Prompt audit
- useful_rules: collect-first sur subagent/downstream actif, reprise d’une tâche `IN_PROGRESS`, claim exact d’une tâche `READY`, subagent unique et ciblé, `none_no_signal` réservé à une indisponibilité runtime prouvée, preuve planner déjà compacte
- redundant_rules: la non-passivité était déjà exprimée deux fois (`jamais passif...` puis la règle `claim/collect/autobatch échoue`); le vrai manque était la hiérarchie d’autorité, pas davantage de verbes
- contradictory_rules: pas de contradiction dure interne, mais une omission contradictoire avec l’architecture cible: le prompt laissait encore l’état produit ambigu entre EC2 public, runtime truth et projections
- authority_confusion_found: oui; `dashboard/monitor/docs = derives` ne suffisait pas à verrouiller `EC2 public > runtime truth > projections`, ni à interdire à `guardian/monitor/queue` de réouvrir un batch `product_done`
- continuity_gap_tolerated: yes
- token_burn_backoff_missing: yes
- likely_output_failures_caused_by_prompt: passivité planner alors que l’EC2 est joignable, réouvertures pilotées par projections, churn `none_no_signal/retry/takeover`, redispatch décoratif au lieu de collect/backoff/claim utile

Optimization
- keep: la décision tick collect-first, la distinction `IN_PROGRESS` vs `READY`, le subagent ciblé, la règle résidu historique -> cleanup admin + retry, et la preuve planner existante
- simplify: une ligne d’autorité compacte au lieu de recopier la doctrine Judge-parity
- remove: l’ambiguïté implicite qui laissait `monitor/guardian/queue` influencer l’état actif ou la clôture produit
- authority_fix: ajout explicite `EC2 public = vérité produit; VM runtime truth = vérité exécution; projections = advisory` + verrou `product_done`
- continuity_fix: ajout explicite `EC2 joignable + aucun batch actif runtime => create_or_claim_now ou repair immédiat`
- backoff_fix: ajout explicite `none_no_signal|retry|takeover` stériles => backoff + action d’unblock/fix
- expected_runtime_impact: direct sur le prochain tick planner, avec meilleure priorisation claim/collect/backoff et moins de faux blockers advisory

Patch proposal
- patch_type: cleanup / authority-fix / anti-churn
- create_now: yes
- target_file: `/Users/venom/Documents/analyse-financiere/platform/automation/cron_tmux_role_runner.sh`
- exact_goal: injecter la hiérarchie d’autorité, la continuité EC2 et le backoff stérile dans `ROLE=planner` sans regonfler le prompt
- expected_gain: moins de `none_no_signal`, moins de réouvertures projection-only, moins de lanes wait-only, plus de `create_or_claim_now` ou `product_done` corrects
- risk: low-to-medium; le bloc planner est central, donc une sur-agressivité serait visible vite

Measurement
- signals_to_watch: `none_no_signal`, `retry/takeover` récurrents, batch re-open après preuve EC2, outputs planner qui classent `ops_clean=no` comme blocage produit, présence d’un backoff quand des lanes stériles persistent
- success_criteria: baisse des sorties passives quand EC2 est joignable; un batch prouvé sur EC2 n’est plus rouvert par `guardian/monitor/queue`; les lanes stériles passent en backoff au lieu de reconsommer
- rollback_condition: hausse des claims/autobatches agressifs alors qu’une lane active canonique existe, ou perte du collect-first sur une vraie tâche downstream active

## 2026-04-16T23:53:08Z orchestration-architect

- `BATCH-96-DEV-01` is now a real shipped delta, not just planner movement: runtime truth merged it at `23:50:23Z`, and public EC2 `GET /api/personal-finance/start?tickers=NVDA` now returns non-empty `brief_of_day.what_changed_today` and `what_matters_now`.
- Product verdict changed accordingly: `ec2_reachable=yes`, `app_progress=yes`, `product_done=yes`, but `ops_clean=no` because monitor/advisory surfaces are still stale or broken.
- Canonical next step is `claim BATCH-96-DEV-02`; do not open a new batch while `BATCH-96` remains actionable in runtime truth.
- Main false progress now is control-plane residue: guardian still points to `DEV-01`, `executors-monitoring-latest.json` is stale, and public monitor `/api/status` is `500` even though product endpoints remain healthy.

## 2026-04-16T10:08:00Z po-vision-batch-architect

- Audit vision produit / architecture / runtime refait avec sources canoniques, mémoire récente, VM truth et endpoints publics EC2.
- Vérité planner VM: `BATCH-94` est l’unique batch canonique actif (`BATCH-94-ARCH`, `IN_PROGRESS`); aucune justification pour ouvrir un second batch.
- Vérité produit EC2: `GET /api/copilot/start`, `GET /api/personal-finance/start?tickers=NVDA` et `GET /api/judge/personal-finance/start?tickers=NVDA` retournent maintenant tous un brief visible + `ask/open`; le blocage public observé plus tôt sur fallback vide n’est plus d’actualité.
- Gap restant: le flux n’est pas encore franchement `portfolio/watchlist-first` (`ranked_action=market`, risque principal encore `AAPL` même avec `NVDA` scope), donc la bonne cible reste la finition de `BATCH-94`, pas un nouveau batch.
- Décision: `create_now=no`. Aucun module Plane créé. Le plus petit blocage concret à surveiller est la conversion de `BATCH-94-ARCH` en dispatch dev/admin avec preuve publique EC2, au lieu d’un progrès limité à l’architecture/planner.

## 2026-04-16T12:03:41Z po-vision-batch-architect

- Audit vision/runtime/public EC2 refait après nouvelle itération planner.
- Vérité runtime locale: `BATCH-95` est désormais le batch canonique actif; `BATCH-95-DEV-01` est `DONE` avec preuve publique EC2, `BATCH-95-DEV-02` est `IN_PROGRESS`.
- Vérité produit publique EC2 confirmée: `GET /api/copilot/start?tickers=NVDA` et `GET /api/personal-finance/start?tickers=NVDA` ouvrent bien maintenant le ticker demandé via `ranked_action=open_nvda` / `target=ticker:NVDA`.
- Décision inchangée: `create_now=no`. Aucun batch Plane à créer, car le scope actif couvre déjà le bon delta produit (`portfolio_first_brief_with_ranked_actions`).
- Plus petit blocage concret: la boucle planner/runtime ne converge pas proprement. `planner-guardian-latest.json` et `parallel-workstreams.json` poussent `BATCH-95-DEV-02`, mais `executors-monitoring-latest.json` est `STALE` et montre `dev/admin delta=NO_DELTA`. Le problème n’est donc pas le backlog, mais la preuve de convergence vers un second delta visible/public EC2.

## 2026-04-16T07:03:03Z po-vision-batch-architect

- Audit vision/architecture/runtime effectué avec lecture canonique (`SOUL.md`, `USER.md`, `MEMORY.md`, memories agents, docs vision/API/Judge parity) + vérification VM/EC2.
- Décision: **aucun nouveau batch**. `BATCH-92` est le bon scope actif et il doit rester l’unique batch ouvert.
- Pourquoi: `planner-guardian-latest.json` côté VM annonce un cycle canonique actif sur `BATCH-92-DEV-01` et recommande explicitement de ne pas créer un nouveau batch.
- Vérité produit publique EC2:
  - `GET /api/judge/personal-finance/start?tickers=NVDA` retourne bien un brief riche + `ranked_action` + `ask/open` + metadata.
  - `GET /api/copilot/start` et `GET /api/personal-finance/start?tickers=NVDA` retombent encore sur `fallback_used=copilot_start_never_empty` avec `No daily brief available yet.`
- Conclusion produit: le meilleur backend (`judge`) est déjà livrable publiquement, mais la porte d’entrée produit (`copilot`/`personal-finance`) n’est pas encore alignée. Le plus petit blocage concret est donc de fermer cet écart public EC2, pas d’ajouter un nouveau batch.
- Décision Plane: `create_now=no` car un batch canonique actif couvre déjà exactement le bon objectif utilisateur visible.

## 2026-04-16T05:04:23Z role-prompt-engineer

Continuity
- previous_target_role: planner
- previous_prompt_issue: le bruit planner résiduel n’était plus dans le bloc `ROLE=planner` ni dans les patches guardian, mais dans la couche partagée du runner
- changed_since_last_run: `planner-guardian-latest.json` est green et `planner-prompt-patches.json` est vide; la prochaine coupe utile était donc `ORCHESTRATION_SHARED_PROMPT`

Target
- role: planner
- prompt_source: `/Users/venom/Documents/analyse-financiere/platform/automation/cron_tmux_role_runner.sh`
- why_this_prompt_now: réduire la couche commune enlève de la doctrine planner-only répétée à chaque dispatch sans retoucher la logique runtime

Prompt audit
- useful_rules: vérité runtime, blocker permission prouvé, claim avant complete, commit avant close, gate de valeur, interdiction d’analyse passive
- redundant_rules: `PLANNER_ORCHESTRATOR`, `PLANNER_SUBAGENT_RULE`, `EXPLORER_POLICY`, `DYNAMIC_WORKERS`, `WORKER_RULE`
- contradictory_rules: close-phase commun trop lourd par rapport aux règles légères déjà prévues pour claim/handoff planner
- too_long_or_noisy_sections: 2981 caractères pour le protocole commun, avec beaucoup de règle planner-only dupliquée
- missing_tool_guidance: la notion “plus petit pas réel utile” n’était pas assez nette
- likely_output_failures_caused_by_prompt: dilution collect/claim/complete, churn retry/takeover, faux besoin de sur-remplir la preuve planner
- architecture_doctrine_overcopy: doctrine endpoint non surcopiée, mais doctrine planner-only oui

Optimization
- keep: règles transverses utiles
- simplify: protocole commun recentré sur truth/claim/proof/complete
- remove: détails planner-only et compat legacy qui n’apportent rien aux autres lanes
- move_out_of_prompt: none
- tool_usage_improvements: une seule ligne claire pour “subagent = preuve, parent = décision”
- expected_runtime_impact: structurel immédiat, direct au prochain cycle planner actif

Patch proposal
- patch_type: cleanup / shorten / anti-churn
- create_now: yes
- target_file: `/Users/venom/Documents/analyse-financiere/platform/automation/cron_tmux_role_runner.sh`
- exact_goal: faire passer `ORCHESTRATION_SHARED_PROMPT` de 2981 à 1554 caractères
- expected_gain: moins de bruit prompt et meilleure lisibilité de l’action canonique
- risk: low à medium parce que la couche est globale

Measurement
- signals_to_watch: `none_no_signal`, `planner_quality_autofill_missing`, `planner_evidence_incomplete_soft`, `ready_but_none_task_update`, `handoff_same_task_streak`, churn `retry/takeover`
- success_criteria: baisse de churn sur le prochain cycle planner actif sans régression dev/admin/scrum
- rollback_condition: hausse des guardian issues ou perte de clarté cross-role après le raccourcissement

Decision
- next_owner: prompt-expert
- next_action: attendre un tick planner non-idle et mesurer si le bruit dominant a quitté la couche partagée

## Admin unblock summary
- blocker: `fc_doctor` pointait encore par défaut vers `127.0.0.1:8050` et `127.0.0.1:7779`, ce qui fabriquait un faux `product_runtime=degraded` sur la VM
- fix: `platform/automation/fc_doctor.py` bascule maintenant sur `FC_PUBLIC_APP_BASE_URL` / `FC_PUBLIC_MONITOR_BASE_URL` avec défauts EC2 publics + test ciblé de précédence
- resume: yes
- next_owner: planner

## 2026-04-16T11:06:00Z po-vision-batch-architect

- Nouveau contrôle complet VM + EC2 public effectué après clôture de `BATCH-94`.
- Vérité runtime VM: plus aucun batch canonique actif; `priority-queue.json` et `parallel-workstreams.json` ont `active_batch_ids=[]`, et `planner-guardian-latest.json` est green/idle (`NO_ACTIVE_CANONICAL_WORK`).
- Conclusion planner: le planner a bien convergé vers une livraison réelle sur le dernier cycle; il ne reste pas de batch ouvert bloqué sans delta utilisateur visible.
- Vérité produit EC2 confirmée:
  - `GET /api/health` = backend public ok.
  - `GET /api/copilot/start`, `GET /api/personal-finance/start?tickers=NVDA`, `GET /api/judge/personal-finance/start?tickers=NVDA` = brief + ask/open publics et utilisables.
  - `GET /api/status?lite=1` côté monitor EC2 reste app-first/advisory et ne doit pas être lu comme vérité control-plane.
- Gap produit restant et réellement visible: le starter n’est pas encore suffisamment portfolio/watchlist-first.
  - `ranked_action` reste `market`.
  - le brief pousse encore `AAPL` comme top risk même quand le scope demandé est `NVDA`.
- Décision backlog:
  - pas de création Plane dans ce run (`create_now=no`, Plane non disponible ici),
  - mais le prochain batch utile est maintenant clair et indépendant: rendre la priorisation du starter réellement scope/portfolio/watchlist-first côté backend public.
- Architecture pressentie pour ce prochain batch:
  - réutiliser `packages/contracts/copilot_v1.py`,
  - corriger l’assemblage/ranking dans `apps/api/src/domains/judge/application/intelligence_service.py` et `apps/api/src/domains/judge/application/judge_endpoint_service.py`,
  - garder `apps/api/src/domains/copilot/application/copilot_service.py` comme adaptateur,
  - ne pas déplacer la logique métier dans `apps/api/src/domains/copilot/api/copilot.py`,
  - preuve finale sur EC2 public, pas sur loopbacks VM.

## 2026-04-16T05:17:00Z role-prompt-engineer

Continuity
- previous_target_role: planner
- previous_prompt_issue: après le nettoyage du protocole commun du runner, il fallait confirmer le prochain bruit dominant avant de retoucher une autre couche
- changed_since_last_run: lecture VM via `scripts/vm_ssh_exec.sh` = planner guardian green/idle, aucun patch dynamique actif, mais historique `legacy/planner-subagents-results` encore rempli de `invalid_subagent_result:start_banner_only`

Target
- role: planner
- prompt_source: `/Users/venom/Documents/analyse-financiere/platform/automation/planner_subagent_manager.py`
- why_this_prompt_now: malgré l’idle courant, la couche commune des sous-agents planner-owned restait la seule cible unique encore justifiée par des signaux réels

Prompt audit
- useful_rules: JSON unique, scope owner-task, conscience produit courte, guidance rôle-spécifique
- redundant_rules: peu de redondance restante
- contradictory_rules: JSON final exigé, mais pas assez d’ordre explicite pour arrêter l’exploration dès preuve/blocage utile
- too_long_or_noisy_sections: problème sémantique plus que volumique
- missing_tool_guidance: “premier octet = {”, summary finale non narrative, sortie finale immédiate
- likely_output_failures_caused_by_prompt: payloads narratifs/non mergeables, retries planner évitables
- architecture_doctrine_overcopy: no

Optimization
- keep: squelette court existant
- simplify: rendre la sortie finale plus impérative
- remove: formulations molles autour de la progression
- move_out_of_prompt: none
- tool_usage_improvements: `{...}` immédiat, summary de résultat final, arrêt immédiat dès preuve/blocage réel
- expected_runtime_impact: direct mais modéré sur la forme des payloads, surtout utile si le backend répond déjà mais enrobe trop

Patch proposal
- patch_type: tool-guidance / anti-churn / shorten
- create_now: yes
- target_file: `/Users/venom/Documents/analyse-financiere/platform/automation/planner_subagent_manager.py`
- exact_goal: passer le bloc `_build_prompt()` de 1746 à 1670 caractères tout en ajoutant un contrat de sortie plus dur
- expected_gain: moins de sorties narratives et meilleure collectabilité planner
- risk: low

Measurement
- signals_to_watch: `invalid_subagent_result:start_banner_only`, `planner_subagent_rejected`, `subagent_status_failed`, résultats avec `recommended_next=none`
- success_criteria: baisse des payloads non mergeables au prochain cycle planner-owned actif
- rollback_condition: aucun effet ou dégradation de la valeur utile des résultats

Decision
- next_owner: prompt-expert
- next_action: mesurer au prochain sous-agent planner-owned si le dominant reste purement backend/startup ou si la nouvelle dureté du prompt réduit déjà le churn

## 2026-04-16T05:25:48Z role-prompt-engineer

Continuity
- previous_target_role: planner
- previous_prompt_issue: après les deux derniers patches, il fallait relire la doc canonique avant toute nouvelle itération
- changed_since_last_run: relecture de `PRODUCT_VISION`, `JUDGE_PARITY_ENDPOINT_ARCHITECTURE`, `API_ENDPOINT_BEST_PRACTICES`; aucun patch supplémentaire

Target
- role: planner
- prompt_source: `documentation canonique`
- why_this_prompt_now: verrouiller l’alignement produit/backend-first/Judge-parity avant de continuer les optimisations de prompt

Prompt audit
- useful_rules: produit = brief+ask + memo explainable; frontend theme protégé; backend-first; Judge = référence endpoint non triviale; routes minces; contrats partagés; degraded explicite; validations publiques via EC2
- redundant_rules: recopier Judge-parity dans des prompts non endpoint
- contradictory_rules: none
- too_long_or_noisy_sections: danger futur = prompts qui remplacent la doc au lieu de la pointer
- missing_tool_guidance: mieux vaut référencer la doc que grossir les prompts
- likely_output_failures_caused_by_prompt: sur-bruit doctrinal et dérive de focus produit
- architecture_doctrine_overcopy: à éviter explicitement

## Orchestration architect summary
- verdict: progrès produit réel sur EC2 public, mais convergence planner/projections encore mauvaise car `BATCH-95` reste ouvert localement après merge canonique VM
- top priority: réconcilier `BATCH-95` dans `priority-queue.json` / `parallel-workstreams.json` avec la vérité VM (`runtime_actionable=false`, `merged` pour `DEV-01/02/03`)
- blocker: batch actif fantôme côté projections locales (`BATCH-95-DEV-03` / `BATCH-95-ADMIN-01`) alors que la VM canonique n’a plus de tâche active
- next useful delivery: formaliser un smoke public EC2 starter-first puis fermer `BATCH-95` côté control-plane local

## Admin unblock summary
- blocker: absence d'etat canonique monotone entre preuve publique EC2, idleness runtime et guardian; resultat = faux progres planner-only + risque d'auto-stop EC2 a vide
- fix: `product_done` rendu monotone dans `runtime_truth_reader.py`, bruit idle de projection degrade en `residue_detected` dans `planner_guardian.py`, et `aws_ec2_idle_stop.sh` passe en watchdog de joignabilite par defaut
- resume: yes
- next_owner: planner

Optimization
- keep: conscience produit minimale et utile
- simplify: doc canonique comme source, prompt comme exécution
- remove: annexes doctrinales longues
- move_out_of_prompt: architecture détaillée vers la doc
- tool_usage_improvements: none
- expected_runtime_impact: structurel

Patch proposal
- patch_type: none
- create_now: no
- target_file: none
- exact_goal: aucun nouveau patch dans ce run
- expected_gain: éviter de continuer sans réalignement doc
- risk: low

Measurement
- signals_to_watch: prochaines couches prompt qui re-gonflent ou recopient la doctrine endpoint
- success_criteria: prochaine optimisation courte, ciblée, doc-aligned

## Admin unblock summary
- blocker: le handoff `planner -> dev` sur `BATCH-95-DEV-02` était cassé par `role_contract_guard.py`, qui auto-remplissait un contrat incomplet `dev` sans `stream_id/task_id`
- fix: `platform/policies/role_contract_guard.py` restaure maintenant le scope canonique depuis le workboard actif pendant l’autofill; test de régression ajouté dans `platform/automation/tests/test_role_contract_guard.py`
- resume: yes
- next_owner: dev
- rollback_condition: none

## Orchestration architect summary
- verdict: valeur produit publique réelle sur EC2 (`start` scope NVDA), mais orchestration encore incohérente entre VM truth, guardian et monitor public.
- top priority: finir `BATCH-95-DEV-03` pour rendre le brief/risk réellement scope/portfolio-first, pas seulement `ranked_action=open_nvda`.
- blocker: `planner-guardian-latest.json` continue de pointer `BATCH-95-DEV-02` pendant que queue/workboard + VM snapshot exposent `BATCH-95-DEV-03` comme seul runnable.
- next useful delivery: prouver sur EC2 public un starter `personal-finance` dont le brief/risk principal suit le ticker demandé.

Decision
- next_owner: prompt-expert
- next_action: reprendre avec un seul patch court, explicitement aligné sur backend-first et sans recopie Judge-parity

## Admin unblock summary
- blocker: `role_contract_guard` et `planner_runtime_actions` décidaient encore sur les probes `127.0.0.1`, donc le faux runtime-down pouvait revenir dans les guards actifs
- fix: défauts actifs réalignés sur `FC_PUBLIC_APP_BASE_URL` / `FC_PUBLIC_MONITOR_BASE_URL` avec cibles EC2 publiques + 2 tests ciblés
- resume: yes
- next_owner: planner

## EC2 app-only monitor semantics
- `apps/monitor/server.py` tague maintenant explicitement le host monitor (`profile=app_only` vs `orchestrator`) et neutralise les gaps d’issue publication / blockers / stale-context comme causes de `DEGRADED` sur l’hôte EC2 app-only.
- `apps/monitor/services/status_service.py` traite désormais `agentic_runtime` et `openclaw_gateway` comme surfaces advisory quand `control_plane_location=remote_vm`, et aligne `health` public sur `product_runtime.status`.
- Rien n’a encore été resyncé vers AWS après ce patch local.

## Agent model policy sweep
- Les configs runner actives et legacy convergent maintenant vers `gpt-5.4` + `high` pour `planner`, `dev`, `admin`, `scrum_master`.
- La relance tmux dans `platform/automation/auto_recover_tmux_roles.sh` force désormais `codex --model gpt-5.4 -c 'model_reasoning_effort="high"'`.
- Les defaults actifs restants en `gpt-5.3-codex-spark` sont limités au fallback rate-limit et au model plane secondaire; les primaires cachés dans `planner_subagent_manager`, `worker_manager`, `openclaw_control_plane`, et les scripts de provisioning cron ont été alignés sur `gpt-5.4/high`.

## Admin unblock summary
- blocker: `executors-monitoring-latest.json` gardait `planner=BLOCKED / PLANNER_RUNTIME_ACTIONS_FAILED` alors que la vérité canonique VM était idle sans active cycle
- fix: `platform/automation/role_execution_monitoring.py` démote désormais ce cas en `IDLE/NO_ACTIVE_CANONICAL_WORK`; test ciblé ajouté
- resume: yes
- next_owner: planner

## Admin unblock summary
- blocker: `fc_doctor` marquait encore `scheduler_authority=error` sur la VM parce qu’il n’acceptait que le vieux chemin `vm_resume_guard`, alors que les jobs OpenClaw cron actifs pilotent bien le runtime
- fix: `platform/automation/fc_doctor.py` reconnaît désormais les jobs OpenClaw activés dans `~/.openclaw/cron/jobs.json` comme autorité scheduler canonique, avec test ciblé ajouté
- resume: yes
- next_owner: dev

## 2026-04-16T06:02:12Z po-vision-batch-architect

## Admin unblock summary
- blocker: `planner_guardian` pouvait encore transformer un runtime canonique idle en faux `PLANNER_RUNTIME_ACTIONS_FAILED` en croyant des compteurs projetés `queue_has_ready/workboard_role_has_work`
- fix: `platform/automation/planner_guardian.py` traite maintenant `projection_decision_reason=runtime_idle_no_active_cycle` comme état idle autoritaire et neutralise ces pénalités + régressions ciblées ajoutées
- resume: yes
- next_owner: planner

- Audit vision/runtime/public app terminé.
- Vérification explicite de livraison réelle: `http://3.98.20.77/api/health`, `http://3.98.20.77/api/copilot/start`, `http://3.98.20.77/api/judge/personal-finance/start` et `http://3.98.20.77:8080/api/status?lite=1` répondent bien publiquement.
- Vérification explicite de convergence planner: `priority-queue.json` et `parallel-workstreams.json` portent `active_batch_ids=[]`; aucun batch canonique ouvert ne justifie un nouveau lot produit.
- Vérification explicite du faux progrès: `planner-graph-state.json` garde encore des reliquats `BATCH-88-ADMIN-01` et `BATCH-90-ADMIN-01` alors que le produit public est déjà livrable; le vrai sujet restant est donc runtime/planner, pas un batch net-new.
- Décision: aucun batch créé maintenant, aucune écriture Plane; proposition fermée sur `create_now=no`.

## 2026-04-16T05:32:43Z role-prompt-engineer

Continuity
- previous_target_role: planner
- previous_prompt_issue: les derniers patches avaient réduit le bruit planner; la demande actuelle visait l’alignement prompt avec la nouvelle réalité d’environnement
- changed_since_last_run: inspection ciblée des prompts actifs = `REGLES_APP_AWS` déjà présent, mais le bloc `ROLE=admin` n’explicitait pas assez la séparation `EC2 publique = produit` / `VM UTM = control-plane`

Target
- role: admin
- prompt_source: `/Users/venom/Documents/analyse-financiere/platform/automation/cron_tmux_role_runner.sh`
- why_this_prompt_now: admin est la lane la plus exposée aux faux diagnostics runtime liés à l’ancienne réalité loopback/VM

Prompt audit
- useful_rules: ordre strict d’action admin, budget borné, takeover, preuve stricte
- redundant_rules: la réalité AWS existait déjà globalement mais pas assez au point d’usage admin
- contradictory_rules: `fc_health_check.sh` appelé sans clarification suffisante de sa portée publique
- too_long_or_noisy_sections: le bloc admin parlait plus d’audit/takeover que de frontière environnementale
- missing_tool_guidance: vérité produit publique EC2 vs vérité control-plane VM
- likely_output_failures_caused_by_prompt: faux blockers runtime, confusion sur `127.0.0.1:8050|7779`
- architecture_doctrine_overcopy: none

Optimization
- keep: squelette admin
- simplify: pré-analyse et step 1 alignés sur le runtime split réel
- remove: un peu de verbosité annexe pour garder le bloc plus court
- move_out_of_prompt: none
- tool_usage_improvements: `fc_health_check.sh` requalifié en preuve produit/public
- expected_runtime_impact: direct

Patch proposal
- patch_type: cleanup / tool-guidance / anti-churn
- create_now: yes
- target_file: `/Users/venom/Documents/analyse-financiere/platform/automation/cron_tmux_role_runner.sh`
- exact_goal: mettre à jour `ROLE=admin` avec la nouvelle réalité d’environnement tout en passant de 3861 à 3832 caractères
- expected_gain: moins de faux diagnostics runtime et meilleure séparation produit/control-plane
- risk: low

Measurement
- signals_to_watch: prochains ticks admin, faux blockers loopback, mauvais `CRON_SCHEDULE_MISSING`
- success_criteria: les blockers produit sont prouvés sur EC2 publique, les blockers orchestration sur VM UTM

## 2026-04-16 infra follow-up
- Réalignement complémentaire fait sur les gates locales restantes: `platform/automation/delivery_value_gate.py` et `platform/automation/state_reconciler.py` utilisent maintenant par défaut les endpoints publics EC2 (`FC_PUBLIC_*`) au lieu des loopbacks VM pour les preuves produit.
- `apps/monitor/README.md` clarifie maintenant explicitement la séparation `127.0.0.1:7779 = monitor orchestration VM` vs `http://3.98.20.77:8080 = monitor public EC2`.
- Validation ciblée PASS: nouveaux tests sur les défauts EC2 publics + `py_compile` OK.
- Baseline inchangée: la suite large `test_state_reconciler.py` contient encore des échecs préexistants non liés à ce patch.
- rollback_condition: si la distinction produit/control-plane devient moins lisible

Decision
- next_owner: prompt-expert
- next_action: vérifier sur le prochain tick admin que l’EVIDENCE cite la bonne surface de vérité

## 2026-04-16T05:35:52Z role-prompt-engineer

Continuity
- previous_target_role: admin
- previous_prompt_issue: la séparation EC2 publique / VM UTM était en place, mais il restait à intégrer le cas concret du screenshot sur les artefacts planner manquants
- changed_since_last_run: `ROLE=admin` traite maintenant l’absence de `executors-monitoring-latest.json` / `agent-iteration-issues*` sur EC2 app-only comme advisory/planner-gap, pas blocker produit; bloc admin recompressé à 3788 caractères

Target
- role: admin
- prompt_source: `/Users/venom/Documents/analyse-financiere/platform/automation/cron_tmux_role_runner.sh`
- why_this_prompt_now: le screenshot confirmait que le vrai résidu d’environnement n’était plus le sync ni le loopback, mais la mauvaise lecture du planner-gap public

Prompt audit
- useful_rules: frontière EC2/VM, preuve publique via `fc_health_check.sh`, preuves control-plane via runtime truth
- redundant_rules: none
- contradictory_rules: sans la nouvelle ligne, un planner-gap EC2 pouvait encore ressembler à un faux blocker produit
- too_long_or_noisy_sections: recompression faite dans le même run
- missing_tool_guidance: cas artefacts monitor/planner absents sur app-only host
- likely_output_failures_caused_by_prompt: faux blockers monitor publics
- architecture_doctrine_overcopy: none

Optimization
- keep: bloc admin environment-aware
- simplify: ligne courte sur planner-gap advisory
- remove: verbosité périphérique
- move_out_of_prompt: none
- tool_usage_improvements: meilleure lecture des signaux monitor publics
- expected_runtime_impact: direct

Patch proposal
- patch_type: cleanup / tool-guidance / anti-churn
- create_now: yes
- target_file: `/Users/venom/Documents/analyse-financiere/platform/automation/cron_tmux_role_runner.sh`
- exact_goal: intégrer le cas planner-gap du screenshot tout en gardant le prompt plus court que la version d’origine
- expected_gain: moins de confusion `issue_publication_gap` vs blocker produit
- risk: low

Measurement
- signals_to_watch: ticks admin avec `issue_publication_gap_roles=["planner"]`
- success_criteria: advisory seulement sur EC2 app-only
- rollback_condition: none

Decision
- next_owner: prompt-expert
- next_action: attendre un tick admin ou monitor public où le planner-gap apparaît

## 2026-04-16T05:41:44Z role-prompt-engineer

Continuity
- previous_target_role: admin
- previous_prompt_issue: le runner admin était réaligné sur `EC2 produit / VM control-plane`, mais la variante admin de `planner_subagent_manager.py` n’avait pas encore le cas concret des artefacts planner/monitor absents sur EC2 app-only
- changed_since_last_run: audit des autres couches prompt actives = `planner_guardian.py` et `planner_companion_tick.sh` laissés intacts; seul le prompt admin des sous-agents nécessitait encore une mise à jour utile

Target
- role: admin
- prompt_source: `/Users/venom/Documents/analyse-financiere/platform/automation/planner_subagent_manager.py`
- why_this_prompt_now: éviter qu’un sous-agent admin planner-owned retransforme un planner-gap public EC2 en faux blocker runtime après les patches déjà faits dans le runner admin

Prompt audit
- useful_rules: JSON final immédiat, scope owner-task, admin borné au runtime/control-plane
- redundant_rules: aucune
- contradictory_rules: séparation `product truth` / `control-plane truth` incomplète sans le cas concret `executors-monitoring-latest.json` / `agent-iteration-issues*`
- too_long_or_noisy_sections: non
- missing_tool_guidance: artefacts planner absents sur EC2 app-only = advisory
- likely_output_failures_caused_by_prompt: faux blockers admin-subagent, mauvais `recommended_next`, collect planner inutile
- architecture_doctrine_overcopy: no

Optimization
- keep: bloc court existant
- simplify: une ligne enrichie au lieu d’une nouvelle section
- remove: aucune couche supplémentaire patchée
- move_out_of_prompt: none
- tool_usage_improvements: cohérence runner admin / subagent admin sur le même host split
- expected_runtime_impact: direct mais ciblé

Patch proposal
- patch_type: tool-guidance / anti-churn
- create_now: yes
- target_file: `/Users/venom/Documents/analyse-financiere/platform/automation/planner_subagent_manager.py`
- exact_goal: ajouter que `executors-monitoring-latest.json` / `agent-iteration-issues*` manquants sur EC2 app-only restent advisory, pas blocker produit
- expected_gain: moins de faux diagnostics admin-subagent et moins de churn planner après collect
- risk: low

Measurement
- signals_to_watch: `planner_subagent_rejected`, payloads admin-subagent citant planner-gap / issue_publication_gap / artefacts monitor manquants comme blocker produit
- success_criteria: ces signaux deviennent advisory dans les prochains résultats admin-subagent
- rollback_condition: aucun effet observable ou bruit accru

Decision
- next_owner: prompt-expert
- next_action: attendre le prochain sous-agent admin planner-owned; ne pas retoucher `planner_guardian.py` ni `planner_companion_tick.sh` sans nouveau signal réel

## 2026-04-16T05:49:07Z role-prompt-engineer

Continuity
- previous_target_role: admin
- previous_prompt_issue: après l’alignement infra EC2/VM, il fallait vérifier si planner repartait bien vers de la valeur au lieu de s’auto-bloquer sur des résidus control-plane
- changed_since_last_run: lecture VM = `planner_guardian` green (score 85), `active_batch_ids=[]`, `runtime_actionable=false`, workboard sans tâche ouverte; malgré ça, le dernier contrat planner était encore `BLOCKED` sur `BATCH-88-ADMIN-01` à cause d’un `planner_graph_state` SQLite résiduel

Target
- role: planner
- prompt_source: `/Users/venom/Documents/analyse-financiere/platform/automation/cron_tmux_role_runner.sh`
- why_this_prompt_now: le frein observé n’était plus un mauvais split d’environnement, mais un planner qui ne distinguait pas assez un résidu runtime historique d’un vrai blocker produit/canonique

Prompt audit
- useful_rules: collect-first, active-task-first, autobatch quand plus rien d’exécutable, no passive analysis
- redundant_rules: aucune
- contradictory_rules: “rester non passif” existait, mais sans cas explicite pour `secondary_compat_only` / graph_state orphelin d’un batch complété
- too_long_or_noisy_sections: le prompt ne devait pas regrossir; recompaction faite dans le même patch
- missing_tool_guidance: sur résidu SQLite historique seul + aucune tâche canonique ouverte, planner doit lancer un cleanup admin puis retry, pas conclure `BLOCKED`
- likely_output_failures_caused_by_prompt: autobatch gelé, faux blockers planner, pas de nouveau delta produit/APIs malgré runtime canonique vide
- architecture_doctrine_overcopy: no

Optimization
- keep: structure planner existante
- simplify: une seule règle opératoire claire pour le cas résidu historique
- remove: verbosité non essentielle dans mission/produit/preuve pour limiter la croissance du bloc; taille finale `2524 -> 2614`
- move_out_of_prompt: none
- tool_usage_improvements: `admin cleanup/reconcile` devient le chemin explicite quand `planner-autobatch` bute seulement sur un résidu `secondary_compat_only`
- expected_runtime_impact: direct sur la reprise planner après migration

Patch proposal
- patch_type: tool-guidance / anti-churn
- create_now: yes
- target_file: `/Users/venom/Documents/analyse-financiere/platform/automation/cron_tmux_role_runner.sh`
- exact_goal: empêcher planner de s’auto-bloquer sur un vieux `graph_state` quand il n’y a plus de batch canonique actif
- expected_gain: moins de waits bloqués, plus de reprise vers autobatch ou cleanup admin utile
- risk: low

Measurement
- signals_to_watch: contrats planner `BLOCKED` avec `active_batch_ids=[]` et `runtime_actionable=false`; `runtime_truth_residue_active`; déclenchement d’un admin cleanup au lieu d’un wait bloqué
- success_criteria: le prochain cas similaire route un cleanup/retry au lieu d’un blocage planner sec
- rollback_condition: pas d’effet observable ou croissance prompt non justifiée

Decision
- next_owner: prompt-expert
- next_action: surveiller le prochain tick planner sans batch actif pour voir si le résidu SQLite devient un cleanup admin temporaire plutôt qu’un faux blocker de delivery

## 2026-04-16T23:25:00Z codex-automations-mac
- updated local Codex automation prompts on this Mac:
  - `/Users/venom/.codex/automations/deblock/automation.toml`
  - `/Users/venom/.codex/automations/orchestrator-agent/automation.toml`
  - `/Users/venom/.codex/automations/po-vision-batch-architect/automation.toml`
  - `/Users/venom/.codex/automations/prompt-expert/automation.toml`
- aligned prompts with the new infra split:
  - product/app proof defaults must target public EC2 via `FC_PUBLIC_APP_BASE_URL` / `FC_PUBLIC_MONITOR_BASE_URL`
  - VM loopbacks stay control-plane only
  - loopback-only degraded is not a product blocker
- verification:
  - only four local Codex automations exist on this Mac
  - all four prompts now mention the AWS/VM split and the public-vs-control-plane proof rule explicitly

## 2026-04-16T23:35:00Z codex-automation-loop
- activated the two local Codex automations that were still paused:
  - `/Users/venom/.codex/automations/orchestrator-agent/automation.toml`
  - `/Users/venom/.codex/automations/po-vision-batch-architect/automation.toml`
- orchestration loop is now continuous on this Mac:
  - `orchestrator-agent` active every 10 minutes
  - `deblock` active hourly
  - `po-vision-batch-architect` active hourly
  - `prompt-expert` already active hourly
- prompt scope tightened:
  - `orchestrator-agent` must now state explicitly whether planner still converges, whether open batches are really advancing, and whether newly written APIs are actually public and usable on EC2
  - `po-vision-batch-architect` must reject batches that cannot converge and prefer batches that produce a public EC2-usable endpoint/contract/flow

## 2026-04-16T23:52:00Z post-migration-baseline-check
- immediate baseline after enabling the continuous loop:
  - EC2 public app runtime: healthy
  - EC2 public monitor: healthy
  - VM runtime host check: healthy on `/home/venom/analyse-financiere`
  - planner control-plane: no active canonical work right now, planner idle
- delivery interpretation:
  - no active open batch is currently progressing
  - no queue/workboard pressure right now
  - product side is live, but orchestration doctor still reports control-plane debt rather than app outage
- runtime truth detail:
  - SQLite is primary again on the VM
  - `quarantined_retryable_residue_count=30`
  - notable delivered residues still visible in quarantine include `BATCH-88-DEV-03` (`/api/copilot/start` contract hardening) and `BATCH-88-DEV-02` (`/personal-finance` open flow)
2026-04-16T23:44:00Z
- Ran a post-migration delivery-efficiency loop on active planner/runtime surfaces.
- VM truth still shows runtime residue/quarantine but no reason to open a new batch; the real risk is planner/admin drifting into passive maintenance instead of shipping.
- Tightened active prompts/guards in `platform/automation/planner_guardian.py`, `platform/automation/runtime/planner/planner_runtime_actions.py`, and `scripts/planner_companion_tick.sh`.
- New rule set: when planner is idle or relaunching, prefer the closest user-visible delta or shared contract/API delivery on the public EC2 runtime; treat runtime repair as a blocker-clearing step, not the end goal.
- No tests run in this pass; verification was static inspection plus VM doctor/dispatch snapshot confirming residue remains a control-flow issue, not a new product-scope gap.
2026-04-16T23:58:00Z
- Strengthened persistent automation prompts so the continuous loop now checks delivery effectiveness after migration, not just infra health.
- Updated `/Users/venom/.codex/automations/po-vision-batch-architect/automation.toml` to require explicit checks that planner converges, open batches are not stalled, and new APIs/routes are genuinely livrable on EC2 public.
- Updated `/Users/venom/.codex/automations/orchestrator-agent/automation.toml` and `/Users/venom/.codex/automations/deblock/automation.toml` with a post-migration delivery loop section focused on public EC2 delivery, not just green runtime signals.
- Verified the new prompt markers are present in all three automation files.
## Orchestration architect summary
- verdict: app slice still public and usable, but orchestration now stalls on contradictory supervision rather than active SQLite work.
- top priority: align planner supervision with quarantined SQLite residue so no idle runtime shows as blocked.
- blocker: `BATCH-88-ADMIN-01` still surfaces as planner blocker in `executors-monitoring-latest.json`.
- next useful delivery: replace the public `/api/personal-finance/start` fallback brief with a real brief.

## 2026-04-16T05:54:48Z role-prompt-engineer

Continuity
- previous_target_role: planner
- previous_prompt_issue: les prompts runtime internes étaient durcis, mais la boucle continue qui juge l’efficacité de livraison après migration restait trop large et pas assez tranchée
- changed_since_last_run: audit des automations locales = `orchestrator-agent` est déjà actif toutes les 10 minutes; la bonne intervention était donc un resserrage de son prompt, pas un changement de cadence

Target
- role: orchestration-architect
- prompt_source: `/Users/venom/.codex/automations/orchestrator-agent/automation.toml`
- why_this_prompt_now: c’est la meilleure couche unique pour suivre en continu “planner marche ? batches convergent ? valeur créée ? nouvelles APIs réellement livrées sur EC2 ?”

Prompt audit
- useful_rules: split VM/EC2, vérité runtime primaire, cible produit, format de rapport clair
- redundant_rules: répétitions AWS, trop de doctrine d’architecture, liste plomberie trop large
- contradictory_rules: audit trop large, pas assez hiérarchisé sur convergence planner/batchs/public delivery
- too_long_or_noisy_sections: prompt monoligne très long et peu maintenable
- missing_tool_guidance: priorité explicite à `planner-guardian`, `planner-prompt-patches`, `planner_board_runtime.py snapshot`; lecture directe `code écrit` vs `endpoint public EC2 utilisable`; classification des vieux `task_id` bloqués sans batch actif comme résidus control-plane
- likely_output_failures_caused_by_prompt: rapports diffus, moins de pression sur le vrai blocker et la vraie livraison
- architecture_doctrine_overcopy: oui, partiellement

Optimization
- keep: cœur produit/runtime/report
- simplify: réécriture complète en prompt multi-ligne plus court et plus direct
- remove: texte mort, répétitions, scans encyclopédiques
- move_out_of_prompt: doctrine détaillée renvoyée aux docs canoniques
- tool_usage_improvements: checklist explicite `planner -> batches -> public delivery -> legacy drift`
- expected_runtime_impact: structurel sur la boucle continue; meilleure qualité des audits de convergence

Patch proposal
- patch_type: restructure / shorten / anti-churn
- create_now: yes
- target_file: `/Users/venom/.codex/automations/orchestrator-agent/automation.toml`
- exact_goal: faire de `orchestrator-agent` un audit continu de convergence post-migration, pas un audit généraliste trop large
- expected_gain: meilleurs verdicts sur planner, batches, valeur créée et APIs publiques
- risk: low

Measurement
- signals_to_watch: prochains rapports `orchestration-architect` avec verdict plus net, top priorities plus concrètes, distinction claire entre résidu control-plane et blocage produit
- success_criteria: les prochains runs tranchent mieux ce qui converge, ce qui bloque, et ce qui est vraiment livré publiquement
- rollback_condition: si le nouveau prompt perd des garde-fous VM/EC2 ou rend les rapports trop pauvres

Decision
- next_owner: prompt-expert
- next_action: laisser tourner `orchestrator-agent` et mesurer si ses prochains audits deviennent plus utiles pour pousser la vraie delivery après migration

## 2026-04-16T05:57:30Z delivery-lane post-ec2 check
- Public EC2 app validation passes: critical_endpoints_smoke, delivery_value_smoke, copilot decision contract.
- copilot/start fixed back to never-empty on public host via existing copilot files.
- planner false-block removed in planner_runtime_actions by ignoring closed/done historical graph-state residue for autobatch gating.
- planner runner prompt crash fixed in cron_tmux_role_runner by removing shell-active backticks from SYSTEM_PROMPT guidance lines.
- Manual planner tick after fixes ended rc=0 with contract: BLOCKER_ID=NONE, DELTA=AUCUNE_TACHE_CANONIQUE_APRES_SANITIZE_SYNC_ET_CLEANUP.
- Queue/workboard state at check: 91 queue items CLOSED, 596 workboard tasks DONE, 0 READY, 0 IN_PROGRESS.

## Admin unblock summary
- blocker: autobatch still self-blocked on projection `stagnation_alert` even with `active_cycle.novelty_target` already present in canonical queue truth
- fix: restored VM cron profile, fixed fractional-UTC stale residue parsing in `planner_runtime_actions.py`, and made `parallel_workstream.py` reuse `active_cycle` novelty metadata during autobatch
- resume: yes
- next_owner: planner

## Admin unblock summary
- blocker: `planner_guardian` pouvait encore lire une tâche `READY_PLANNER` projetée comme travail canonique alors que la queue active était vide, et pénaliser à tort un runtime idle
- fix: `platform/automation/planner_guardian.py` ignore maintenant ce cas `runtime_idle_no_active_cycle` et ne marque plus passivité/dépendance sur un runtime canonique totalement vide; régressions ajoutées dans `platform/automation/tests/test_planner_guardian.py`
- resume: yes
- next_owner: planner

## 2026-04-16T06:05:34Z role-prompt-engineer

Continuity
- previous_target_role: orchestration-architect
- previous_prompt_issue: la dernière itération avait raccourci l’audit continu, mais pas encore revalidé la couche guardian planner après reprise `BATCH-92`
- changed_since_last_run: lecture VM = `planner-guardian-latest.json` dit `IDLE/NO_ACTIVE_CANONICAL_WORK` tandis que `planner-prompt-patches.json` garde un patch actif `claim_or_autobatch_now`

Target
- role: planner
- prompt_source: `/Users/venom/Documents/analyse-financiere/platform/automation/planner_guardian.py`
- why_this_prompt_now: cette contradiction poussait encore une relance planner inutile alors que le runtime canonique était vide

Prompt audit
- useful_rules: `follow_canonical_active_task`, `novelty_target_first`, `planner_delivery_proof_complete`, non-passivité quand runway/READY existent vraiment
- redundant_rules: pression `claim_or_autobatch_now` maintenue même en idle canonique
- contradictory_rules: summary idle/no work vs patch actif anti-passivité
- too_long_or_noisy_sections: problème d’activation, pas de volume
- missing_tool_guidance: règle claire “runtime canonique vide => pas de claim/autobatch ni d’escalade immédiate”
- likely_output_failures_caused_by_prompt: churn planner/autobatch, faux rouge guardian, relances sans delta
- architecture_doctrine_overcopy: no

Optimization
- keep: la pression guardian quand un vrai READY ou un runway ouvert existent
- simplify: helper `_no_canonical_work(...)` partagé par patches/directives
- remove: patch `claim_or_autobatch_now` et directive immédiate en état idle prouvé
- move_out_of_prompt: none
- tool_usage_improvements: meilleure utilisation de la vérité canonique avant d’ordonner `planner-autobatch`
- expected_runtime_impact: direct

Patch proposal
- patch_type: anti-churn / tool-guidance
- create_now: yes
- target_file: `/Users/venom/Documents/analyse-financiere/platform/automation/planner_guardian.py`
- exact_goal: supprimer les injonctions guardian claim/autobatch quand queue/workboard/top-level/cycle sont tous vides
- expected_gain: moins de faux relaunches planner après fermeture réelle du travail
- risk: low

Measurement
- signals_to_watch: `planner-prompt-patches.json.active`, directives guardian, `planner_passive_forbidden_violation`, `planner_autobatch_missing_when_idle`, churn `none_no_signal`
- success_criteria: prochain état idle sans patch actif `claim_or_autobatch_now` ni directive immédiate
- rollback_condition: silence guardian alors qu’un READY/runway réel réapparaît

Decision
- next_owner: prompt-expert
- next_action: relire le prochain run VM de `planner_guardian` et confirmer la disparition de la pression claim/autobatch en idle

## 2026-04-16T07:05:35Z role-prompt-engineer

Continuity
- previous_target_role: planner
- previous_prompt_issue: le dernier patch avait retire la pression guardian en idle, mais la relance `planner_companion` restait encore susceptible de pousser claim/dispatch generique alors qu’un downstream canonique etait deja actif
- changed_since_last_run: lecture VM = `planner-guardian-latest.json` vert (`score=85`) avec `canonical_active_task_id=BATCH-92-DEV-01`, `active_task_role=dev`, `task_update=analysis_only`; `planner-prompt-patches.json` active toujours `follow_canonical_active_task`

Target
- role: planner
- prompt_source: `/Users/venom/Documents/analyse-financiere/scripts/planner_companion_tick.sh`
- why_this_prompt_now: c’etait la seule couche planner active non encore retouchee aujourd’hui qui pouvait encore envoyer un message de claim/delta livrable moins precis que le guardian patch deja actif

Prompt audit
- useful_rules: relance modee, cooldown, pression vers un delta EC2 public
- redundant_rules: messages generiques `Priorise un delta livrable maintenant` / `Lancer la capability utile maintenant`
- contradictory_rules: ces messages pouvaient encore entrer en collision avec `follow_canonical_active_task` quand `BATCH-92-DEV-01` etait deja IN_PROGRESS
- too_long_or_noisy_sections: peu de volume, mais bruit parce que le texte manquait de priorite concrete
- missing_tool_guidance: pas de mention directe de `planner_subagent_manager.py collect` ni de `planner_runtime_actions.py handoff-ack|handoff-close`
- likely_output_failures_caused_by_prompt: `analysis_only`, claim planner inutile, redispatch
- architecture_doctrine_overcopy: no

Optimization
- keep: structure courte du companion et distinction EC2 public
- simplify: tirer une seule consigne canonique depuis `canonical_active_task_*`
- remove: exhortation generique au delta quand la priorite downstream est deja connue
- move_out_of_prompt: none
- tool_usage_improvements: message custom explicite `collect/ack/repair`
- expected_runtime_impact: direct sur la qualite des relances planner

Patch proposal
- patch_type: tool-guidance / anti-churn
- create_now: yes
- target_file: `/Users/venom/Documents/analyse-financiere/scripts/planner_companion_tick.sh`
- exact_goal: faire gagner dans les branches `ready_planner_task` et `dev_ready_count` une instruction explicite `collect/ack/repair` quand `canonical_active_task_role != planner`
- expected_gain: meilleure coherence companion/guardian/planner, moins de churn `analysis_only`
- risk: low

Measurement
- signals_to_watch: `planner-guardian-latest.json.summary.task_update`, `planner-prompt-patches.json.active`, `analysis_only`, `ready_but_none_task_update`, `handoff_same_task_streak`
- success_criteria: le prochain tick sur `BATCH-92-DEV-01` suit `collect/ack/repair` sans message generique de claim
- rollback_condition: si le companion masque un vrai claim planner alors qu’aucun downstream actif n’existe

Decision
- next_owner: prompt-expert
- next_action: relire le prochain tick VM de `planner_companion` / `planner_guardian` et mesurer si `analysis_only` baisse pendant le flux `BATCH-92-DEV-01`
## 2026-04-16T08:06:00Z po-vision-batch-architect

- Relecture complète vision/backend/runtime/public EC2 faite avant toute proposition de batch.
- Décision: **aucun nouveau batch**. `BATCH-92` est déjà `DONE` dans `docs/operations/orchestrator/parallel-workstreams.json` côté VM, donc ouvrir un batch de plus serait du faux progrès.
- Blocage concret le plus petit: l’app publique EC2 ne reflète pas encore le code déjà mergé pour le starter copilot.
  - `GET http://3.98.20.77/api/judge/personal-finance/start?tickers=NVDA&debug=true` retourne bien le contrat riche attendu.
  - `GET http://3.98.20.77/api/copilot/start?tickers=NVDA&debug=true` et `GET http://3.98.20.77/api/personal-finance/start?tickers=NVDA&debug=true` restent sur `fallback_used=copilot_start_never_empty`.
- Réalité code: le rescue Judge est bien présent dans `apps/api/src/domains/copilot/api/copilot.py` et couvert par `apps/api/src/domains/copilot/tests/test_personal_finance_start_judge_rescue.py`; le delta restant est donc publication/runtime, pas architecture produit manquante.
- Consigne backlog: ne pas créer de lot tant que l’écart `code merged -> EC2 public behavior` n’est pas expliqué ou résorbé.

## 2026-04-16T09:14:00Z po-vision-batch-architect

- Audit de suivi refait avec vérité locale + publique:
  - `planner-guardian-latest.json` pointe maintenant vers `BATCH-93-DEV-03` (`IN_PROGRESS`) comme unique tâche canonique active.
  - `docs/operations/orchestrator/parallel-workstreams.json` confirme `BATCH-93` comme seul stream `IN_PROGRESS`; `BATCH-92` est déjà `DONE`.
- Vérité produit EC2 confirmée:
  - `GET http://3.98.20.77/api/judge/personal-finance/start?tickers=NVDA` expose bien le brief + ask/open riche.
  - `GET http://3.98.20.77/api/copilot/start?tickers=NVDA` et `GET http://3.98.20.77/api/personal-finance/start?tickers=NVDA` exposent désormais aussi un brief live Judge-backed avec `fallback_used=null`; le delta visible n’est plus bloqué.
- Interprétation produit:
  - le planner avance encore une livraison réelle, pas seulement de l’analyse, car `BATCH-93-DEV-01` a restauré le starter public EC2 et `BATCH-93-DEV-02` a sécurisé le clic ask/open côté widget.
  - le plus petit travail restant est de fermer `BATCH-93-DEV-03` puis `ADMIN-01`/`GOV_REVIEW`, pas d’ouvrir un autre batch.
- Décision stricte: `create_now=no`, aucun batch Plane à créer. Le backlog doit attendre la clôture complète de `BATCH-93`.

## 2026-04-16T08:04:15Z role-prompt-engineer

- Targeted planner prompt in `/Users/venom/Documents/analyse-financiere/platform/automation/cron_tmux_role_runner.sh`.
- Prompt problem: the main decision rule mixed planner `IN_PROGRESS` and `READY`, which left room for a bad claim on a non-READY planner-owned task and matched the live failure `CLAIM_ERROR: task BATCH-92-GOV_REVIEW not READY for role planner`.
- Patch applied: split the rule so planner now resumes `IN_PROGRESS` work without new claim, and claims only the exact planner task that is explicitly READY.
- Validation: `bash -n /Users/venom/Documents/analyse-financiere/platform/automation/cron_tmux_role_runner.sh` passed.
- Watch next planner VM ticks for lower `planner_runtime_actions_failed` / `CLAIM_ERROR ... not READY`; rollback if legitimate READY planner claims stop firing.

## Admin unblock summary
- blocker: stale canonical `data/news_feed.json` kept `product_value` blocked on `news_stale`, which would have stopped the next canonical continuation despite healthy public EC2 runtime
- fix: ran `bash scripts/vm_safe_exec.sh -- "bash scripts/dev_tools.sh refresh-data"` on the VM and refreshed `news_feed.json`
- resume: yes
- next_owner: planner

## 2026-04-16T09:04:44Z role-prompt-engineer

Continuity
- previous_target_role: planner
- previous_prompt_issue: le dernier patch planner a corrigé le mauvais claim sur tâche non-READY, mais pas le bruit de preuve sur `claim`
- changed_since_last_run: lecture VM = `planner-guardian-latest.json` rouge (`score=55`) sur `BATCH-93-DEV-03` avec `missing_architecture_plan_ref`, `missing_vision_alignment`, `missing_architecture_audit`; `planner.last_contract` montre `task_update=claim` avec `planner_evidence_incomplete_soft`

Target
- role: planner
- prompt_source: `/Users/venom/Documents/analyse-financiere/platform/automation/cron_tmux_role_runner.sh`
- why_this_prompt_now: le prochain gain réel vient d’un `claim` planner plus mergeable et moins dépendant de l’autofill qualité

Prompt audit
- useful_rules: ordre collect-first, suivi downstream, subagent unique, preuve de clôture détaillée sur `complete`, conscience produit courte
- redundant_rules: traceabilité architecture/vision répétée sur `handoff` et `complete` mais absente sur `claim`
- contradictory_rules: guardian requiert déjà cette traceabilité sur `claim|handoff|complete`, le prompt non
- too_long_or_noisy_sections: la section `Preuve planner` répétait des champs sans poser une règle commune courte
- missing_tool_guidance: pas de rappel de `batch_dependency_policy=single_batch`, ni d’`architecture_plan_ref|architecture_check`, `architecture_audit`, `vision_alignment` sur `claim`
- likely_output_failures_caused_by_prompt: `planner_evidence_incomplete_soft`, `planner_quality_autofill_missing`, `missing_architecture_plan_ref`, `missing_vision_alignment`, `missing_architecture_audit`
- architecture_doctrine_overcopy: non; aucun besoin d’ajouter la doctrine Judge-parity ici

Optimization
- keep: clôture légère sur `claim`, clôture complète sur `complete`
- simplify: une règle commune `claim|handoff|complete` pour la traceabilité; bullets spécifiques plus courts ensuite
- remove: l’ambiguïté qui autorisait un `claim` sans traceabilité
- move_out_of_prompt: aucune annexe d’architecture
- tool_usage_improvements: le guardian peut scorer directement le `claim` planner sans attendre un autofill mou
- expected_runtime_impact: direct sur la stabilité du contrat planner

Patch proposal
- patch_type: cleanup / tool-guidance / anti-churn
- create_now: yes
- target_file: `/Users/venom/Documents/analyse-financiere/platform/automation/cron_tmux_role_runner.sh`
- exact_goal: imposer sur `claim|handoff|complete` `batch_dependency_policy=single_batch` + `architecture_plan_ref|architecture_check` + `architecture_audit(paths impactés)` + `vision_alignment(batch/target/impact)`; garder `root_cause/fix_applied/verify` sur `complete` seulement
- expected_gain: moins de churn qualité et de guardian rouge pendant les dispatch utiles
- risk: low

Measurement
- signals_to_watch: `planner-guardian-latest.json.issues`, `planner.last_contract`, `planner_evidence_incomplete_soft`, `planner_quality_autofill_missing`
- success_criteria: prochain `claim` planner sans les trois manques de traceabilité et avec un guardian moins rouge
- rollback_condition: si le `claim` devient trop lourd ou ralentit le dispatch

Decision
- next_owner: prompt-expert
- next_action: relire le prochain tick VM planner sur `BATCH-93-DEV-03` et confirmer que le gain vient du prompt, pas d’un autofill contractuel

## 2026-04-16T10:03:13Z role-prompt-engineer

Continuity
- previous_target_role: planner
- previous_prompt_issue: après le durcissement de la preuve planner, le seul résidu prompt visible était `architecture_ref_not_canonical`
- changed_since_last_run: vérification VM = `planner-guardian-latest.json` green (`score=92`), `planner-prompt-patches.json` vide, `BATCH-94-ARCH` actif; seul le champ `architecture_plan_ref` restait mal contraint

Target
- role: planner
- prompt_source: `/Users/venom/Documents/analyse-financiere/platform/automation/cron_tmux_role_runner.sh`
- why_this_prompt_now: corriger le dernier défaut de preuve planner sans rouvrir une refonte globale

Prompt audit
- useful_rules: collect-first, preuve planner courte, conscience produit backend-first
- redundant_rules: `architecture_plan_ref|architecture_check` fusionnait deux intentions
- contradictory_rules: le guardian veut une ref canonique, le prompt laissait une doc de doctrine servir de preuve
- too_long_or_noisy_sections: une seule ligne portait trop d’ambiguïté sémantique
- missing_tool_guidance: séparation explicite entre preuve d’architecture et docs de doctrine
- likely_output_failures_caused_by_prompt: `architecture_ref_not_canonical`, backfills planner après tick utile
- architecture_doctrine_overcopy: oui, par fuite de doctrine dans le champ de preuve

Optimization
- keep: preuve planner structurée, champs `architecture_audit` et `vision_alignment`
- simplify: `architecture_plan_ref` canonique, `architecture_check` compact, docs de doctrine context-only
- remove: l’ambiguïté “JUDGE_PARITY peut servir de plan ref”
- move_out_of_prompt: none
- tool_usage_improvements: le guardian peut scorer le champ sans correction implicite
- expected_runtime_impact: direct mais ciblé

Patch proposal
- patch_type: tool-guidance / anti-churn / cleanup
- create_now: yes
- target_file: `/Users/venom/Documents/analyse-financiere/platform/automation/cron_tmux_role_runner.sh`
- exact_goal: expliciter que `architecture_plan_ref` doit pointer vers `docs/architecture/ARCHITECTURE_MAP.md` ou les racines impactées, et que les docs `PRODUCT_VISION` / `JUDGE_PARITY` / `API_ENDPOINT_BEST_PRACTICES` ne servent que de contexte
- expected_gain: faire disparaître `architecture_ref_not_canonical` au prochain tick planner utile
- risk: low

Measurement
- signals_to_watch: `planner-guardian-latest.json.issues`, `planner.last_contract` (`architecture_plan_ref`), `planner-prompt-patches.json`
- success_criteria: prochain `claim|handoff|complete` planner avec ref canonique et sans issue `architecture_ref_not_canonical`
- rollback_condition: si le planner cesse de publier une ref d’architecture exploitable

Decision
- next_owner: prompt-expert
- next_action: attendre le prochain tick planner VM sur `BATCH-94-ARCH`; si l’issue persiste, le problème sera requalifié en parsing/runtime et non plus en prompt

## Admin unblock summary
- blocker: planner lane falsely flagged `invalid_missing_cycle_binding` on `BATCH-94-ARCH`
- fix: `platform/automation/lane_validity.py` now recovers inline `stream_id/task_id/batch_id` from planner contract evidence
- resume: yes
- next_owner: planner

## 2026-04-16T11:03:07Z role-prompt-engineer

Continuity
- previous_target_role: planner
- previous_prompt_issue: la dernière dérive visible dans le prompt planner principal était `architecture_ref_not_canonical`
- changed_since_last_run: `planner-guardian-latest.json` est vert/idle (`score=100`) et `planner-prompt-patches.json` est vide, mais le guardian publiait encore `task_update=none_no_signal` pour un idle canonique propre

Target
- role: planner
- prompt_source: `/Users/venom/Documents/analyse-financiere/platform/automation/planner_guardian.py`
- why_this_prompt_now: réduire un faux signal encore publié par la couche guardian sans retoucher la décision planner ni regrossir le prompt

Prompt audit
- useful_rules: override canonical idle, scoring proof-aware, patches dynamiques ciblés
- redundant_rules: none significatif
- contradictory_rules: `no_canonical_work` réécrivait le résumé en `task_update=none_no_signal` alors que le prompt planner réserve ce code à un runtime indisponible prouvé
- too_long_or_noisy_sections: pas un problème de longueur; défaut purement sémantique
- missing_tool_guidance: none
- likely_output_failures_caused_by_prompt: faux comptage `none_no_signal`, reporting plus bruité côté monitor/admin, lecture trop “incident” d’un idle propre
- architecture_doctrine_overcopy: no

Optimization
- keep: `status=IDLE`, `delta=NO_ACTIVE_CANONICAL_WORK`, `planner_artifact=canonical_runtime_truth_idle`
- simplify: idle propre => `none_no_ready`
- remove: l’association idle propre -> `none_no_signal`
- move_out_of_prompt: none
- tool_usage_improvements: none
- expected_runtime_impact: direct sur la sémantique des publications guardian; structurel sur les métriques `none_no_signal`

Patch proposal
- patch_type: cleanup / anti-churn
- create_now: yes
- target_file: `/Users/venom/Documents/analyse-financiere/platform/automation/planner_guardian.py`
- exact_goal: publier `task_update=none_no_ready` quand le runtime canonique est idle et qu’aucune tâche canonique n’est exécutable
- expected_gain: moins de faux `none_no_signal` sans perdre les garde-fous planner
- risk: low

Measurement
- signals_to_watch: `planner-guardian-latest.json` (`summary.task_update`), rapports qui agrègent `none_no_signal`, absence de régression sur `planner_autobatch_missing_when_idle`
- success_criteria: au prochain idle canonique, le guardian publie `none_no_ready` au lieu de `none_no_signal`
- rollback_condition: si les états idle propres cessent d’être reconnus ou si les heuristiques autobatch/ready-idle se dégradent

Decision
- next_owner: prompt-expert
- next_action: vérifier la prochaine publication guardian idle et confirmer que le faux `none_no_signal` a disparu

## 2026-04-16T12:06:33Z role-prompt-engineer

Continuity
- previous_target_role: planner
- previous_prompt_issue: le guardian idle summary était corrigé; la prochaine couche utile était le patch dynamique live qui suivait la tâche canonique downstream
- changed_since_last_run: `planner-prompt-patches.json` n’est plus vide; il active `follow_canonical_active_task` sur `BATCH-95-DEV-02`, donc cette instruction est désormais la principale couche réellement injectée dans le prompt planner

Target
- role: planner
- prompt_source: `/Users/venom/Documents/analyse-financiere/platform/automation/planner_guardian.py`
- why_this_prompt_now: c’est la seule consigne dynamique active du planner; elle doublonnait le protocole commun et utilisait un wording endpoint (`route bloquante`) trop spécifique

Prompt audit
- useful_rules:
  - priorité à la tâche canonique active
  - collect via `planner_subagent_manager.py collect`
  - chemin explicite `planner_runtime_actions.py handoff-ack|handoff-close`
  - interdiction de nouveau batch / ANALYSIS / redispatch avant transition
- redundant_rules:
  - `Priorité absolue`
  - `Collect d'abord`
  - répétition du bannissement batch/analysis/redispatch déjà présent dans le prompt partagé
- contradictory_rules:
  - `corrige la route bloquante` oriente vers un cas endpoint alors que la lane active peut être n’importe quel travail downstream
- too_long_or_noisy_sections:
  - l’instruction `follow_canonical_active_task` elle-même, seule ligne active du patch loader
- missing_tool_guidance:
  - formulation lane-generic pour débloquer le travail actif sans retomber sur un biais endpoint
- likely_output_failures_caused_by_prompt:
  - redispatch inutile
  - retour ANALYSIS au lieu de collect
  - sur-spécification planner sur un blocage qui n’est pas forcément une route
- architecture_doctrine_overcopy:
  - non, mais un vocabulaire endpoint a fuité dans un patch planner générique

Optimization
- keep:
  - priorité tâche canonique
  - collect d’abord
  - wrappers `handoff-ack|handoff-close`
  - interdiction de nouveau batch / ANALYSIS / redispatch
- simplify:
  - instruction unique plus courte et plus directe
- remove:
  - `absolue`
  - `d'abord`
  - `route bloquante`
- move_out_of_prompt:
  - none
- tool_usage_improvements:
  - `debloque la lane active` remplace le wording endpoint et laisse les outils canoniques explicites
- expected_runtime_impact:
  - direct sur le patch planner actif; moins de bruit, meilleure clarté d’action sur une lane downstream déjà canonique

Patch proposal
- patch_type: shorten / anti-churn / tool-guidance
- create_now: yes
- target_file: `/Users/venom/Documents/analyse-financiere/platform/automation/planner_guardian.py`
- exact_goal: raccourcir `follow_canonical_active_task` et le rendre lane-generic sans perdre `collect` ni `handoff-ack|handoff-close`
- expected_gain: moins de bruit prompt live et moins de confusion endpoint-vs-lane quand une tâche downstream est déjà `IN_PROGRESS`
- risk: low

Measurement
- signals_to_watch:
  - `logs-codex-runs/orchestrator-state/planner-prompt-patches.json`
  - `logs-codex-runs/orchestrator-state/planner-guardian-latest.json`
  - `handoff_same_task_streak`
  - `planner_evidence_incomplete_soft`
  - churn `retry/takeover`
- success_criteria:
  - prochain patch actif toujours collectable, sans wording `route bloquante`, avec suivi downstream sans nouveau batch/ANALYSIS
- rollback_condition:
  - si le planner perd la clarté `collect` / `handoff-ack|handoff-close` ou recommence à ignorer la tâche canonique active

Decision
- next_owner: prompt-expert
- next_action: attendre le prochain cycle planner non-idle avec tâche downstream canonique active et vérifier que le patch live reste court, lane-generic et sans redispatch inutile

Validation
- command_or_check: `python3 platform/automation/tests/test_planner_guardian.py`
- observed_result: PASS (`20 tests`)
- targeted_check: `PYTHONPATH=/Users/venom/Documents/analyse-financiere/platform/automation python3 - <<'PY' ... build_prompt_patches(...) ... PY`
- observed_patch: instruction active = `Priorité: faire avancer ... debloque la lane active. Aucun nouveau batch, ANALYSIS ou redispatch avant transition.`

## 2026-04-16T12:11:47Z prompt-expert

- user signal confirmé: ce n’était pas normal que le planner ne crée pas de nouveaux batches selon la priorité de la vision
- root cause trouvée hors prompt: `planner_autobatch()` bloquait sur `runway_not_empty` dès qu’un batch canonique actif existait, et seedait les batches depuis `PRODUCT_VISION#One sentence` avec priorité forcée `P2`
- patch appliqué dans `platform/automation/compat/projections/parallel_workstream.py`: seed autobatch d’abord depuis `PRODUCT_VISION` `P0/P1`, priorité propagée au queue item + stream + task, et mode `allow_active_queued` limité à l’ignorance du batch actif courant
- patch appliqué dans `platform/automation/cron_tmux_role_runner.sh`: le preflight planner appelle maintenant `planner-autobatch ... --allow-active-queued`
- validations: `python3 -m py_compile ...` OK; `bash -n platform/automation/cron_tmux_role_runner.sh` OK; `python3 -m unittest discover -s platform/automation/tests -p "test_parallel_workstream_queue_sync.py"` OK (`21 tests`); test ciblé `PlannerRuntimeActionsAutobatchGuardTests.test_planner_autobatch_creates_queued_batch_while_active_cycle_exists_when_allowed` OK
- bruit résiduel: le test ciblé `RoleRuntimeContextTests.test_runner_contains_planner_preflight_sync_contract` échoue sur une assertion préexistante non liée (`complete --role planner --task <task_id>` absent du runner), pas sur le nouveau flag autobatch
- signaux à suivre au prochain tick réel: création d’un seul batch planner `queued_only=1` derrière le batch actif, titre/priorité issus de `P0/P1`, absence de retour au seed générique `One sentence` / `P2`

## Orchestration architect summary
- verdict: régression publique réelle; control-plane continue à sur-vendre `BATCH-95`
- top priority: restaurer immédiatement la disponibilité publique EC2 avant toute lecture de progrès batch
- blocker: `BATCH-95-DEV-03` reste “actif” en projection alors que `http://3.98.20.77/` et `/api/health` sont à `502`
- next useful delivery: rétablir EC2 puis revalider publiquement `judge -> copilot -> personal-finance`

## 2026-04-16T23:08:00Z po-vision-batch-architect

- Audit vision produit / architecture / runtime refait après revalidation publique EC2.
- Vérité produit EC2 confirmée: `GET /api/health`, `GET /api/copilot/start?tickers=NVDA&debug=true`, `GET /api/personal-finance/start?tickers=NVDA&debug=true`, et `GET /api/judge/personal-finance/start?tickers=NVDA&debug=true` renvoient tous un flux visible `brief + ask/open` avec `ranked_action=open_nvda`.
- Vérité control-plane actuelle: `priority-queue.json` et le monitor public exposent `active_batch_ids=[]` / `active_batch=null`; `planner-graph-state.json` conserve des preuves mergées de `BATCH-95`, mais pas de nouveau batch canonique actif.
- Conclusion explicite: `BATCH-95` a déjà épuisé son delta utilisateur principal. Le résiduel restant est principalement monitor/guardian/projection et ne justifie ni de garder le thème actif côté produit, ni d’ouvrir immédiatement un nouveau batch tant que le planner n’est pas en convergence propre.
- Décision: `create_now=no`.
- Prochain batch candidat prêt à créer plus tard si le planner redevient moteur de livraison réel:
  - `Ask/Open -> investment memo shared contract`
  - but: rendre `ask/open immédiat` pleinement judge-parity via contrat partagé + façade endpoint service réutilisée, avec preuve publique EC2 par tests/smokes ciblés sur `/api/copilot/ask` et `/api/personal-finance/ask`.

## Admin unblock summary
- blocker: certaines surfaces compatibles lisaient `workstreams` alors que `parallel-workstreams.json` ne publiait plus que `streams`, ce qui fabriquait un faux `active_batch=null`
- fix: `platform/automation/compat/projections/parallel_workstream.py` republie désormais l’alias `workstreams` et la projection VM a été resynchronisée via `sync-priority`
- resume: yes
- next_owner: admin

## Orchestration architect summary
- verdict: aucun progrès produit nouveau; batch actif = restauration `502`, pas nouveau delta utilisateur
- top priority: restaurer immédiatement les endpoints publics EC2 avant tout nouveau claim de delivery
- blocker: `BATCH-95-DEV-03` est `IN_PROGRESS` en projection mais absent de la vérité SQLite alors que le public reste en `502`
- next useful delivery: remettre `judge -> copilot -> personal-finance` publiquement utilisable puis re-smoker `ranked_action`

## 2026-04-16T22:27:24Z role-prompt-engineer

Continuity
- previous_target_role: planner
- previous_prompt_issue: `planner_guardian` traitait une lane downstream `READY_*` comme si elle était déjà active
- changed_since_last_run: VM `planner-guardian-latest.json` (source `rate_limit_gate_checkpoint`) = score 50/red avec `ready_but_none_task_update`; tâche canonique `BATCH-95-DEV-03` en `READY_DEV`; `planner-prompt-patches.json` publiait encore `follow_canonical_active_task` avec consigne `collect`/`handoff-ack`

Target
- role: planner
- prompt_source: `/Users/venom/Documents/analyse-financiere/platform/automation/planner_guardian.py`
- why_this_prompt_now: défaut de guidance réellement actif et directement corrélé au churn planner sur la lane canonique prête

Prompt audit
- useful_rules: patch guardian court et tool-explicite pour empêcher nouveau batch/ANALYSIS quand une lane downstream travaille déjà
- redundant_rules: même guidance `collect/ack` pour `READY_*` et `IN_PROGRESS|BLOCKED`
- contradictory_rules: `READY_DEV` recevait une consigne de collect impossible au lieu d’un dispatch/run
- too_long_or_noisy_sections: pas de bruit de longueur; erreur de branchement
- missing_tool_guidance: manque d’un `planner_subagent_manager.py run` explicite pour lane prête
- likely_output_failures_caused_by_prompt: `ready_but_none_task_update`, `none_no_signal`, waits improductifs, redispatch inutile
- architecture_doctrine_overcopy: no

Optimization
- keep: guidance `collect`/`handoff-ack` pour downstream réellement actif
- simplify: branche courte dédiée à `READY_*`
- remove: wording `collect` sur lane prête
- move_out_of_prompt: none
- tool_usage_improvements: `READY_*` downstream -> `planner_subagent_manager.py run`; états actifs -> `collect`/`handoff-ack|handoff-close`
- expected_runtime_impact: direct au prochain guardian tick

Patch proposal
- patch_type: tool-guidance / anti-churn
- create_now: yes
- target_file: `/Users/venom/Documents/analyse-financiere/platform/automation/planner_guardian.py`
- exact_goal: ne plus publier de consigne `collect` pour un downstream `READY_*`; publier une consigne de `run` ciblé et garder `claim_or_autobatch_now` supprimé dans ce cas
- expected_gain: moins de waits planner, meilleure clarté de la prochaine action, moins de churn `READY_* -> none_no_signal`
- risk: low

Measurement
- signals_to_watch: `ready_but_none_task_update`, `none_no_signal` quand `READY_DEV` existe, contenu de `planner-prompt-patches.json`
- success_criteria: prochain patch guardian READY mentionne `planner_subagent_manager.py run`; le planner cesse de collecter une lane seulement prête
- rollback_condition: si la nouvelle branche READY provoque du dispatch spam ou retire la bonne guidance pour `IN_PROGRESS|BLOCKED`

Decision
- next_owner: prompt-expert
- next_action: surveiller le prochain tick VM et ne repatcher que si le churn persiste malgré la nouvelle branche READY

## Orchestration architect summary
- verdict: aucun progrès produit sur ce run; EC2 public régresse en `502` et le planner continue de projeter `BATCH-95-DEV-03` sans vérité SQLite
- top priority: restaurer le spine public `judge -> copilot -> personal-finance` puis refaire les smokes EC2
- blocker: faux batch actif `BATCH-95-DEV-03` dans guardian/queue/workboard alors que `fc_doctor` ne prouve que `BATCH-95-DEV-01/02`
- next useful delivery: revalider publiquement un `ranked_action` ticker-scoped non nul avant toute nouvelle itération planner

## Admin unblock summary
- blocker: tick planner casse par `.codex/config.toml` invalide (`[agents]` scalaire incompatible OpenClaw/Codex)
- fix: suppression du bloc `[agents]` invalide dans `/Users/venom/Documents/analyse-financiere/.codex/config.toml` + validation `codex exec` depuis la VM
- resume: yes
- next_owner: planner

## 2026-04-16T23:04:37Z role-prompt-engineer

Continuity
- previous_target_role: planner
- previous_prompt_issue: la branche guardian `READY_*` downstream était corrigée, mais le guardian restait rouge sur un runtime canonique idle
- changed_since_last_run: `planner-guardian-latest.json` local publiait encore `ready_but_no_delta` + `ready_but_none_task_update` alors que le même payload résumait `NO_ACTIVE_CANONICAL_WORK`

Target
- role: planner
- prompt_source: `/Users/venom/Documents/analyse-financiere/platform/automation/planner_guardian.py`
- why_this_prompt_now: c’était le bruit prompt actif le plus clair et le plus directement contradictoire

Prompt audit
- useful_rules: guardian court, dynamique, bien branché sur l’état canonique quand une vraie lane existe
- redundant_rules: READY projeté pénalisé même quand la vérité canonique dit idle
- contradictory_rules: `summary.delta=NO_ACTIVE_CANONICAL_WORK` + reco `Claim une tache READY`
- too_long_or_noisy_sections: bruit faible en volume, fort en impact
- missing_tool_guidance: pas de neutralisation claire du guardian quand le runtime canonique est idle
- likely_output_failures_caused_by_prompt: faux rouges, faux streaks, faux claim/autobatch
- architecture_doctrine_overcopy: no

Optimization
- keep: guidance downstream active/ready, hard guard novelty, proof patching
- simplify: idle canonique = guardian neutre
- remove: `ready_but_*`, reco claim READY, et `ready_idle_streak` quand l’idle est canonique
- move_out_of_prompt: none
- tool_usage_improvements: le planner n’est plus poussé vers `claim/sync-priority/autobatch` sur simple bruit de projection
- expected_runtime_impact: direct et structurel

Patch proposal
- patch_type: anti-churn / cleanup / tool-guidance
- create_now: yes
- target_file: `/Users/venom/Documents/analyse-financiere/platform/automation/planner_guardian.py`
- exact_goal: aligner `score/issues/recommendations/streaks` avec `NO_ACTIVE_CANONICAL_WORK`
- expected_gain: moins de churn prompt, moins de faux retries, moins de redispatch inutile
- risk: low

Measurement
- signals_to_watch: `planner-guardian-latest.json.issues`, `recommendations`, `streaks.ready_idle_streak`, `planner-prompt-patches.json.active`
- success_criteria: prochain idle guardian sans `ready_but_*`, sans reco claim READY, et avec `ready_idle_streak=0`
- rollback_condition: un vrai READY canonique devient silencieux

Decision
- next_owner: prompt-expert
- next_action: surveiller le prochain tick guardian idle et rollback seulement si la détection de vrai READY régresse
- ## 2026-04-16T23:28:06Z delivery-first-governor

- Implémentation du premier correctif structurel anti-boucle vide.
- `platform/automation/runtime/truth/runtime_truth_reader.py` publie maintenant un `product_delivery_state` canonique avec:
  - `active_batch_id`
  - `phase`
  - `product_done`
  - `ops_clean`
  - `public_proof_status`
  - `user_visible_delta_confirmed`
  - `next_batch_eligible`
  - `ec2_reachable`
  - `freeze_reason`
  - timestamps de preuve/delta + `advisory_mismatch`
- `apps/monitor/services/status_service.py` expose ce canonique sous `delivery_control.product_delivery_state` et recopie les champs de synthèse (`phase`, `product_done`, `ops_clean`, `next_batch_eligible`, `ec2_reachable`, `freeze_reason`).
- `platform/automation/planner_autonomy_tick.sh` lit désormais cette autorité avant d’agir:
  - `external_outage` bloque la lane planner immédiatement
  - `idle_ready_for_next_batch` / `product_done` empêchent les résidus projection-only de forcer `repair_only`
  - le batch actif canonique prime sur `active_cycle` projeté
  - fallbacks cross-host ajoutés pour `flock` / `timeout`
- `scripts/aws_ec2_idle_stop.sh` est passé en mode safe par défaut: arrêt EC2 désactivé tant que l’opérateur n’active pas explicitement `AWS_EC2_IDLE_STOP_MODE=http_idle_opt_in`.
- Vérifications:
  - `PYTHONPATH=platform/automation python3 platform/automation/tests/test_runtime_truth_reader.py` PASS
  - `python3 platform/automation/tests/test_planner_autonomy_tick.py` PASS
  - `python3 apps/monitor/tests/test_status_never_null.py` OK (skipped=4 car `fastapi` absent dans ce runtime)
  - `python3 -m py_compile ...` PASS
  - `bash -n platform/automation/planner_autonomy_tick.sh scripts/aws_ec2_idle_stop.sh` PASS

## Orchestration architect summary
- verdict: la porte d’entrée publique EC2 est bien utilisable et portfolio-aware par défaut (`open_aapl`), mais `BATCH-96` ne crée toujours aucun delta batch neuf; il a seulement avancé de `ANALYSIS` vers `ARCH`
- top priority: soit ouvrir immédiatement `BATCH-96-DEV-01` sur un vrai delta starter portfolio-first visible, soit fermer/backoff `BATCH-96` comme résidu planner-only
- blocker: le planner peut rouvrir un scope déjà livré comme batch `net_new` actif, tandis que `fc_doctor` surestime ce même batch en `product_done=true`
- next useful delivery: rendre le starter public par défaut réellement portfolio-first dans le brief/risk ordering, pas seulement dans `ranked_action=open_aapl`

## Orchestration architect summary
- verdict: progrès produit réel depuis le run de 2026-04-16 23:50 UTC; `BATCH-96-DEV-01` est bien livré publiquement sur EC2, mais `BATCH-96` reste ouvert inutilement sur un `DEV-02` non prouvé
- top priority: soit livrer `BATCH-96-DEV-02` avec une vraie preuve EC2 visible, soit le backoff/fermer pour clore `BATCH-96`
- blocker: monitor public `500` + surfaces advisory stale, pendant que `dev.last_contract` ne montre qu’un `CONTRACT_SCOPE_AUTOFILL`
- next useful delivery: un seul delta public de plus pour `DEV-02`, sinon fermeture monotone du batch et ouverture propre du prochain lot

## Orchestration architect summary
- verdict: aucun nouveau delta public depuis le run précédent; EC2 reste bon, `BATCH-96` est `product_done=yes`, mais la boucle delivery est cassée parce que le runtime ne passe pas proprement au batch suivant.
- top priority: supprimer le résidu canonique `BATCH-96` après merge+preuve publique, puis ouvrir immédiatement le prochain batch utile depuis la runtime truth.
- blocker: `planner_board_runtime.py snapshot` garde `active_cycle.active_batch_ids=["BATCH-96"]` alors que sqlite n’a plus que `BATCH-96-DEV-01/02/03` en `merged`, `runtime_actionable=false`, et `next_action=none`.
- next useful delivery: un petit lot EC2 visible qui rende le starter réellement portfolio/watchlist-first, pas un nouveau suivi de preuve ou `WAITING_DEP` sur `BATCH-96`.

## 2026-04-16T23:43:50Z orchestration-architect

- EC2 public remains healthy and user-usable; the three starter endpoints still return a real brief with `ranked_action=open_nvda`.
- Runtime truth still has a real active batch: `BATCH-96`, now advanced to `BATCH-96-DEV-01` runnable/in progress after closure of `BATCH-96-ANALYSIS` and `BATCH-96-ARCH`.
- No new public user delta is visible yet for `BATCH-96`; the progress since the last run is structural only, not product-done.
- Main mismatch now: runtime truth says `BATCH-96` is active, while the public monitor still exposes `active_batch=null`; treat that as advisory residue, not canonical idleness.
- Immediate operator priority: finish `BATCH-96-DEV-01` or back off wait-only lanes; do not open a new batch while EC2 is healthy and this batch is still actionable.
