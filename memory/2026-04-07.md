# Daily Memory 2026-04-07

- Admin/infra continuation 2026-04-07: live canonical flow still shows `BATCH-84` active, but the real blocker is a stale board contradiction on the downstream admin handoff. Queue/batch intent still points to `BATCH-84-ADMIN-01`, while the task row exposes `state=IN_PROGRESS`, `status=READY_PLANNER`, `owner=admin`, `next_action=wait_or_collect_result`, and `planner_takeover_required=true`. This suppresses self-advancing dispatch even though the canonical handoff is already ready to be retried.
- Fix applied 2026-04-07: `planner_runtime_actions.py` dispatch selectors and `planner_autonomy_tick.sh` now reason from operational state (`status` when it carries canonical runtime meaning) instead of raw `state` only. New invariant: a downstream task with stale `state=IN_PROGRESS` but canonical `status=READY_*` remains dispatchable, and planner autonomy must not treat that as a live in-progress blocker.
- Follow-up verification 2026-04-07: the canonical `dispatch-capability --target-role admin` surface now succeeds on `BATCH-84-ADMIN-01` instead of failing with `CLAIM_ERROR not READY`. Queue projection also corrected its batch-level `next_action` from `compléter ... READY_PLANNER` to `claim BATCH-84-ADMIN-01 (READY_PLANNER pour admin)`. The remaining blocker class is now explicit: dispatch falls into `reason=planner_takeover_runtime_not_healthy` / `backend=planner_takeover`, so the orchestration bug is fixed and the next bottleneck is runtime health under planner takeover, not stale claim semantics.

- Admin/infra orchestration follow-up: `admin_dispatcher_tick.sh` now treats planner-owned admin handoffs as claimable when the canonical task is `READY`, `READY_PLANNER`, or `READY_ADMIN`, instead of requiring only plain `READY`. This is a systemic fix for the stalled-flow class where planner keeps dispatching but `admin` never turns `READY_PLANNER` into real execution. The rule is also documented in `docs/ops/ORCHESTRATION_RELIABILITY_SPEC.md` so future agents preserve planner->admin self-advancing behavior.
- Backend auto-heal fix: `scripts/restart_api_if_stale.sh` was incorrectly returning `[OK]` when the backend process existed but `/api/health` timed out without socket pressure. The guard now treats any non-responsive health probe as restart-worthy, then requires the API to come back and pass smoke before declaring success. This closes the gap where `monitor_stack_guard.sh` kept calling the heal path every minute but product runtime stayed degraded anyway.
- Dispatcher robustness fix: `platform/automation/admin_dispatcher_tick.sh` no longer relies only on `.role` in workboard tasks. It now resolves canonical target role from `role`, `assigned_to`, or `assignee` for ready/in-progress detection. This removes another recurring source of “planner dispatch exists but downstream claim stayed invisible” when projection fields drifted by key name.
- Admin/infra: remplacé les self-calls backend->backend dans  par des lectures de snapshots locaux (, ) pour réduire la saturation des 3 workers API et éviter que  se retrouve bloqué derrière des appels internes à .
- Admin/infra: remplacé les self-calls backend-vers-backend dans domains/copilot/application/copilot_service.py::_fetch_live_market_context() par des lectures de snapshots locaux (forecasts, news_feed) pour réduire la saturation des 3 workers API et éviter que /api/health se retrouve bloqué derrière des appels internes à localhost:8050.
- Admin/infra: après suppression des self-calls localhost:8050 dans le service copilot et redémarrage backend, /api/health répond de nouveau rapidement; vérifier maintenant si le monitor remonte backend_api.status et product_runtime.status sans dégradation stale.
- Admin/infra: role_contract_guard probe planner aligné sur /api/status?lite=1 au lieu de /api/status, pour éviter les faux runtime_unavailable quand le monitor complet est plus lent mais que le runtime est sain.
- Admin/infra: planner restait en runtime_unavailable car /api/health retombait en timeout réel. Déclenchement du heal backend borné via scripts/restart_api_if_stale.sh avant nouvelle validation du contrat planner.
- Admin/infra: planner no longer enters runtime_unavailable from backend_api health alone; planner passive exception now depends on monitor/runtime truth reachability, while product backend remains a separate delivery gate.
- Admin/infra: tentative de matérialisation live du fix planner par tick borné + revalidation via monitor API seulement (pas de lecture de logs legacy comme vérité).
- Admin/infra: admin_dispatcher_tick a matérialisé le handoff canonique BATCH-84-ADMIN-01 (CLAIM_OK). Le flux planner->admin n’est plus bloqué sur READY_PLANNER dans ce cycle.
