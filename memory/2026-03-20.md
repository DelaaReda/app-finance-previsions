# Daily Memory 2026-03-20

- Vérification avant reprise: pas de nouveau changement code critique depuis les fixes planner/monitor du 13-14 mars; la dérive réelle est runtime.
- État live constaté: runtime reparti en `planner-only`, batches 68/69 en cours de merge + `BATCH-70` ready/running, `event_store_primary=true`, `openclaw_gateway` advisory dégradé.
- Régression opérationnelle trouvée: `fc_doctor.sh --json` passe mais prend ~37s sous charge live, donc `/api/doctor?refresh=1` du monitor retombe sur le timeout HTTP de 20s.
- Ajustement appliqué: `apps/monitor/server.py` budget doctor monitor par défaut relevé à 45s pour refléter le coût réel du doctor en runtime actif.

- Recheck 2026-03-20: BATCH-64 and BATCH-65 are fully DONE; runtime health OK. Repaired BATCH-70 stale planner state by canonically completing BATCH-70-ANALYSIS; batch is now READY_DEV with DEV-01 as next delivery step.
- Orchestration hardening 2026-03-20: planner capability routing invariant added. Active or finished subagents are now ignored/quarantined when `owner_task_id` does not match the target role domain (`dev`->`dev` task only, `admin`->`admin` task only). Applied in `platform/automation/runtime/planner/planner_runtime_actions.py`, with reconciler filtering in `platform/automation/state_reconciler.py` and normative doc update in `docs/ops/ORCHESTRATION_RELIABILITY_SPEC.md`.
- Re-read of live orchestration artifacts after the hardening patch: queue/workboard active cycle currently points to `BATCH-24`, while planner audit and legacy subagent logs still contain repeated `BATCH-70-ANALYSIS` claims/dispatch attempts. Conclusion: `BATCH-70` is now stale runtime debt unless reactivated by canonical planning state; agents should prefer queue/workboard over stale role memory or legacy subagent history.
- Session/recovery hardening 2026-03-20: added generic Codex session freshness guard in `platform/automation/cron_tmux_role_runner.sh` using `*.codex_exec_session_meta` fingerprint + max-age, and VM resume recycling in `scripts/vm_resume_guard.sh` + `platform/automation/vm_resume_guard.sh` to kill stale `codex_*_cron` / `qwen_*_cron` sessions and clear persisted resume artifacts after large suspend gaps. Also standardized planner/dev secondary Codex fallback to `gpt-5.3-codex-spark` with `high` reasoning before `qwen`.
- Recheck 2026-03-20: wrote role handoff memory for planner/dev. Active stream is BATCH-70; canonical repair closed stale ANALYSIS; next action is dispatch/resume BATCH-70-DEV-01.
- Planner-only runtime recheck: planner_subagent_manager status showed 4 stale running subagents with no live monitor target (), including BATCH-70-DEV-01 and older BATCH-66/68/69 entries; canonical cleanup launched before any new dispatch to avoid starving auto-delivery.
- Canonical dispatch snapshot guard added: `platform/automation/runtime/truth/dispatch_snapshot.py` now quarantines SQLite-primary graph states stuck `running/pending` in `wait_or_collect_result` for >45m with `last_meaningful_delta=none`, so stale planner subagents stop occupying the active lane.
- Planner recovery pass executed via `planner_runtime_actions.py` after the guard: stale planner-owned `BATCH-70-ANALYSIS` residue was collected/quarantined as route mismatch, then the stream normalized to `BATCH-70 state=IN_PROGRESS`, `BATCH-70-DEV-01 state=IN_PROGRESS`, `planner_dispatch.active_count=1`, `quarantined_stale_active_count=3`.
- Architecture recheck 2026-03-20 late afternoon: canonical queue/workboard now advertise active cycle `BATCH-24`, while planner tick logs and planner subagent status still carry stale `BATCH-70-*` residue (`BATCH-70-DEV-01` active, old `BATCH-70-ANALYSIS` retries in recent history). Session freshness is fixed (`resume=0` after recycle), so the remaining architecture bug is no longer stale resume; it is dispatch-coordination drift plus overlapping planner ticks (`RUN_LOCK_BUSY`) against a non-canonical stream.
- Working rule after this recheck: queue/workboard active cycle wins over planner role memory, planner tick history, and legacy subagent rows. Non-active-cycle planner subagents are compat debt only and must not drive dispatch or readiness.
- Active-cycle dispatch gate 2026-03-20 evening: patched planner selection, active-subagent detection, stale cleanup, finished-result collection, and reconciler active-owner filtering so out-of-cycle tasks no longer count as dispatch candidates or active blockers. Goal: eliminate the class of failures where an old batch is reanimated after planning moved to a new active cycle.
- Live lane calibration 2026-03-20 evening: tmux session presence is currently not proof of work. `codex_planner_cron` is blocked on an interactive Codex update prompt, and `codex_dev_cron`, `codex_admin_cron`, `admin-agents-sync-cron`, `adminapp_codex_sync`, and `clawsentinel` are running from `/home/venom/shared/analyse-financiere (deleted)` instead of `/home/venom/analyse-financiere`. Architecture rule reaffirmed: queue/workboard + runtime truth win over tmux/session appearance; lane bootstrap/session hygiene is now a first-class admin/infra blocker class.

## 2026-03-20 planner resume follow-up
- Patched platform/automation/runtime/truth/dispatch_snapshot.py to quarantine event-store active rows that conflict with workboard truth (owner_task_role_mismatch, owner_task_not_in_progress).
- Added regression coverage in platform/automation/tests/test_planner_dispatch_metrics.py for stale planner ANALYSIS vs ready dev dispatch.
- Validation: PYTHONPATH=platform/automation python3 -m pytest -q platform/automation/tests/test_planner_dispatch_metrics.py platform/automation/tests/test_planner_autonomy_tick.py platform/automation/tests/test_planner_subagent_manager.py => 51 passed.
- Live runtime truth after patch: BATCH-70-DEV-01 is IN_PROGRESS and planner dispatch metrics now report latest_owner_task_id=BATCH-70-DEV-01, active_count=1, status=ok, planner_state=waiting_on_agents.
- planner.last_contract is still stale on BATCH-70-ANALYSIS; do not use it as primary truth for this stream until next successful planner contract rewrite.
- Operational note: avoid spawning extra planner runner sessions while a lock-holding runner is already active.
- Coordination decision 2026-03-20: do not introduce `chat_admin_manual_team.md` or another ad-hoc chat file. Reuse `memory/agents/admin-agents.md` as the compact shared manual board, with queue/workboard + runtime truth still canonical and daily memory carrying architecture decisions.
- Collaboration effectiveness recheck 2026-03-20: live tmux sessions are currently a weak signal. `codex_dev_cron`, `codex_admin_cron`, `admin-agents-sync-cron`, `adminapp_codex_sync`, and `clawsentinel` are anchored to `/home/venom/shared/analyse-financiere (deleted)`, while `codex_planner_cron` is sitting in an interactive Codex update prompt. Conclusion: session existence alone is not proof of productive collaboration; canonical runtime truth + queue/workboard + tick logs remain primary until lane startup state is repaired.
- Queue/workboard drift fix 2026-03-20: `platform/automation/runtime/planner/planner_runtime_actions.py` CLI path `claim` was mutating the workboard without `recompute_states()` + `reconcile_state()`, leaving `priority-queue.json` stale in `READY_PLANNER` after a planner claim. Patched the CLI to reconcile queue/workboard after claim, and added regression coverage in `platform/automation/tests/test_planner_dispatch_metrics.py`. Validation: `PYTHONPATH=platform/automation python3 -m pytest -q platform/automation/tests/test_planner_dispatch_metrics.py platform/automation/tests/test_planner_autonomy_tick.py` => `16 passed`.
- Session hygiene correction 2026-03-20 late: live filesystem check shows `/home/venom/analyse-financiere` is a symlink to `/home/venom/shared/analyse-financiere` on this VM (`samefile=True`). The wrong invariant was string-based path comparison; the correct invariant is: lane invalid iff cwd is `(deleted)`, not `samefile`-equivalent to the canonical repo, or blocked on the interactive Codex update prompt.
- Applied samefile-aware lane validity in `platform/automation/lib/workspace_paths.sh`, `platform/automation/auto_recover_tmux_roles.sh`, and `platform/automation/vm_resume_guard.sh`, plus spec update in `docs/ops/ORCHESTRATION_RELIABILITY_SPEC.md`.
- Live recheck after recycle: `codex_planner_cron`, `admin-agents-sync-cron`, `adminapp_codex_sync`, and `clawsentinel` are now valid repo-alias sessions (shared path string but same repo inode) and no longer blocked on the update prompt; `codex_dev_cron` and `codex_admin_cron` remain absent after recycle, which is acceptable under planner-only mode.
- Remaining blocker class after this fix: tmux session existence is still not proof of productive work when the pane is only an idle shell. Next reliability slice is to require a fresh execution proof or tick artifact before counting a lane as live progress.
- Codex auto-lane bootstrap hardening 2026-03-20 evening: `platform/automation/auto_recover_tmux_roles.sh` now seeds `~/.codex/version.json` with `dismissed_version=latest_version` before launching Codex and starts lanes with `codex --cd /home/venom/analyse-financiere --no-alt-screen`. Result: `planner/dev/admin` come up as non-blocking `node` Codex sessions instead of stopping on the update-choice prompt.
- Canonical live truth after that bootstrap fix: queue/workboard direct read shows no current `READY*` or `IN_PROGRESS` items; doctor reports `overall=ok`, `primary=ok`, `planner_dispatch.status=ok`, `planner_state=idle`, `active_count=0`, `latest_owner_task_id=BATCH-69-ARCH`. Conclusion: core bootstrap is recovered, but lane usefulness remains advisory until a future canonical work item appears or a fresh execution-proof artifact is emitted.
- Runtime consumer hardening 2026-03-20 late evening: `state_reconciler.py`, `runtime/planner/planner_runtime_actions.py`, and `runtime/planner/planner_board_runtime.py` now intersect `active_cycle.active_batch_ids` with actually open queue/workboard rows. Fresh proof after patch: the raw projections still carry top-level `active_cycle.active_batch_ids=["BATCH-24"]`, but direct `BATCH-24` rows are `CLOSED/DONE`; `planner_board_runtime.snapshot()` now returns `active_cycle={}`, `planning_alignment_status=stale_cycle_state`, `planning_alignment_reason=active_batch_ids_not_open`, `runnable_task_ids=["BATCH-71-PLAN"]`, `next_action=collect_subagent_results`. This is the real blocker class: stale top-level cycle projection, not missing tmux sessions.
- Human live override 2026-03-20 latest: operational truth is `active_cycle=BATCH-24`; `codex_planner_cron` is only partially repaired, and `codex_dev_cron`, `codex_admin_cron`, `admin-agents-sync-cron`, `adminapp_codex_sync`, and `clawsentinel` are still not validated as useful lanes. Rule: human live verification of queue/workboard + runtime truth beats stale inferences from tmux/session state, samefile assumptions, or earlier lane recovery notes.
- Final bootstrap recheck 2026-03-20 17:37 America/New_York: canonical queue/workboard are idle (`ready=[]`, `active=[]`). `auto_recover_tmux_roles.sh` now reports planner/dev/admin UP after non-interactive restart, each session runs a real Codex process (`cmd=node`) with no update prompt. However, useful-work validation is still pending because there is no current canonical item to execute, and auxiliary sessions still need separate revalidation.
- Filesystem nuance locked in: `/home/venom/analyse-financiere` is a symlink to `/home/venom/shared/analyse-financiere` on this VM. Therefore raw tmux `pane_current_path` strings can show `shared` even when the lane is on the correct repo inode. The invalid cases are now: `(deleted)` cwd, non-samefile workspace, interactive Codex prompt, or no real Codex process.
- Active-cycle normalization fix 2026-03-20 late night: `platform/automation/compat/projections/parallel_workstream.py` `reconcile_state(...)` now normalizes and persists top-level `active_cycle` on both queue and workboard. Closed stale ids are removed from `active_batch_ids` and migrated into `recent_completed_batch_ids` instead of surviving forever as fake live cycles.
- Regression coverage added in `platform/automation/tests/test_parallel_workstream_queue_sync.py` for the exact failure mode: `BATCH-24` closed in rows but still advertised as top-level active cycle.
- Live validation after patch: targeted queue-sync suite passed (`14 passed`), `reconcile-state` on the live runtime moved stale `BATCH-24` to `recent_completed_batch_ids`, and canonical `active_cycle.active_batch_ids` now points to `BATCH-71`.
- Fresh canonical useful-work proof now exists: `BATCH-71` queue=`IN_PROGRESS`, stream=`IN_PROGRESS`, `next_action="compléter BATCH-71-PLAN (READY_PLANNER pour planner)"`, and `BATCH-71-PLAN state=IN_PROGRESS`. Remaining coordination blocker is narrower: dev/admin still lack their own fresh proof on this cycle, and auxiliary sessions `admin-agents-sync-cron`, `adminapp_codex_sync`, `clawsentinel` remain absent/unvalidated.
- Fresh planner blocker after canonical rebasing: `planner.last_contract` at `2026-03-20 17:49:52 America/New_York` shows `BLOCKED / planner_delivery_proof_missing` on `complete --task BATCH-71-PLAN`. This is now the live orchestration gate: `BATCH-71-PLAN` stays `IN_PROGRESS`, downstream `BATCH-71-ANALYSIS/ARCH/DEV*/ADMIN-01/GOV_REVIEW` stay `WAITING_DEP`, and dev/admin cannot yet produce fresh useful work on the current cycle.
- Planner autonomy guard landed in `platform/automation/planner_autonomy_tick.sh`: if canonical `active_cycle.active_batch_ids` is non-empty, planner autobatch/create is now blocked and must surface the pinned-cycle blocker instead of minting a side batch. This was added after observing planner create `BATCH-71` while the top-level cycle still advertised `BATCH-24`.
- Planner completion autofill fix: `platform/automation/runtime/planner/planner_runtime_actions.py` now intercepts planner `complete` calls even when `--note`/`--architecture-checks` are sparse, backfills `root_cause`, `fix_applied`, `verify`, `architecture_check`, and `vision_alignment` from canonical task context, and forwards `architecture_check` into `complete_task(...)`. Goal: remove `planner_delivery_proof_missing` as a transport/metadata failure mode for planner-owned closures like `BATCH-71-PLAN`.
- Live follow-up after the planner proof transport fix: `BATCH-71-PLAN` was completed canonically (`state=DONE`) and the stream advanced to `BATCH-71-ANALYSIS state=READY_PLANNER`; queue and workboard now both expose `next_action="claim BATCH-71-ANALYSIS (READY_PLANNER pour planner)"`.
- Prompt anti-regression added in `platform/automation/cron_tmux_role_runner.sh`: planner instructions now explicitly remind that `PLAN/ANALYSIS/ARCH/GOV_REVIEW` completion requires non-empty `root_cause`, `fix_applied`, `architecture_check`, `vision_alignment`, plus structured `verify=before=...; after=...; test=...`.

- Auxiliary session revalidation after planner proof recovery: `admin-agents-sync-cron`, `adminapp_codex_sync`, and `clawsentinel` are all absent on the live VM. They are no longer treated as merely unvalidated; they are missing auxiliaries and should be recreated intentionally if still required. Canonical delivery flow remains on `BATCH-71`, with `BATCH-71-PLAN` completed and `BATCH-71-ANALYSIS` as the next planner gate.
- Live admin/infra recheck after canonical planner closure: direct workboard read confirms `BATCH-71-PLAN state=DONE` and `BATCH-71-ANALYSIS state=IN_PROGRESS`; queue/workboard still pin `active_cycle.active_batch_ids=["BATCH-71"]`. This is now the only stream that counts.
