# Daily Memory 2026-04-15

## Orchestration architect summary
- verdict: file-only mac-host audit; app progress is real, but orchestration is poor and current delivered value is moderate.
- top priority: ship the current apps/api + apps/monitor reliability lot independently of orchestration cleanup.
- blocker: BATCH-85-PLAN stays READY_PLANNER in workboard while BATCH-85 still has zero SQLite rows/events.
- next useful delivery: release the brief/action/memo reliability hardening slice now.

## Admin unblock summary
- blocker: queue/workboard sync was backfilling a synthetic `BATCH-85-PLAN` ahead of an already-started autobatch `BATCH-85-ANALYSIS`, causing false planner WIP blockage
- fix: patched `parallel_workstream.py` to preserve autobatch analysis-only streams during sync, prune stray upstream PLAN tasks, and keep autobatch next_action on ANALYSIS
- resume: yes
- next_owner: planner

## Orchestration architect summary
- verdict: projection drift improved in queue/workboard, but runtime truth is still stuck on BATCH-84 and delivery orchestration remains poor.
- top priority: ship the current apps/api + apps/monitor reliability lot independently of orchestration cleanup.
- blocker: BATCH-85 is active only in queue/workboard; orchestration-runtime.sqlite still has zero BATCH-85 rows/events and stale guard files still block on removed BATCH-85-PLAN.
- next useful delivery: release the copilot/portfolio/monitor reliability slice now.

## Role prompt engineer summary
- role: planner
- issue: planner prompt remained oversized and duplicated rules already injected by system/shared/guardian layers, weakening collect-first behavior
- patch: shortened the `ROLE=planner` block in `/Users/venom/Documents/analyse-financiere/platform/automation/cron_tmux_role_runner.sh` from 7186 to 2525 chars while keeping decision order, canonical tools, and critical proof guards
- expected_gain: fewer stale redispatches or new-batch impulses and clearer claim/collect/complete behavior

## Orchestration architect summary
- verdict: file-only mac-host audit; app progress is still real, but orchestration remains poor because `BATCH-85` now exists in SQLite/runtime truth while queue and monitor still disagree on what is active.
- top priority: ship the current `apps/api` + `apps/monitor` reliability lot independently, then VM-validate it.
- blocker: host_context_blocked for live validation, with structural drift across `priority-queue.json`, `parallel-workstreams.json`, `orchestration-runtime.sqlite`, and `executors-monitoring-latest.json` on `BATCH-85`.
- next useful delivery: release the current copilot/portfolio/monitor reliability slice with cached context, lightweight portfolio fallbacks, and event-store-first monitor fixes.

## Admin unblock summary
- blocker: historical out-of-cycle SQLite planner rows were still exposed as active subagents, pushing planner into `COLLECT_ACTIVE_CAPABILITY` and `planner_subagent_not_found` on `BATCH-85`
- fix: patched `planner_board_runtime.py` to source active-subagent and collect-pending signals from active-cycle-filtered `dispatch_snapshot`, then added a regression test
- resume: yes
- next_owner: planner

## Orchestration architect summary
- verdict: file-only mac-host audit; app lot is still real, but orchestration remains poor because `BATCH-85` moved only in projection and planner is now blocked on a missing subagent ref.
- top priority: ship the current apps/api + apps/monitor reliability lot independently of orchestration cleanup.
- blocker: `BATCH-85-ARCH` is blocked in planner collect/materialization while `orchestration-runtime.sqlite` still has zero `BATCH-85` rows/events.
- next useful delivery: release the brief/action/memo reliability slice now.

## Role prompt engineer summary
- role: planner
- issue: shared orchestration prompt repeated planner autonomy rules and could push new batch creation before collect/repair/ack on canonical downstream active work
- patch: shortened the planner section in `platform/automation/cron_tmux_role_runner.sh` to prioritize canonical active-task follow-up and keep batch creation as fallback only
- expected_gain: less redispatch churn, fewer stale ANALYSIS relaunches, clearer planner next action

## Admin unblock summary
- blocker: no active delivery blocker is visible on `BATCH-85`; this run is blocked only on live VM validation because sandboxed SSH cannot reach `dev-vm-utm`
- fix: none applied; confirmed the prior `BATCH-85` sync fix still holds in local canonical state and that stale `BATCH-84` runtime residue is quarantined
- resume: yes
- next_owner: planner

## Orchestration architect summary
- verdict: projection drift improved in queue/workboard, but runtime truth is still stuck on BATCH-84 and delivery orchestration remains poor.
- top priority: ship the current apps/api + apps/monitor reliability lot independently of orchestration cleanup.
- blocker: BATCH-85 is active only in queue/workboard; orchestration-runtime.sqlite still has zero BATCH-85 rows/events and stale guard files still block on removed BATCH-85-PLAN.
- next useful delivery: release the copilot/portfolio/monitor reliability slice now.
