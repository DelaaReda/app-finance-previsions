# Daily Memory 2026-03-09

- Monitor access canonicalized to LAN-only: `http://192.168.64.9:7780/` for host-facing UI, `http://127.0.0.1:7779/` inside VM.
- FC Monitor dashboard now exposes a `Vue Live Canonique` panel rendered from `/api/status?lite=1`, plus `/api/monitor/access`, so operators/Atlas can trust the live state even when heavier panels partially time out.
- Planner subagent fallback hardened: `planner_subagent_manager.py` now falls back to Qwen not only after `codex_exec` quota hits but also for `openclaw` quota/rate-limit failures and pre-existing codex/global rate-limit caches; effective backend is surfaced as `qwen`.
- Qwen CLI fallback is now verified headless on the real `fc_agent_tick -> cron_tmux_role_runner` path: direct `qwen` one-shot returns `OK`, fallback skips cleanly when auth is missing, and once auth is configured it launches `qwen_scrum_master_cron` and completes the tick. Default readiness probe raised to `20s`.
