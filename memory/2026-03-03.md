# Daily Memory 2026-03-03

## Hotfix execution - queue/workboard sync + forecast fallback
- Resolved `BATCH-05/06/07` state desync across planning artifacts:
  - `docs/operations/orchestrator/priority-queue.json`
  - `docs/operations/orchestrator/parallel-workstreams.json`
  - `docs/product/planning/WORKSTATE.md`
- Normalized active flow to:
  - `BATCH-05=IN_PROGRESS`
  - `BATCH-06=WAITING_DEP (depends_on BATCH-05)`
  - `BATCH-07=WAITING_DEP (depends_on BATCH-06)`
- Fixed forecast fallback regression in `apps/api/src/platform/legacy/jobs/forecasts_simple.py`:
  - fallback now uses real change/cache, then quote delta (`price` vs `previous_close`), then raw `change_percent`.
  - neutralizes up-day ratio when no history exists to avoid directional bias.
- Validation:
  - `PYTHONPATH=. pytest -q platform/legacy/jobs/tests/test_forecasts_simple.py` -> PASS
  - targeted copilot/forecasts/judge/market_data tests -> PASS
- Workspace hygiene:
  - moved 3 malformed root artifact files into `.trash/root-garbage-20260303/`.

## Monitoring agents runtime (snapshot 2026-03-03 00:53Z)
- Runtime topology active: canary cron profile with 3 role ticks (`planner`, `dev`, `admin`) + utility jobs.
- Sessions tmux présentes: `codex_planner_cron`, `codex_dev_cron`, `codex_admin_cron` + anciennes lanes spécialisées.
- Health check:
  - Backend/frontend UP.
  - Rate-limit hits today elevated.
  - orchestration quality warnings: `signal_unparseable`, `session_not_ready`, `contract_guard blocks`.
- Tick status:
  - `dev`: latest tick OK (`rc=0`).
  - `admin`: latest tick OK (`rc=0`).
  - `planner`: latest tick ended `rc=124` after `primary_prompt_end rc=124` + `retry_prompt_begin`, no final output line logged.
- Live process observation:
  - one `timeout 90 codex ...` planner retry observed as orphaned child (`ppid=1`) during tick window, then exited; no active planner process after timeout window.
- Executors monitor (updated_at_utc `2026-03-03T00:50:09Z`):
  - planner `PASS`
  - backend/frontend/data_analyst still `BLOCKED` with workboard/intent mismatch signals.
- Live watchdog note:
  - `scripts/tmux_live_watchdog.sh start` reports `started` but immediate `status=no`; likely ROOT path mismatch in watchdog script (`../..`).

## Monitoring follow-up (2026-03-03 01:00Z window)
- `admin` tick `00:56Z` completed successfully (`rc=0`) but remains BLOCKED in executor monitor with `ROLE_OUTPUT_NOT_SPECIFIC` / `CHANNELS_READ_INVALID`.
- `planner` timeout case persisted for tick `00:44Z` (`rc=124`) with retry started but no closure trace line.
- `planner` cron resumed as expected at `01:00:04Z` (`primary_prompt_begin` observed), execution still in progress at `01:00:31Z`.
- `dev` remained stable on last completed tick (`00:50:09Z`, `rc=0`), next scheduled execution pending.

## Prompt optimization (runner) - deep analysis applied
- `scripts/cron_tmux_role_runner.sh` compacted to reduce prompt bloat and retry loops.
- `load_3day_memory_context()` now profile-aware + lean summaries:
  - coordination: daily=24, role_history=18
  - analysis: daily=14, role_history=12
  - delivery: daily=8, role_history=8
  - per-line clipping (`TMUX_ROLE_MEMORY_MAX_LINE_CHARS=180`) to avoid giant memory lines.
- Dispatch mode split:
  - `primary` => full orchestration protocol
  - `retry` => compact orchestration protocol
- Added prompt-size observability traces:
  - `prompt_memory_context ... bytes=<n>`
  - `dispatch_prompt scope=primary|retry ... bytes=<n>`
- Synced path note: `scripts/cron_tmux_role_runner.sh` and `platform/automation/cron_tmux_role_runner.sh` resolve to same canonical file.

## Deep dive agent execution (2026-03-03 07:50-07:55 EST)
- Confirmed `dev` agent did analyze execution path (not idle): contract and memory show explicit `workboard/context + claim` verification with state mismatch note.
- Reproduced root cause: `status` showed `BATCH-06-DEV-*` as `READY` while `claim` failed `CLAIM_ERROR ... not READY`.
- Root cause in code: `claim_task()` used persisted board state without `recompute_states()` unlike `status`.
- Applied fix in `platform/automation/parallel_workstream.py`: run `recompute_states(board)` before claim candidate selection.
- Validated claim flow on temp board: explicit claim with valid prechange plan/checks now returns `CLAIM_OK`.
- Unblocked local tooling compatibility: `scripts/parallel_workstream.py` symlink repointed to relative canonical target (`../platform/automation/parallel_workstream.py`), fixing broken absolute path.
- Prompt/runtime mismatch fixed in `platform/automation/cron_tmux_role_runner.sh` (dev lane): claim instruction now includes valid `--change-plan` + `--architecture-checks` shape required by prechange gate.
- Monitor drift found: web monitor process had cwd on deleted inode (`/home/venom/shared/analyse-financiere (deleted)`), causing stale/inconsistent UI behavior.
- Monitor restarted from canonical repo path; `/api/status` now reports consistent health (`STALE` when planner/admin are in rate-limit backoff).

## Lean contract pass (post-audit bottlenecks)
- Confirmed `platform/policies/role_contract_guard.py` remains lean (~198 lines) and lowered `run_note` threshold from 5 words to 3 words to reduce form-overhead.
- Aligned prompt contracts in `platform/automation/cron_tmux_role_runner.sh`:
  - planner/dev/qa/architect instructions now target minimal EVIDENCE fields only.
  - retry checklist now asks only artifact + task_update + lock_check + run_note (+ stream/task if active, cmd/tests on complete, cmd_err_excerpt for permission blockers).
- Fast-path retry already active in runner:
  - `TMUX_ROLE_SKIP_TMUX_RETRY_IF_CODEX=1` skips tmux retry when codex fallback is available.
- Product board conflict removed:
  - `docs/product/planning/PROJECT_BOARD.md` switched `BATCH-07` from READY to `WAITING_DEP (fermé tant que BATCH-06 non clôturé)`.

## Runtime cleanup check (2026-03-03 01:19Z)
- Purged expired RL cache file from runtime state:
  - trashed `/home/venom/.openclaw/cron/role-state/planner.rate_limit_gate_cache`
  - cache payload was expired (`until=1772500055`, current epoch greater at check time)
- Workboard state sanity:
  - `parallel-workstreams.json` now has `tasks_len=48`
  - recursive micro-tasks/streams (`BATCH-05-*-*` style 4+ segments) detected: `0`
  - remaining `BATCH-05` tasks are only canonical trio (`PLAN/BACKEND/FRONTEND`) in `DONE`
- Assignment sanity:
  - `BATCH-06-BACKEND` is `role=backend_engineer`, `assignee=backend_engineer`, `state=READY`
- Intent registry sanity:
  - `docs/operations/orchestrator/intent-registry.json` has `active_total=0` (including `data_analyst=0`)
- Monitor snapshot after cleanup (`scripts/monitor_agents.sh`):
  - queue active: `BATCH-06 READY`, `BATCH-07 WAITING_DEP`
  - roles `planner/backend_engineer/frontend_engineer/data_analyst` show `delta=READY_ITEM_AVAILABLE`
  - rate-limit section: `codex` and `qwen` both show no active rate-limit

## Orchestration mismatch fix (2026-03-03 late session)
- Confirmed mismatch source:
  - crontab active jobs = `planner`, `dev`, `admin` (`fc_agent_tick.sh`)
  - `parallel-role-topology-active.json` still listed legacy 4 roles
  - monitor script was hardcoded to legacy roles
- Applied alignment fixes:
  - updated `docs/operations/orchestrator/parallel-role-topology-active.json` to 3-role lean mode
  - updated `docs/operations/orchestrator/parallel-role-topology.json` to same 3-role baseline
  - patched `scripts/auto_recover_tmux_roles.sh` with explicit `admin -> codex_admin_cron` mapping
  - rewrote `scripts/monitor_agents.sh` to discover active roles dynamically from crontab and report canonical workboard coverage
  - monitor now reports tmux session drift (`orphans`) vs expected sessions
- Verification:
  - `bash scripts/monitor_agents.sh` now shows `ACTIVE CRON ROLES: planner, dev, admin`
  - monitor confirms `COVERAGE: cron roles align with workboard canonical lanes`
  - monitor snapshot currently reports 4 orphan tmux sessions (legacy lanes + qwen_dev), no respawn expected with updated topology
  - `python3 scripts/parallel_workstream.py context --role dev --limit 5` includes `BATCH-06-BACKEND/BATCH-06-FRONTEND/BATCH-06-DATA` in dev lane context (alias coverage working)

## Lean contract + prompt simplification (2026-03-03 01:27Z)
- Contract guard adjusted to keep strict-but-minimal enforcement:
  - `run_note` minimum raised to 5 words.
  - `task_update=complete` now requires both `cmd` and `tests_run`.
- Runner prompt contracts simplified in `platform/automation/cron_tmux_role_runner.sh` (via `scripts/` symlink):
  - removed mandatory overhead fields (`channels_read`, `impact_*`, `arch_rule/conformance/violations`, `reflection_*`, `self_qa` hard requirements).
  - role prompts now require lean evidence core: `task_update`, `lock_check`, `run_note`, role artifact, and conditional `stream_id/task_id`, `cmd/tests_run`.
- Retry chain optimized:
  - added `TMUX_ROLE_SKIP_TMUX_RETRY_IF_CODEX` (default `1`) to skip redundant tmux retry when codex fallback is available.
  - expected path in tmux mode: `primary tmux -> codex_exec fallback` (no intermediate tmux retry).
- Rate-limit/fallback evidence payloads reduced to lean fields (less contract noise).
- Queue/workboard gate check confirmed no BATCH-07 claim conflict:
  - `BATCH-07` remains `WAITING_DEP` in queue and all `BATCH-07-*` tasks are `WAITING_DEP` in workboard.
- Validation note:
  - `pytest -q platform/automation/tests/test_role_contract_guard.py` currently fails before assertions due stale test path (`/home/venom/shared/analyse-financiere/platform/scripts/role_contract_guard.py` missing).

## Task naming cleanup (2026-03-03 session local)
- Removed role-labeled active task IDs for current delivery streams to reduce confusion in lean topology:
  - `BATCH-06-BACKEND/FRONTEND/DATA` -> `BATCH-06-DEV-01/02/03`
  - `BATCH-07-BACKEND/FRONTEND/DATA` -> `BATCH-07-DEV-01/02/03`
- Updated both stream task templates and active task objects in:
  - `docs/operations/orchestrator/parallel-workstreams.json`
- Standardized active delivery roles to canonical `dev` for these tasks and enriched descriptions/notes for API contracts, UX delivery, and signal-quality tracks.
- Updated queue next actions in `docs/operations/orchestrator/priority-queue.json` to remove stale `attendre clôture BATCH-05` and reference new `DEV-01/02/03` tasks.
- Runtime check: `python3 platform/automation/parallel_workstream.py status --role dev --compact` now reports READY tasks `BATCH-06-DEV-01/02/03`.

## Prompt acceleration pass (planner/dev/admin) — delivery speed
- Patched `platform/automation/cron_tmux_role_runner.sh` to enforce a Claude-like delivery style:
  - mandatory per-tick chain: `root_cause -> fix_applied -> verify`
  - stronger run note requirement in prompt (`>=5` words)
  - `dev` specialization by task id (`DEV-01 API/back`, `DEV-02 UI/front`, `DEV-03 data quality/scoring`)
  - anti-stall rule: no `analysis_only` when READY/IN_PROGRESS exists
- Fixed missing admin prompt bug in runner:
  - added explicit `build_prompt` branch for `admin` (runtime unblock + health/recovery + evidence)
  - added `required_artifact_marker_for_role(admin)=ADMIN_ARTIFACT`
- Synced contract guard with admin role artifact:
  - `platform/policies/role_contract_guard.py` now recognizes `admin_artifact`
- Updated supervision prompt template:
  - `docs/ops/prompts/CODEX_SUPERVISION_SAME_PROMPT.txt` now enforces root-cause/fix/verify loop + DEV specialization + anti-stall wording
- Updated reliability spec for lean canonical lanes and prompt delivery standard:
  - `docs/ops/ORCHESTRATION_RELIABILITY_SPEC.md` now references `planner/dev/admin` lane coverage and delivery-style evidence fields.

## Workspace root cleanup + FC Monitor organization
- Cleaned root-level clutter by relocating non-project UI bundles to archive:
  - moved `index.js`, `main-D_pzgCQe.js`, `MainWindowPage-DXEzOgrP.js` to `archive/artifacts/claude-desktop-ui/2026-03-03/`.
- Reorganized monitor dashboard into canonical app location:
  - moved monitor server from `scripts/monitor_server.py` to `apps/monitor/server.py`
  - added compatibility wrapper at `scripts/monitor_server.py` (run-path launcher to canonical server)
  - added `apps/monitor/README.md`
- Relocated misc root files into structured folders:
  - `SSH_MCP_SOLUTION.md` -> `docs/ops/SSH_MCP_SOLUTION.md`
  - `BATCH-03-HANDOFF.md` -> `handoff/legacy/BATCH-03-HANDOFF.md`
  - `_DIAGNOSTIC.sh` -> `scripts/diagnostics/_DIAGNOSTIC.sh`

## Docs sync only (2026-03-03 01:33Z)
- Updated documentation only (no runtime file edit):
  - `docs/ORCHESTRATION_LEAN.md`: added critical cron coverage section (`planner`, `backend_engineer`, `frontend_engineer`, `data_analyst`) with stagger schedule and verification commands.
  - `docs/ops/ORCHESTRATION_RELIABILITY_SPEC.md`: CRON_GATE now explicitly requires the same 4 critical roles + stagger; Runtime Contract Policy section now documents lean evidence baseline.
  - `docs/ops/ORCHESTRATION_AGENTS_READY.md`: added explicit warning that lane normalization can hide missing delivery roles; added quick cron grep check and updated lean contract minimum (`run_note>=5`, `complete=>cmd+tests_run`).
  - `docs/ops/AGENTS_READY.md`: added operational clarification for cron coverage during delivery phases.

## Monitoring improvements (2026-03-03 01:41Z)
- Enhanced `scripts/monitor_agents.sh` for runtime truth visibility:
  - added explicit `CRON COVERAGE` check for critical roles (`planner`, `backend_engineer`, `frontend_engineer`, `data_analyst`) from live `crontab -l`.
  - added monitor freshness line from `executors-monitoring-latest.json` (`fresh/stale` by age).
  - added role source display (`src`) and schedule column tied to actual cron lines.
  - added `DISPATCH SAFETY` block to detect `BATCH-06`/`BATCH-07` READY conflicts.
  - expanded last tick scan window (`tail -60`) and fixed line truncation for stable panel rendering.
- Validation:
  - `bash -n scripts/monitor_agents.sh` -> PASS
  - `bash scripts/monitor_agents.sh` -> output shows cron coverage + freshness + dispatch safety.

## Documentation alignment pass (2026-03-03 late)
- Updated orchestration docs to remove legacy 4-role assumptions and align with canonical lanes monitoring (`planner/dev/admin`).
- `docs/ops/ORCHESTRATION_RELIABILITY_SPEC.md`:
  - replaced hardcoded `4 active tick jobs` gate with canonical lane coverage logic from `crontab`.
  - documented session expectations by canonical lane and added `scripts/monitor_agents.sh` as runtime source-of-truth command.
- `docs/ops/ROLE_CONTRACT_EVIDENCE_SCHEMA.md` rewritten to lean v2:
  - baseline = 8 keys + minimal evidence (`task_update`, `lock_check`, `run_note>=3`, role artifact),
  - conditional requirements only (`stream_id/task_id`, `cmd`, `handoff_to`, `cmd_err_excerpt` for permission blockers),
  - removed mandatory overhead fields from schema.
- `docs/ops/ORCHESTRATION_AGENTS_READY.md` rewritten:
  - canonical lane mapping rules, lean contract checklist, mismatch/orphan troubleshooting, updated supervision commands.
- `docs/ORCHESTRATION_LEAN.md` refreshed:
  - clarified crontab source-of-truth via `crontab -l` + monitor,
  - updated roadmap snapshot (`BATCH-06 READY`, `BATCH-07 WAITING_DEP`),
  - removed stale fixed cadence wording from role sections.

## Archive recovery verification (items 2/3/5/6/7/8)
- Verified requested items against active runtime architecture (`finance-copilot.sh` -> `apps/api/runtime/copilot.sh` -> `apps/api/src/platform/run_api.py`).
- Item 2 (legacy judge/cache docs): confirmed adaptation required before reintegration; old `src/services` path references do not match current domain-based layout.
- Item 3 (api-v1 compat wrappers): confirmed not required in current runtime (namespace routing via `apps/api/src/platform/routes/__init__.py`).
- Item 5 (archived orchestrator/agent modules): confirmed not used in app launch path.
- Item 6 (judge profiles utility): restored profile YAMLs to active path:
  - `apps/api/src/data/judge_profiles/equity_1w.yaml`
  - `apps/api/src/data/judge_profiles/sector_regime.yaml`
- Item 7 (legacy scripts migration):
  - confirmed Stooq fallback already integrated in `apps/api/src/platform/legacy/jobs/stocks_prices_refresh.py`.
  - removed stale optional call to removed `fetch_prices_stooq.sh` in `apps/api/runtime/copilot.sh`.
  - `validate_marker.sh` remains legacy/obsolete (not reintegrated).
- Item 8 (skills audit artifacts): moved back to expected repo path `finance-app/`:
  - `openclaw-skill-audit.json`
  - `openclaw-skill-security-deep-2026-02-24.json`
  - `openclaw-skill-security-deep-2026-02-24.md`
  - `openclaw-skills-rollout-2026-02-24.md`

## Architecture audit + VM batches (2026-03-03 session)
- Performed runtime benchmark via official launcher (`./finance-copilot.sh`) with probe scripts:
  - API core latency very low (health p95 ~25ms; forecasts/recommendations/brief p95 <= 6ms).
  - Startup wall time measured around `41.34s` (`/usr/bin/time -p`).
  - Blocking regressions confirmed:
    - `/api/stocks/SPY/sheet` returns 404 (`No module named 'research.scoring'`).
    - `/api/macro/series/latest` returns 404.
    - startup logs contain runtime compatibility/import failures:
      - `news_ingest.py`: `datetime | None` TypeError.
      - `validate_and_generate_data.py`: `No module named 'jobs.forecasts'`.
      - missing legacy job reference `judge_quality_report.py`.
- Produced architecture artifacts for team execution:
  - `docs/product/planning/ARCH_MIGRATION_PERF_AUDIT_2026-03-03.md`
  - `docs/product/planning/ARCH_BATCHES_VM_P0_P2_EXEC_SPEC_2026-03-03.md`
- Batch state:
  - `BATCH-VM-P0-01` marked done (runtime bootstrap already integrated).
  - next recommended flow: `P0-02` (compat/import bridge) -> `P0-03` (endpoint contract recovery) -> `P1/P2`.

## BATCH-VM-P0-02 execution (done)
- Applied code fixes:
  - `apps/api/src/research/__init__.py`: namespace bridge now includes current `research/` + legacy `platform/legacy/research/`.
  - `apps/api/src/platform/legacy/jobs/validate_and_generate_data.py`:
    - added `from __future__ import annotations` (runtime safety),
    - fallback import to `jobs.forecasts_simple` when `jobs.forecasts` is absent.
  - `apps/api/src/platform/legacy/jobs/news_ingest.py`: added `from __future__ import annotations`.
- Runtime verification (launcher + probes):
  - `/api/stocks/SPY/sheet` switched from 404 to `200` (payload returned).
  - no match in logs for:
    - `TypeError unsupported operand type(s) for |`
    - `No module named 'jobs.forecasts'`
    - `No module named 'research.scoring'`
- Remaining P0 contract gap:
  - `/api/macro/series/latest` still returns `404` (tracked for `BATCH-VM-P0-03`).
- Added verification report: `docs/ops/ARCHIVE_RECOVERY_VERIFICATION_2026-03-03.md`.

## Continuous dev pass (architecture + orchestration hardening)
- Corrected Judge LLM ranking path resolution in `apps/api/src/domains/judge/application/g4f_client.py`:
  - `_src_root()` now resolves to `apps/api/src`
  - `_backend_root()` now resolves to `apps/api/src/platform/legacy`

## Monitor hardening + obsolete roles cleanup (2026-03-03 17:5x EST)
- Monitor API role discovery locked to active topology (`parallel-role-topology-active.json`) to prevent obsolete historical roles from appearing in UI tabs/cards.
- `apps/monitor/server.py` endpoints now resolve roles dynamically per request (`active_roles()`), and `/api/status` now returns explicit `roles`.
- `scripts/health_snapshot.sh` output enriched with timestamp + `blocked_detail` and `rl_detail` for faster diagnosis in monitor panels.
- `scripts/cleanup_monitoring_noise.sh` hardening:
  - includes `health-snapshot.log` trimming,
  - compacts repeated legacy permission-noise burst in `role-recovery.log` into one summary marker.
- Runtime validation on VM:
  - `/api/status`, `/api/log-catalog`, `/api/agent-insights`, `/api/log-view` all reachable.
  - roles visible in monitor APIs: `planner`, `dev`, `admin` only.
  - confirmed `working.json` is found at runtime.
- Removed cross-layer dependency in `apps/api/src/domains/copilot/application/copilot_service.py`:
  - dropped `services.service_standard` bridge import for `utc_now_iso`
  - added local canonical UTC timestamp helper.
- Hardened simple forecasts job determinism in `apps/api/src/platform/legacy/jobs/forecasts_simple.py`:
  - removed `sys.path.insert(...)` and random-based fallback/noise
  - added deterministic fallback and deterministic reasoning selection for stable outputs and easier troubleshooting.
- Updated Judge fallback tests to target canonical module path:
  - `apps/api/src/domains/judge/tests/test_g4f_client_model_fallback.py` now imports `domains.judge.application.g4f_client`.
- Strengthened workspace validation guard:
  - `platform/automation/validate_agent_workspace_layout.sh` now checks runtime ghost path, ghost RAG file, `.bak*` drift, root clutter files, bridge debt markers, and supports GNU/BSD date handling.
- Improved supervision specs/prompts:
  - `docs/ops/prompts/CODEX_SUPERVISION_SAME_PROMPT.txt` now includes strict P0 architecture hardening order + reuse-first contract.
  - `docs/ops/ORCHESTRATION_RELIABILITY_SPEC.md` now includes `ARCH_PATH_GATE` and `REUSE_GATE`.
- Validation run:
  - `python3 -m py_compile ...` (edited python files) -> PASS
  - `cd apps/api/src && PYTHONPATH=. pytest -q domains/judge/tests/test_g4f_client_model_fallback.py platform/legacy/jobs/tests/test_forecasts_simple.py` -> PASS
  - `bash platform/automation/validate_agent_workspace_layout.sh` -> PASS with WARN (expected existing drift artifacts).

## Continuous dev pass (bridge reduction + runtime path cleanup)
- Rewired active API/runtime imports away from bridge modules where possible:
  - `apps/api/src/platform/main.py` now imports copilot/g4f/llm from canonical domain/legacy modules.
  - `apps/api/src/domains/copilot/api/{copilot.py,context.py}` now resolve `ContextService` from domain service first.
  - `apps/api/src/domains/judge/api/judge.py` now resolves judge pipeline/builder/g4f via domain modules.
  - `apps/api/src/domains/forecasts/api/recommendations.py` and `apps/api/src/domains/market_data/api/{correlations.py,stocks_extra.py}` now use domain application services first.
- Canonicalized tested-model artifact location:
  - `apps/api/src/domains/judge/application/g4f_client.py` now prefers `apps/api/runtime/data/llm/models/tested_g4f_models*.json` with `src/` fallback.
  - `apps/api/src/platform/legacy/scripts/test_g4f_models.py` output path changed to `apps/api/runtime/data/llm/models/tested_g4f_models.json`.
  - `apps/api/runtime/copilot.sh` log text updated to runtime tested-model path.
- Reduced root clutter:
  - moved `apps/api/src/api.log` -> `apps/api/runtime/logs/api.log`.
  - moved `apps/api/src/juge-appel*.json` -> `apps/api/runtime/data/judge/`.
- Archived legacy backup artifacts (recoverable cleanup, no hard delete):
  - moved all `*.bak*` from `apps/api/src` and `scripts` to `archive/obsolete-backups/2026-03-03/` (11 files).
- Validation run:
  - `python3 -m py_compile` on all touched modules -> PASS
  - `cd apps/api/src && PYTHONPATH=. pytest -q domains/judge/tests/test_g4f_client_model_fallback.py domains/copilot/tests/test_copilot_service.py platform/legacy/jobs/tests/test_forecasts_simple.py` -> PASS (`12 passed`)
  - `bash platform/automation/validate_agent_workspace_layout.sh` -> PASS (`warn` reduced to 4; `.bak*` warning removed).

## Orchestration hardening pass (BATCH-26 lean chain) — 2026-03-03 evening
- Aligned active hardening execution model to canonical lanes only:
  - Rewrote BATCH-26 execution chain in queue/workboard to PLAN -> DEV-01 -> DEV-02 -> DEV-03 -> ADMIN-01 -> GOV-REVIEW.
  - Removed active BATCH-26 task IDs with backend/frontend/data labels to reduce lane confusion.
- Strengthened dev prompt and specs for execution quality:
  - platform/automation/cron_tmux_role_runner.sh dev prompt now enforces reuse_check=<module|NONE(reason)>, forbids doc-only patch on DEV-* unless explicit, and adds stricter specialization guidance.
  - docs/ops/ORCHESTRATION_RELIABILITY_SPEC.md now requires reuse_check for delivery claim/complete and codifies lean active IDs.
  - docs/ops/prompts/CODEX_SUPERVISION_SAME_PROMPT.txt updated with explicit BATCH-26 chain + reuse_check requirement.
  - docs/product/planning/tasks.md role claim examples migrated to planner/dev/admin canonical lanes.
  - docs/product/planning/WORKSTATE.md refreshed to BATCH-26 runtime reality.
- Process guard fix:
  - Added admin to ROLE_CATALOG in platform/automation/parallel_workstream.py to support canonical admin lane in status/context/claims.
- Validation snapshot:
  - JSON syntax checks PASS for queue/workboard.
  - parallel_workstream.py status --role dev --compact now exposes BATCH-26-DEV-01 as READY.
  - parallel_workstream.py status --role admin --compact now works (no UNKNOWN_ROLE for admin).
  - parallel_workstream.py validate still BLOCKED on historical proof artifacts and old done-task hygiene (pre-existing debt), not on the new BATCH-26 lean chain.

## Planner autonomy hardening (2026-03-03)
- Added continuous planner quality loop via `platform/automation/planner_guardian.py` integrated in `platform/automation/cron_tmux_role_runner.sh`.
- Guardian now scores planner contracts and tracks streaks (`ready_idle`, `low_score`, `runway_no_batch`) and can emit escalation directives on repeated drift.
- Planner prompt now auto-injects latest guardian feedback (`score/issues/recommendations`) each tick for closed-loop self-correction.
- Added architecture/vision traceability checks in guardian (canonical refs expected: PRODUCT_VISION/BATCH + apps/api/apps/web paths).
- Added runbook doc in month folder: `docs/ops/2026-03/PLANNER_GUARDIAN_LOOP.md`.
- Added planner clean troubleshooting logs in runner:
  - `logs-codex-runs/role-runner/planner.events.log` (dedup structured events)
  - `docs/orchestrator-ops/planner-timeline.log` (human-tail timeline)
  - `docs/orchestrator-ops/planner-audit-events.jsonl` (contract/evidence + guardian score)
- FC Monitor logs access improved for planner troubleshooting:
  - added endpoints `/api/logs/{role}/events`, `/api/planner/timeline`, `/api/planner/log-bundle` in `apps/monitor/server.py`.
  - dashboard link rows now expose planner bundle/timeline and role events.
  - public check on `https://fc-monitor.loca.lt` returned HTTP 200 for new endpoints after monitor restart.
- Added dedicated monitor UI page `/planner-debug` (auto-refresh) for planner supervision with guardian score/streaks/issues/recommendations + planner audit/timeline/runner events.
- Ensured public access via `https://fc-monitor.loca.lt/planner-debug` after VM monitor restart.
- VM monitor runtime now pinned in tmux session `fc_monitor_web` (with tunnel session `monitor_tunnel`).

## FC Monitor dynamic truth upgrade (2026-03-03)
- Updated `apps/monitor/server.py` to align monitor data reads with canonical architecture paths (`docs/operations/orchestrator/*`) with legacy fallback support.
- Added dynamic role discovery from runtime logs/contracts so monitor APIs (`/api/execution/all`, `/api/execution-insights/all`, `/api/ticks/all`) expose all detected agents, not only planner/dev/admin.
- Improved UI runtime truth and visibility:
  - 12s auto-refresh,
  - batch state chips + queue/workboard mismatch panel,
  - runtime freshness badge,
  - execution panel now shows tick + runner + events logs,
  - execution truth matrix per agent (activity, quality, issues, event/severity signals, interesting events).
- Improved visual troubleshooting of logs:
  - stronger severity classification (error/warn/action/contract/meta),
  - filtering of noisy Qwen/TUI lines in the UI (without deleting raw logs).
- Runtime validation:
  - monitor server restarted in VM (`fc_monitor_web` tmux session),
  - public checks OK on `https://fc-monitor.loca.lt` for `/api/status`, `/api/execution-insights/all`, `/`.

## FC Monitor UI regression fix (2026-03-03)
- Root-cause found for "monitor appears empty/0": frontend fallback rendered synthetic zeros when `/api/status` fetch failed (tunnel/API transient), instead of flagging source outage.
- Fixed in `apps/monitor/server.py` JS block:
  - `fetchJson()` now returns `{ok,data,error,url}` metadata.
  - `load()` keeps last good payload and marks `__status_unavailable` when status endpoint fails.
  - `render()` now shows explicit `Data source indisponible` banner and avoids fake numeric zeros (`—` placeholders) when data source is unavailable.
- Tunnel availability restored by restarting tmux session `monitor_tunnel` (localtunnel subdomain `fc-monitor`).
- Live validation: `https://fc-monitor.loca.lt/api/status` back to 200 with real queue/workboard metrics.

## Backend compat/runtime fixes (F-020/F-013 follow-up) — 2026-03-03
- Fixed legacy storage compatibility in `apps/api/src/platform/legacy/storage/base.py`:
  - unified writes to canonical `apps/api/runtime/data`,
  - kept backward-compatible `save_json` signatures (`(data, filename)` and `(key, payload)` + kwargs forms),
  - added runtime-first load with fallback read from legacy `platform/legacy/data`.
- Fixed Python 3.9 annotation runtime crashes:
  - added `from __future__ import annotations` to:
    - `apps/api/src/platform/legacy/research/llm_client.py`
    - `apps/api/src/platform/legacy/jobs/market_brief.py`
- Added missing endpoint alias in `apps/api/src/platform/main.py`:
  - new `GET /api/macro/series/latest` built from `/api/macro/series`.
- Validation (runtime interpreter `/usr/bin/python3`):
  - `health=200`, `/api/forecasts=200`, `/api/recommendations/daily=200`, `/api/stocks/SPY/sheet=200`, `/api/brief/daily=200`, `/api/macro/series/latest=200`.
  - startup log no longer contains `unsupported operand type(s) for |`.

## Admin unblock hardening (2026-03-03 late)
- Added writable-workspace guard in cron runner/recovery/tick/setup scripts to avoid selecting read-only shared roots and causing mkdir permission storms.
- Verified admin timeout baseline remains 300/120 with resume=1 in fc_agent_tick.sh.
- Confirmed admin memory context is no longer hard-disabled by default; opt-out only via TMUX_ROLE_ADMIN_DISABLE_MEMORY_CONTEXT=1.
- Monitor public status validated post-fix: fc-monitor.loca.lt returns queue/workboard data with mismatch_count=0.
