# Daily Memory 2026-04-08

- Admin/infra planner-progress recheck: the live canonical truth no longer matches the stale operator report verbatim. Queue already shows `BATCH-84=IN_PROGRESS`, but the real contradiction remains on `BATCH-84-ADMIN-01`: workboard row still had `state=IN_PROGRESS` while operational `status=READY_PLANNER`, with `planner_takeover_required=true`. This was overstating autonomous progress. Fix applied: `state_reconciler.py` now immediately normalizes stale `IN_PROGRESS` rows back to their operational status (`READY_*`, `BLOCKED`, `WAITING_DEP`), and `apps/monitor/server.py` now derives capability readiness/activity from the same operational-state logic instead of raw `state`. Result target: a task cannot count as active execution if canonical status still says ready-to-claim.

- Admin/infra continuation 2026-04-08: the planner->admin claim semantics are repaired, but live takeover still hit `planner_takeover_status_unavailable:timed out` during the backend-heal window. The remaining orchestration defect is that planner takeover was still depending on the heavy `/api/status` monitor surface instead of lightweight health truth.
- Fix applied 2026-04-08: `planner_runtime_actions.py::_planner_takeover_admin_task()` now uses `/api/status?lite=1` instead of the full status payload, treats status fetch failure as advisory (`health=UNKNOWN`) rather than hard failure, and continues to gate takeover on the actually critical surfaces: `doctor` runtime checks plus `app_runtime.backend_api.status`. New invariant: planner takeover must not fail only because the full monitor endpoint is slow while backend auto-heal is in flight.

## Planner progress check — 2026-04-08
- planner is alive (`PLANNER_DISPATCH_ACTIVE`) and still points to `BATCH-84-ADMIN-01`.
- flow is not truly advancing autonomously: queue still shows `READY_PLANNER=1`, `in_progress=0`, admin is only `READY` to claim, and product/backend runtime remain degraded.
- practical blocker stays the same: planner->admin handoff is not yet self-advancing into real execution.
- Admin/infra: claim_task aligne maintenant status=IN_PROGRESS avec state=IN_PROGRESS pour éviter qu\un reconciler/status-reader ne renvoie une tâche claimée vers READY_PLANNER. Cible immédiate: BATCH-84-ADMIN-01.
- Admin/infra: fixé un drift canonique workboard où claim_task pouvait laisser status=READY_PLANNER alors que state=IN_PROGRESS. recompute_states aligne désormais status sur state; après reconcile-state, BATCH-84-ADMIN-01 est IN_PROGRESS/IN_PROGRESS et le queue status lite montre ready=0, in_progress=1.
- Admin/infra: added a canonical delivery-runtime gate in `state_reconciler.py`. When an active-cycle capability handoff is ready for planner takeover but `status?lite=1` reports `backend_api.status != ok`, the reconciler now blocks the downstream task/stream/queue row with `policy_blocker=backend_runtime_required_before_takeover`, surfaces `meta.delivery_runtime_gate` in the queue, and restores the rows automatically when backend health returns. This removes the false steady state `READY_PLANNER=1 / in_progress=0` under degraded backend.
