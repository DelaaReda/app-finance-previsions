# Daily Memory 2026-03-07

## Runtime / monitor revalidation (VM-only)
- Revalidated planner-only orchestrator live after runtime/product fixes.
- Current truth:
  - `/api/status` => `health=OK`, `execution_mode=planner_experimental`, `core_roles=["planner"]`
  - `/api/doctor` => `status=ok`, `sessions=ok`, `queue_workboard=ok`, `product_value=ok`, `delivery_integrity=ok`
  - public monitor canonical URL restored: `https://fc-monitor.loca.lt/api/status` returns 200 again
- Product guard truth after backend + monitor reload:
  - `copilot.status=ok`
  - `forecasts.status=degraded` but `valid=true`
  - `priority_guard.status=ok` (`blocked_reasons=[]`)
- Remaining real problems:
  - forecasts are still stale/degraded, not invalid
  - queue snapshot still shows one planner-side inconsistency marker on `BATCH-58` (`queue IN_PROGRESS` while task state no longer in progress), even though doctor mismatch remains `0`
  - dev capability runway exists (`READY_DEV=4`) but throughput is still not moving automatically yet

## OpenClaw/Codex bridge hardening
- Root cause: OpenClaw built-in codex-cli backend used unsupported flags on `codex exec resume` (`--color`, then `--sandbox`) and repo-root `.codex/config.toml` was too expressive for OpenClaw parser.
- Fix: dedicated minimal capability/control-plane workspaces + `agents.defaults.cliBackends.codex-cli` override written by `platform/automation/openclaw_control_plane.py`.
- Validation: `python3 platform/automation/openclaw_control_plane.py --apply --validate-bridge --validate-agent planner --validate-timeout 45` now returns `bridge_validation.ok=true` with two consecutive planner calls (`fresh + resume`) on `codex-cli/gpt-5.4`.
- Browser state also confirmed healthy via `openclaw browser status --json`.
- Commits: `c554c7b` and `1a9f654`.

## Browser proof + planner dispatch observability
- Added canonical browser proof helper `platform/automation/browser_smoke.py` backed by OpenClaw browser (`start/open/wait/snapshot/console/errors/screenshot`) with JSON proof output under `logs-codex-runs/browser-smoke/`.
- Added shared planner dispatch metrics helper `platform/automation/planner_dispatch_metrics.py`; monitor and doctor now expose recent success/failure/fallback-like rates for planner-owned subagents/capabilities.
- `delivery_integrity` now reports browser-proof coverage for web-facing deliveries (`browser_proof_required_count`, `browser_proof_present_count`, `browser_proof_coverage`) without turning historical completions red by default.
- Live local monitor after restart:
  - `/api/status` exposes `planner_subagents.recent_success_rate` and browser-proof fields under `delivery_integrity`
  - `/api/doctor?refresh=1` exposes `checks.planner_dispatch`
- Public `loca.lt` tunnel remains less reliable than local monitor; local runtime is the source of truth.

## OpenClaw main WhatsApp agent defaults
- The main OpenClaw agent used for WhatsApp must stay on `codex-cli/gpt-5.4`.
- OpenClaw does not accept `thinkingDefault` inside `agents.list[].main`; that schema breaks gateway startup.
- Durable fix:
  - `agents.defaults.thinkingDefault = "xhigh"`
  - explicit `main` entry with `model = "codex-cli/gpt-5.4"`
  - repo sync source: `platform/automation/openclaw_control_plane.py`
- Live validation confirmed `openclaw agent --agent main --json` resolves to provider `codex-cli` and model `gpt-5.4`.
