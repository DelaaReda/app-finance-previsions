# Daily Memory 2026-03-06

## Planner/Scrum quality soft-autofix + monitor metrics (VM-only)
- Patched `role_contract_guard.py`:
  - planner quality fields (`root_cause`, `fix_applied`, `verify`, `reuse_check`) now soft-enforced without hard-blocking lanes,
  - preserved strict issue-report blockers and passive planner reconcile semantics.
- Patched runner + workstream:
  - scrum preflight executes `sync-priority` then `reconcile-state` (soft-fail, traced evidence),
  - new `parallel_workstream.py reconcile-state` command for non-destructive queue/workboard convergence.
- Patched monitor status payload:
  - orchestration metrics: `planner_quality_score`, `planner_quality_missing_count`, `scrum_actions_sent_60m`, `scrum_message_emit_skip_60m`,
  - agent payload enrichments for planner/scrum badges.
- Patched monitor fallback aggregator to always include new planner/scrum keys when agent payload is unknown.
- Validation (VM):
  - `python3 -m pytest -q platform/automation/tests/test_role_contract_guard.py platform/automation/tests/test_parallel_workstream_queue_sync.py platform/automation/tests/test_message_bus_auto_id.py platform/automation/tests/test_role_runtime_context.py apps/monitor/tests/test_status_never_null.py apps/monitor/tests/test_runtime_diagnostics.py`
  - Result: `74 passed`.

## Runtime unblock follow-up (planner/admin/scrum) — 2026-03-06
- Runner hardening:
  - `cron_tmux_role_runner.sh`: scrum advisory normalization now always applies for `scrum_master` (not only advisory mode flag), and reconciliation forces non-blocking advisory contract shape with `scrum_artifact/channels/impact`.
  - Removed Python 3.9+ type annotations in inline `build_scrum_master_auto_post_intents()` script to avoid runtime failures on Python 3.8 hosts.
  - Added planner quality pre-guard autofill for `claim|complete|handoff` (`root_cause`, `fix_applied`, `reuse_check`, `verify`, `vision_alignment`) with issue marker `planner_quality_autofill_missing`.
  - Added runtime-truth downgrade for `PLANNER_EVIDENCE_INCOMPLETE` to avoid hard loop-blocking.
- Monitor hardening:
  - `apps/monitor/server.py` now treats `RUNTIME_DOWN*` and `RUNTIME_DEGRADED*` admin blockers as stale-recoverable when doctor probes confirm API+monitor are up.
  - Planner blocker `PLANNER_EVIDENCE_INCOMPLETE` is tagged soft in monitor health classification.
- Tests executed:
  - `test_role_contract_guard.py`, `test_role_runtime_context.py`, `test_runner_message_receipts.py`, `test_agent_message_bus.py`, `test_planner_quality_guard.py` all pass.

## Monitor/Planner/Scrum runtime convergence follow-up (VM-only)
- Fixed scrum message-flow noise in `cron_tmux_role_runner.sh`:
  - cooldown now returns `BLOCK ...` non-fatal (no ERR trap spam),
  - bus `post` invocation moved to conditional form to avoid false `fatal_error` logs on dedup,
  - auto-intents extraction is soft-fail and no longer breaks run completion.
- Added admin runtime stale normalization in monitor (`apps/monitor/server.py`):
  - `runtime_down` blockers now auto-soften when live probes are healthy,
  - status reflects real runtime (`WAIT/PASS` + `RUNTIME_RECOVERED_SOFT`) instead of stale hard block.
- Hardened advisory safety in guard:
  - `scrum_master` missing artifact is autofilled (`scrum_artifact`) as low-severity advisory issue (non-blocking).
- Runtime check after patch:
  - `/api/status`: `health=OK`, `doctor=ok`, `planner=IN_PROGRESS`, `admin=PASS/WAIT`, `scrum_master=WAIT (non-blocking)`.
  - `orchestration`: planner quality and scrum action counters visible and non-null.

## Monitor instability + stale dev config signal (VM-only)
- Root cause found in `scripts/monitor_stack_guard.sh`:
  - guard lock FD (`/tmp/fc-monitor-guard.lock`) could leak into long-lived child (`run_api.py`) via `exec_safe ./finance-copilot.sh start`, causing later guard runs to skip forever.
  - fixed by closing FD 9 before `exec_safe` and rotating lock default to `/tmp/fc-monitor-guard.v2.lock`.
- Reduced false monitor restarts:
  - local liveness now checks only `/api/status` (no hard dependency on heavier `/api/runtime-diagnostics`).
  - monitor probe hardened in `apps/monitor/server.py` (`_probe_http_ok`) to swallow `OSError`/unexpected network exceptions (e.g. `ConnectionResetError`) instead of throwing 500 in `/api/status`.
- Validation on VM:
  - `bash -n scripts/monitor_stack_guard.sh`
  - `python3 -m py_compile apps/monitor/server.py`
  - `pytest -q apps/monitor/tests/test_status_never_null.py apps/monitor/tests/test_runtime_diagnostics.py` => `12 passed`.
  - endpoint timings healthy: local `/api/status`, `/api/ticks/all`, `/api/execution/all`, `/api/log-view` all fast; public `https://fc-monitor.loca.lt/api/status` returning 200.
- `dev RUNNER_CONFIG invalid` confirmed stale:
  - `logs-codex-runs/fc-ticks/dev.cron.log` contained an old one-line error from 2026-03-04 (`runner_config.v1.yaml`) while current cron/runtime use `runner.v1.yaml` with successful role runs.

## S1-S5 stabilization pass (VM validation rerun)
- Fixed monitor status NameError (`queue_snapshot` undefined) in `apps/monitor/server.py` by using canonical `queue_state_counts` for `queue_ready_dev`.
- Guard compatibility patch in `platform/policies/role_contract_guard.py`: preserve strict issue-report blockers while guaranteeing `planner_evidence_incomplete_soft` marker for non-blocked planner quality deltas.
- Revalidated on VM (`dev-vm-utm`): targeted suite `68 passed` (`role_contract_guard`, admin dispatcher, dev-ready force-claim, fc_doctor, monitor tests).
- Runtime smokes: `scripts/monitor_contract_smoke.sh` PASS, `scripts/critical_endpoints_smoke.sh` PASS.
- Runtime gate: `scripts/runtime_e2e_gate.sh` PASS (`result=OK`, `endpoint_failures=0`, `doctor_status=ok`, proof `runtime-e2e-20260306T115548Z`).

## Monitor/Dev runtime hardening (2026-03-06T07:01 local)
- Root cause monitor flapping: recursive self-call chain `/api/status -> doctor_snapshot -> fc_doctor.sh -> /api/status` + self-probe in admin runtime recovery path.
- Fixes applied:
  - `apps/monitor/server.py`: added `_DOCTOR_RUNNING` recursion guard in `doctor_snapshot()`.
  - `apps/monitor/server.py`: removed self-probe of `http://127.0.0.1:7779/api/status` inside `/api/status` admin normalization path.
  - `apps/monitor/server.py`: bounded log/event reads via tail-style access in hotspots (`tick_age`, `tick_hist`, iteration issues, guardian events, logs endpoint).
  - `platform/automation/runner/config.sh`: loader default now prefers `runner_config.py` over legacy `runner/config_loader.py`; legacy config file is fallback only.
  - `platform/policies/role_contract_guard.py`: for `dev` `task_update=claim`, deep delivery evidence is no longer hard-blocked.
- Runtime validation:
  - `/api/status`, `/api/ticks/all`, `/api/execution/all`, `/api/log-view` return 200 consistently in burst tests.
  - `dev` moved from `BLOCKED (DEV_DELIVERY_EVIDENCE_MISSING / DEV_ARCH_CHECK_FORMAT_INVALID)` back to `PASS` with `claim BATCH-27-DEV-01` and global health returned `OK`.

## Planner mono-lane migration tooling + cutover docs (VM-only)
- Added `platform/automation/migrate_to_planner_monolane.py`:
  - dry-run by default, `--apply`, and `--rollback --report <path>` support,
  - reassigns open workboard/queue ownership to canonical `planner`,
  - preserves original execution target under `meta.planner_monolane.planner_subagent_target_role`,
  - writes reversible reports in `evidence/runtime-gates/`.
- Added unit coverage in `platform/automation/tests/test_migrate_to_planner_monolane.py` for dry-run/apply/rollback.
- Added operator docs:
  - `docs/ops/PLANNER_MONO_LANE_CUTOVER_RUNBOOK.md`
  - `docs/operations/orchestrator/PLANNER_MONO_LANE_CUTOVER_CHECKLIST.md`
- Updated `docs/ops/CRON_PROFILES_SPEC.md` to include `planner-experimental`.
- Validation:
  - `python3 -m pytest -q platform/automation/tests/test_migrate_to_planner_monolane.py platform/automation/tests/test_planner_subagent_manager.py` -> `8 passed`
  - live dry-run report: `evidence/runtime-gates/planner-monolane-dryrun-latest.json`
  - current dry-run delta on live board: `task_changes=17`, `queue_changes=26`.

## Planner mono-lane live cutover + observability alignment (VM-only)
- Applied planner mono-lane migration live:
  - `python3 platform/automation/migrate_to_planner_monolane.py --apply`
  - report: `evidence/runtime-gates/planner-monolane-migration-apply-20260306T192119Z.json`
- Installed mono-lane cron profile:
  - `FC_EXPERIMENTAL_PLANNER_ONLY=1 bash scripts/fc_setup_crons.sh --profile planner-experimental`
- Post-cutover dry-run now returns zero delta:
  - `task_changes=0`, `queue_changes=0`
  - report: `evidence/runtime-gates/planner-monolane-post-cutover-dryrun.json`
- Patched monitor + doctor to reflect planner-only runtime truth:
  - `/api/status` now exposes `execution_mode=planner_experimental`
  - monitor core roles reduce to `planner` when planner orchestrator cron-only is enabled
  - doctor `sessions.expected_core` now follows runner config and returns `planner` only in planner-only mode
- Validation:
  - `apps/monitor/tests/test_status_planner_dev_policy.py` + `platform/automation/tests/test_fc_doctor.py` => `11 passed`
  - `/api/status` snapshot after reload: `roles=[planner,scrum_master]`, `core_roles=[planner]`
  - `bash scripts/fc_doctor.sh --json | jq .checks.sessions` => `expected_core=[planner]`, `missing_core=[]`.

## Planner-only cleanup follow-up (VM-only)
- Killed stale tmux delivery lane sessions no longer valid in planner-only mode:
  - `codex_dev_cron`, `codex_admin_cron`, `codex_scrum_master_cron`, `codex_qa_cron`, `codex_tester_cron`
- Remaining sessions after cleanup:
  - `codex_planner_cron`
  - `admin-agents-sync-cron`
  - `adminapp_codex_sync`
  - `clawsentinel`
- Monitor UI adjusted so planner-only mode ignores legacy scrum/advisory artifacts unless planner-owned subagents are actually active.
- Live status after reload:
  - `/api/status` => `roles=[planner]`, `execution_mode=planner_experimental`, `health=OK`
  - `fc_doctor` sessions => `expected_core=[planner]`, `missing_core=[]`.
