# Daily Memory 2026-03-06

## Planner/Scrum quality soft-autofix + monitor metrics (VM-only)
- Patched `role_contract_guard.py`:
  - planner quality fields (`root_cause`, `fix_applied`, `verify`, `reuse_check`) now soft-enforced without hard-blocking lanes,
  - preserved strict issue-report blockers and passive planner reconcile semantics.
- Patched runner + workstream:
  - scrum preflight executes `sync-priority` then `reconcile-state` (soft-fail, traced evidence),
  - new `parallel_workstream.py reconcile-state` command for non-destructive queue/workboard convergence.
- Patched monitor status payload:
  - orchestration metrics: `planner_quality_score`, `planner_quality_missing_count`, `scrum_actions_sent_60m`, `scrum_message_emit_skip_60m`,
  - agent payload enrichments for planner/scrum badges.
- Patched monitor fallback aggregator to always include new planner/scrum keys when agent payload is unknown.
- Validation (VM):
  - `python3 -m pytest -q platform/automation/tests/test_role_contract_guard.py platform/automation/tests/test_parallel_workstream_queue_sync.py platform/automation/tests/test_message_bus_auto_id.py platform/automation/tests/test_role_runtime_context.py apps/monitor/tests/test_status_never_null.py apps/monitor/tests/test_runtime_diagnostics.py`
  - Result: `74 passed`.

## Runtime unblock follow-up (planner/admin/scrum) — 2026-03-06
- Runner hardening:
  - `cron_tmux_role_runner.sh`: scrum advisory normalization now always applies for `scrum_master` (not only advisory mode flag), and reconciliation forces non-blocking advisory contract shape with `scrum_artifact/channels/impact`.
  - Removed Python 3.9+ type annotations in inline `build_scrum_master_auto_post_intents()` script to avoid runtime failures on Python 3.8 hosts.
  - Added planner quality pre-guard autofill for `claim|complete|handoff` (`root_cause`, `fix_applied`, `reuse_check`, `verify`, `vision_alignment`) with issue marker `planner_quality_autofill_missing`.
  - Added runtime-truth downgrade for `PLANNER_EVIDENCE_INCOMPLETE` to avoid hard loop-blocking.
- Monitor hardening:
  - `apps/monitor/server.py` now treats `RUNTIME_DOWN*` and `RUNTIME_DEGRADED*` admin blockers as stale-recoverable when doctor probes confirm API+monitor are up.
  - Planner blocker `PLANNER_EVIDENCE_INCOMPLETE` is tagged soft in monitor health classification.
- Tests executed:
  - `test_role_contract_guard.py`, `test_role_runtime_context.py`, `test_runner_message_receipts.py`, `test_agent_message_bus.py`, `test_planner_quality_guard.py` all pass.

## Monitor/Planner/Scrum runtime convergence follow-up (VM-only)
- Fixed scrum message-flow noise in `cron_tmux_role_runner.sh`:
  - cooldown now returns `BLOCK ...` non-fatal (no ERR trap spam),
  - bus `post` invocation moved to conditional form to avoid false `fatal_error` logs on dedup,
  - auto-intents extraction is soft-fail and no longer breaks run completion.
- Added admin runtime stale normalization in monitor (`apps/monitor/server.py`):
  - `runtime_down` blockers now auto-soften when live probes are healthy,
  - status reflects real runtime (`WAIT/PASS` + `RUNTIME_RECOVERED_SOFT`) instead of stale hard block.
- Hardened advisory safety in guard:
  - `scrum_master` missing artifact is autofilled (`scrum_artifact`) as low-severity advisory issue (non-blocking).
- Runtime check after patch:
  - `/api/status`: `health=OK`, `doctor=ok`, `planner=IN_PROGRESS`, `admin=PASS/WAIT`, `scrum_master=WAIT (non-blocking)`.
  - `orchestration`: planner quality and scrum action counters visible and non-null.
