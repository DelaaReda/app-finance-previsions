# Daily Memory 2026-03-02

## Orchestration context optimization
- Runner switched to lean-by-default context mode to reduce token burn in role sessions.
- Added role-aware memory profiles (coordination, analysis, delivery) with different context budgets.
- Added auto-generated role memory summaries at memory/agents/summaries/ROLE.summary.md from scripts/role_memory_append.py.
- Loader now prefers summary artifacts when present and keeps fallback to raw memory files.
- Docs aligned: ROLE_MEMORY_STRATEGY_3DAY, AGENTS_READY, AGENT_MEMORY_POLICY.

## Reliability hardening (P0/P1)
- Added role/global rate-limit cooldown gating in scripts/fc_agent_tick.sh to avoid retry storms.
- Added codex model guard in scripts/fc_agent_tick.sh and scripts/cron_tmux_role_runner.sh (unsupported model -> safe fallback).
- Added cron profile support in scripts/fc_setup_crons.sh (full/canary) and propagated profile mode in scripts/fc_reactivate_guard.sh.
- Extended scripts/fc_health_check.sh with model config section and active cooldown visibility.
- Updated docs/ops/ORCHESTRATION_RELIABILITY_SPEC.md with MODEL_GATE, RATE_LIMIT_GATE, and canary/full reactivation commands.
- Validation demandée: LM_USED_ROLE_MODEL déjà en gpt-5.2, jobs OpenClaw désactivés (enabled_true=0) confirmés.
- Réactivation canary exécutée via fc_setup_crons --canary (planner + backend uniquement).
- Correctif appliqué: scripts/auto_recover_tmux_roles.sh ROOT path (.. au lieu ../..) pour éviter logs path non writable.
- Correctif appliqué: scripts/fc_reactivate_guard.sh ignore les lignes commentées dans le comptage des jobs cron.
- Vision backlog extended: added BATCH-06..BATCH-10 in docs/orchestrator-ops/priority-queue.json with chained dependencies from BATCH-05.
- Product vision roadmap updated with BATCH-08..BATCH-10 (Decision UX, Alerting, Cost/Runtime + release gate).

## Vision expansion (core completion batches)
- Added new queue batches BATCH-11..BATCH-14 in docs/orchestrator-ops/priority-queue.json with detailed goals, success criteria, evidence, and subtasks.
- Updated docs/product/planning/PRODUCT_VISION.md roadmap with BATCH-11..BATCH-14.
- Added execution spec doc: docs/product/planning/BATCHES_11_14_EXEC_SPEC.md (epics, tasks by role, acceptance, gate evidence).

## Pre-reactivation acceleration package
- Added docs/operations/orchestrator/PRE_REACTIVATION_ACCELERATION_PLAN.md (work packages PR-01..PR-06, canary/full sequence, kill switches).
- Added docs/operations/orchestrator/ROLE_DISPATCH_LOW_CONTEXT_PACK.md (role memory profiles, context caps, dispatch cards, anti-loop rules).
- Added docs/operations/orchestrator/REACTIVATION_CANARY_FULL_CHECKLIST.md (pre-check/canary/extended/full/rollback/J+1).

## Backlog expansion beyond BATCH-14
- Added BATCH-15..BATCH-28 to docs/operations/orchestrator/priority-queue.json (creative expansion aligned with product vision, chained dependencies).
- Updated roadmap in docs/product/planning/PRODUCT_VISION.md with sections for BATCH-15..BATCH-28.
- Added detailed spec doc docs/product/planning/BATCHES_15_28_EXEC_SPEC.md (epics, role tasks, acceptance).

## Forecast-first expansion (no trading execution focus)
- Added BATCH-29..BATCH-40 to docs/operations/orchestrator/priority-queue.json with strict analysis/prediction orientation.
- Updated PRODUCT_VISION roadmap with BATCH-29..40 and explicit positioning: analysis/forecast first, not order execution.
- Added docs/product/planning/BATCHES_29_40_FORECAST_EXEC_SPEC.md with epics/tasks/acceptance for forecast-centric delivery.

## Global multi-layer forecast expansion
- Added BATCH-41..BATCH-50 to docs/operations/orchestrator/priority-queue.json focused on geopolitical, legal/policy, insider, supply-chain, multi-level macro forecasting.
- Updated docs/product/planning/PRODUCT_VISION.md with roadmap items BATCH-41..50 and explicit free/public data constraint.
- Added docs/product/planning/BATCHES_41_50_GLOBAL_FORECAST_SPEC.md with epics, acceptance, mandatory evidence, and guardrails.

## Batch mode switched to no-dependency parallel dispatch
- Updated docs/operations/orchestrator/priority-queue.json: all batches now have depends_on=[]; non-closed batches next_action set to READY_FOR_PARALLEL_DISPATCH; planned batches dispatch_authorized=true.
- Updated docs/operations/orchestrator/parallel-workstreams.json: dependency_mode=NONE; stream depends_on cleared; WAITING_DEP converted to PLANNED.

## Architect role contract enforcement
- `platform/policies/role_contract_guard.py` now enforces explicit execution details for architect role when queue/workboard has work:
  - Required evidence keys: `batch_scope`, `task_breakdown`, `implementation_plan`, `validation_plan`, `handoff_plan`
  - Blocker: `ARCHITECT_PLAN_EVIDENCE_MISSING` if any key is missing
- Reflection threshold enforcement via environment variable chain:
  - `TMUX_ROLE_MIN_REFLECTION_PASSES` > `LM_USED_ROLE_MIN_REFLECTION_PASSES` > `MODEL_CONFIG_PARALLEL_ROLE_MIN_REFLECTION_PASSES` > default 2
  - Required reflection dimensions: `scope`, `dependency_impact`, `risk`, `verification`, `rollback`
  - Blockers: `REFLECTION_PASSES_INVALID`, `REFLECTION_DIMENSIONS_INCOMPLETE`
- Updated `memory/agents/architect.md` with role contract requirements, blockers, and handoff expectations
- Updated `memory/agents/summaries/architect.summary.md` with contract requirements summary
- Updated product rule in docs/product/planning/PRODUCT_VISION.md: batches are independent with parallel dispatch allowed under QA guardrails.

## Parallel no-dependency operating model hardening
- Enriched priority queue with parallel dispatch metadata (lanes, waves, qa gate level, objective type, free-data flags) and set all active items READY_FOR_PARALLEL_DISPATCH.
- Enriched parallel workstreams with lane/wave metadata and explicit no-dependency execution mode.
- Added operations playbook: docs/operations/orchestrator/PARALLEL_BATCH_EXECUTION_PLAYBOOK.md.
- Added planning matrix: docs/product/planning/FORECAST_LAYER_COVERAGE_MATRIX.md to track layer/driver coverage and consistency checks.

## Architecture and free-data blueprint
- Added docs/product/planning/ARCHITECTURE_FORECAST_FREE_DATA_BLUEPRINT.md.
- Includes: architecture diagnosis from current code, target stack choices, free/public source catalog (macro, geopolitics, laws, insider), canonical contracts, and detailed parallel task pack ARCH-L1..L5.

## Source key requirements matrix
- Added docs/product/planning/FREE_DATA_SOURCE_KEY_MATRIX.md with source-by-source classification: NO_KEY / KEY_REQUIRED / KEY_OPTIONAL, env vars, fallback, cadence, and bootstrap phases.
- Linked matrix in ARCHITECTURE_FORECAST_FREE_DATA_BLUEPRINT.md.

## Orchestration surgery (agents paused)
- Stopped all active role automation until finalization:
  - system crontab role jobs removed (fc_agent_tick/vm_resume/auto_recover/watchdog block disabled),
  - tmux role sessions killed,
  - OpenClaw cron checked: enabled jobs = 0.
- Applied surgical fixes on runtime reliability/token burn:
  - scripts/fc_agent_tick.sh:
    - VM resume kill threshold now configurable and raised (default 1800s, was effectively 600s kill loop on 22m cadence),
    - global cooldown changed from hard skip to soft codex gate with qwen fallback path,
    - tick timeout made configurable with margin (default 540s instead of fixed 480s),
    - stricter rate-limit backoff trigger (provider/AGENT_RATE_LIMIT signatures only).
  - scripts/cron_tmux_role_runner.sh:
    - strict rate-limit detector (429/too-many-requests/quota/api-rate-limit-reached only),
    - valid structured contracts no longer overwritten by generic rate-limit false positives,
    - codex exec non-zero outputs truncated for noise control, partial valid contract recovery added,
    - when SKIP_RETRY_ON_TIMEOUT=1 and primary channel=codex_exec, retry is skipped (major token saver on repeated rc=124 loops).
- Provisioning anti-regression aligned:
  - platform/automation/configure_parallel_team_crons.sh and configure_tmux_role_crons.sh now default MIN_REFLECTION_PASSES floor to 2 (not 5),
  - platform/automation/cron_tmux_role_runner.sh aligned to 2-pass floor,
  - scripts/fc_health_check.sh legacy model warning now recommends gpt-5.2 baseline.
- Log-based impact estimate (historic logs replay with new strict regex):
  - 40 prior `rate_limit_gate` events reviewed, only 7 are true provider signals, 33 would be filtered as false positives.

## Final hardening pass (post-analysis)
- Reflection/token policy fully aligned to `min=2` (configurable) on active paths:
  - `scripts/cron_tmux_role_runner.sh` fallback default moved from 5 -> 2.
  - `platform/automation/cron_tmux_role_runner.sh` fallback default moved from 5 -> 2.
  - `platform/policies/role_contract_guard.py` now enforces reflection threshold from runtime env (`TMUX_ROLE_MIN_REFLECTION_PASSES` chain), floor=2.
  - Provisioning scripts aligned: `configure_parallel_team_crons.sh` and `configure_tmux_role_crons.sh` default/floor set to 2.
- Automation runner rate-limit probe/cache aligned with main runner to reduce stale skips:
  - `RATE_LIMIT_PROBE_TIMEOUT`: 18 -> 10
  - `RATE_LIMIT_CACHE_TTL_SECONDS`: 600 -> 180
- Role runners now default to `RETRY_ENGINE_DEFAULT=sdk` (codex exec primary), with automatic tmux fallback for non-codex agents.
- Model policy updated per user preference (quota split):
  - baseline coordination remains `gpt-5.2`,
  - delivery roles now default to `gpt-5.3-codex-spark` in `xhigh`: backend_engineer, frontend_engineer, integrator, infra_engineer, data_analyst, dev, tester, qa.
  - propagation done in config + role-runners + OpenClaw cron provisioning scripts so the override is effective in both fc_agent_tick and direct cron payload modes.

## Architect + Planner role refinement
- Architect locked explicitly on spark+xhigh in model config:
  - `LM_ROLE_ARCHITECT_MODEL=gpt-5.3-codex-spark`
  - `LM_ROLE_ARCHITECT_THINKING=xhigh`
- Planner public rename applied as alias:
  - external role label: `vision-architect-tasks-planner`
  - canonical runtime role remains `planner` (compatibility with workboard/guards/scripts).
  - alias accepted in `fc_agent_tick.sh`, `scripts/cron_tmux_role_runner.sh`, `platform/automation/cron_tmux_role_runner.sh`.
- Role prompts hardened:
  - planner now required to output batch/task/how + handoff clarity (`batch_scope`, `task_breakdown`, `execution_plan`, `handoff_plan`).
  - architect now required to output explicit execution details (`batch_scope`, `task_breakdown`, `implementation_plan`, `validation_plan`, `handoff_plan`) with concrete "quoi/comment".
- Guard enforcement added for architect plan detail when work exists:
  - `ARCHITECT_PLAN_EVIDENCE_MISSING` if mandatory planning fields are absent.

## Architect solo effectiveness test
- Ran one isolated tick (no cron reactivation): `timeout 420 bash scripts/cron_tmux_role_runner.sh architect`.
- Result: structured contract returned with all detailed planning keys present:
  - `batch_scope`, `task_breakdown`, `implementation_plan`, `validation_plan`, `handoff_plan`.
- Returned `task_update=none_no_ready` (no actionable READY slot in runtime context), with explicit next action and mitigations.
- Post-test hygiene: architect tmux session cleanup executed; OpenClaw enabled jobs remain `0`.
- Delivery model clarified in docs:
  - dev = autonomous self-QA evidence
  - qa = global gate with self_qa_ref + app/real-data/browser checks when UI impact.
- Runtime status rechecked: OpenClaw enabled jobs count remains `0`; no tmux role sessions active.
- Historical log analysis summary (latest scan):
  - `rc=124` heavily concentrated on planner/backend/frontend/data_analyst (legacy runs),
  - many runs ended in `codex_exec_fallback` success after primary timeout,
  - no `REFLECTION_PASSES_INVALID` events found after threshold alignment.

## ACLED incremental ingestion automation
- Added scripts/data/acled_geopolitical_ingest.py: login-based ACLED parser, structured JSON extraction, incremental hash-based updates, run snapshots.
- Added scripts/data/setup_acled_ingest_cron.sh: idempotent cron installer for 4x/day ingestion.
- Added docs/ops/ACLED_INGEST_RUNBOOK.md with commands, outputs, and payload schema.

## Model/fallback policy correction (Codex Spark primary)
- Reverted `platform/config/lm_used_model_config.sh` from `qwen primary` to `codex spark primary` on role lanes.
- Set fallback model metadata to `qwen` only (`LM_USED_FALLBACK_MODEL=qwen`).
- Updated `scripts/fc_agent_tick.sh` to remove `global cooldown -> force qwen` behavior.
- Qwen fallback now activates only when `codex.rate_limit_gate_cache` is active; role backoff remains role-local.

## Uniformisation role unifie vision-architect-tasks-planner (chirurgical)
- Objectif applique: uniformiser le nom public vision-architect-tasks-planner et regrouper les lanes coordination analyst/architect/po/scrum_master dans la lane planner canonique.
- parallel_workstream.py:
  - canonicalisation role renforcee (vision-architect-tasks-planner, analyst, architect, po, scrum_master -> planner),
  - template stream mis a jour (ANALYSIS et ARCH assignes a planner),
  - claims/completes/context/channels/handoffs/replay alignes sur role canonique,
  - compat legacy conservee pour appels CLI anciens.
- Provisioning/cron:
  - configure_parallel_team_crons.sh et topology mis a jour: suppression lanes dediees analyst/architect, planner decrit comme lane unifiee,
  - configure_tmux_role_crons.sh: execution planner via alias public, job architect dedie retire + disable legacy jobs coordination,
  - fc_setup_crons.sh: schedule planner appelle maintenant explicitement vision-architect-tasks-planner,
  - fc_agent_tick.sh: alias legacy coordination rediriges vers planner.
- Prompt planner durci:
  - obligations arch_rule/review_scope/violations + channels_read/impact + regle stricte quand queue_has_ready=0 (pas de faux dispatch).
- Validation:
  - bash -n OK sur scripts modifies,
  - py_compile OK (parallel_workstream.py, role_runtime_context.py),
  - tests cibles OK (test_parallel_workstream_channels.py, test_parallel_workstream_prechange_gates.py),
  - tick isole vision-architect-tasks-planner -> contrat valide (VERDICT WAIT, non bloque),
  - mapping regroupement verifie: context/channels role analyst|architect sortent role=planner.
- Hygiene runtime confirmee: OpenClaw enabled=true count = 0, aucune session tmux active.

## Claude Desktop anti-permission prompts (YOLO mode)
- Added `scripts/claude_desktop_enable_yolo_mode.sh`:
  - forces `mcpServers.*.alwaysAllow=true`,
  - sets `preferences.bypassPermissionsModeEnabled=true` in both:
    - `~/.config/Claude/claude_desktop_config.json`
    - `~/.config/Claude/config.json`
  - creates timestamped backups before write.
- Added/updated docs pointers:
  - `docs/ops/CLAUDE_DESKTOP_UI_IO.md`
  - `README.md`
- Runtime applied on host config:
  - `desktop_bypassPermissionsModeEnabled=true`
  - `app_bypassPermissionsModeEnabled=true`
  - restart Claude Desktop required once.
