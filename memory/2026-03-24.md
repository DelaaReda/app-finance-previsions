# Daily Memory 2026-03-24

- Recheck of autonomous orchestration on 2026-03-24: canonical queue/workboard truth moved from older March 20 assumptions to `BATCH-84` with `active_cycle.active_batch_ids=["BATCH-84"]`.
- Fresh canonical batch state: `BATCH-84` is `IN_PROGRESS`, owned by `planner`, with batch-level next action `compléter BATCH-84-ADMIN-01 (READY_PLANNER pour admin)`.
- Live tmux is secondary and incomplete at this check: `codex_dev_cron`, `codex_admin_cron`, and `adminapp_codex_sync` are visible; `codex_planner_cron`, `admin-agents-sync-cron`, and `clawsentinel` are not visible in `tmux list-panes`.
- Working rule refreshed: do not reason from stale March 20 board/history (`BATCH-71`, older planner proof notes). Autonomous work must follow only the current canonical handoff on `BATCH-84`.
- Mission split for autonomous flow on 2026-03-24:
  - planner: keep `BATCH-84` canonical and advance the planner-owned orchestration handoff
  - admin: help only on the explicit canonical handoff `BATCH-84-ADMIN-01`
  - dev: remain downstream until canonical `READY_DEV` work appears on `BATCH-84`
- Value-delivery check since 2026-03-20: yes, real user-facing value was delivered after that date. Best evidence is `BATCH-80` and `BATCH-82` proofs showing the personal finance copilot vertical slice, conversation history, decision journal, frontend copilot widget/page, and runtime validation. `BATCH-81` and `BATCH-83` appear mostly as reuse/revalidation/proof closure rather than fresh net-new surface.
- Root-cause diagnosis for "low value despite 4 days running": `BATCH-68`..`BATCH-84` all repeat the same product title/scope, so the system is churning on the same objective. Proofs repeatedly say `already implemented`, `reuse existing`, or `no code changes required`, which means the orchestration is closing many batches on validation/repackaging of an already-live slice instead of forcing novelty. Current problem is not "nothing works"; it is that planner/orchestration still reward throughput and proof closure more than new user-visible delta.
- Anti-stagnation plan 2026-03-24:
  - add a novelty gate: every batch must be classified `net_new`, `hardening`, `validation`, or `reuse_only` before downstream work starts
  - stop duplicate-scope loops: repeated same-title batches must justify a new user-visible delta or be treated as hardening/validation only
  - raise stagnation explicitly: two consecutive `reuse_only`/`validation` batches on the same scope force planner to write a novelty target before continuing
  - keep role discipline: planner owns novelty selection, admin only handles canonical validation/runtime handoffs, dev stays downstream until true `READY_DEV`
  - track two metrics separately: batch throughput vs net-new user value
- Runtime anti-stagnation wiring 2026-03-24:
  - `platform/automation/compat/projections/parallel_workstream.py` now stamps canonical queue items with `scope_key`, `novelty_class`, `delivery_kind`, and `user_value_delta_visible`
  - queue `meta` now carries a double scoreboard (`throughput_*` vs `net_new_user_value_*`) and a `stagnation_alert` payload when the same scope loops on `reuse_only`/`validation`
  - `planner_runtime_actions.py planner-autobatch` persists board events emitted by novelty/stagnation logic instead of dropping them on skip paths
  - `planner_board_runtime.snapshot()` now exposes `delivery_scoreboard` and `stagnation_alert`, so planner/runtime layers can consume the anti-stagnation signal from canonical truth
- Automation hardening 2026-03-24: the anti-stagnation rule is now enforced in the canonical autobatch path. `product_priority_guard.py` computes recent batch classes from queue/workboard evidence, `planner_runtime_actions.py planner-autobatch` refuses to mint a new batch with `reason=stagnation_requires_novelty_target` when the two most recent same-scope batches are both low-novelty (`validation` / `reuse_only`), and `planner_autonomy_tick.sh` surfaces that as an explicit planner autonomy issue instead of silently churning another duplicate batch.
- Blocked handoff escalation 2026-03-24: `planner_autonomy_tick.sh` now inspects the canonical active cycle before deferring on `active_cycle_pinned`. If the pinned cycle is actually waiting on an `admin` or `dev` task that has been `IN_PROGRESS` beyond the freshness threshold with no meaningful proof (`artifact`, `runtime_artifact`, `verify`, `summary`, `last_meaningful_progress_at`), the tick emits `canonical_handoff_stale` instead of hiding the problem behind a generic pinned-cycle warning. This is a reusable orchestration guard, not a manual rescue for `BATCH-84`.
- Hard stagnation enforcement 2026-03-24: `planner_autonomy_tick.sh` now runs the novelty/stagnation guard before claiming planner work, not just before minting a new batch. That turns repeated same-scope `reuse_only` / `validation` churn into an explicit canonical blocker (`planner_stagnation_requires_novelty_target`) instead of allowing more planner-side proof churn under the same active cycle.
- Proof-consumption TTL 2026-03-24: `state_reconciler.py` now upgrades “proof exists but state did not move” into a canonical blocker. Tasks with structured delivery evidence (`artifact`, `runtime_artifact`, `verify`, `summary`, `artifacts`, `proof_manifests`, `last_meaningful_progress_at`) that stay non-terminal past TTL are auto-marked `BLOCKED` with `blocked_reason=proof_transition_stalled`. This removes a class of false `IN_PROGRESS` optimism without any manual batch rescue.
- Delivery-value gate hardening 2026-03-24: `product_priority_guard.py` no longer treats proof-complete delivery as successful independent delivery when app runtime is degraded. `delivery_control.status` is now degraded if the live app runtime gate fails (`copilot_status != ok`, invalid forecasts, or P0 breakage), and repeated `complete` events for the same task are deduped and surfaced as `proof_churn` instead of inflating recent completion counts.
- Completion idempotency hardening 2026-03-24: `_auto_idempotency_key()` in `parallel_workstream.py` is now deterministic and `complete_task()` returns a no-op if the same completion replay hits an already terminal task. This stops one class of repeated proof/event emission at the source rather than relying only on downstream reporting deduplication.
- Supervision freshness hardening 2026-03-24: `iteration_issue_report.py` now refreshes canonical `dev` and `admin` snapshots whenever planner publishes `agent-iteration-issues-latest.json`, using the active cycle from queue/workboard instead of waiting for a fresh downstream lane tick. These refreshed role records now carry `canonical_active_batch_ids`, `canonical_task_id`, `canonical_task_state`, and `projection_secondary_only`.
- Planner guardian canonical alignment 2026-03-24: `planner_guardian.py` now reads canonical active-cycle truth from `logs-codex-runs/orchestrator-state/{priority-queue,parallel-workstreams}.json`. If the active cycle is already on a non-planner task, guardian stops recommending `autobatch` / new `ANALYSIS` / new batch creation and instead points to the canonical active task and its blocker.
- Supervision materialization 2026-03-24T23:19Z: one canonical planner pass refreshed the live `latest` artifacts under `logs-codex-runs/orchestrator-state/`. `agent-iteration-issues-latest.json` now publishes fresh planner/dev/admin snapshots from `BATCH-84`, and `planner-guardian-latest.json` is aligned on `BATCH-84-ADMIN-01` instead of stale idle/autobatch advice. The current canonical blocker is explicit: hard guard `stagnation_requires_novelty_target` plus `BATCH-84-ADMIN-01` blocked on `proof_transition_stalled`.
- Novelty-target workflow 2026-03-24: `planner_runtime_actions.py` now exposes a canonical `novelty-target` mutation. Planner can set `novelty_target` + `user_visible_delta` on the active cycle, and `product_priority_guard.build_autobatch_novelty_gate()` will unblock same-scope autobatch intentionally with `reason=novelty_target_set` instead of forcing a human to bypass the stagnation hard-stop.
- Health normalization 2026-03-24: `apps/monitor/services/status_service.py` no longer upgrades app/backend health to `ok` when `doctor` still marks the same surface degraded. Live probes now enrich the payload but do not silently override degraded doctor consensus.
- Proof replay guard 2026-03-24: `parallel_workstream.py` now clears `last_idempotency_key` on fresh claim and treats same-idempotency completions on already terminal tasks or tasks returned to `READY_PLANNER` / `READY_DEV` as no-ops. This removes another source of proof churn after canonical close/handoff.
- Role recadrage 2026-03-24: architecture/admin infra mission is to improve automated delivery orchestration itself, not to manually close a specific batch. Primary work is now novelty gating, duplicate-scope prevention, stagnation escalation, lane validity, and canonical handoff progression without human babysitting.

Mission publication 2026-03-24: added canonical shared mission to `docs/ops/AGENTS_READY.md` and linked it from `memory/agents/admin-agents.md` so all agents align on improving automated delivery orchestration rather than manually rescuing individual batches.
- Admin autonomy publication 2026-03-24: added `Admin Autonomy Plan` to `docs/ops/AGENTS_READY.md` so admin/infra agents have an explicit autonomous operating loop, allowed work, disallowed drift, and success signals while following the shared orchestration mission.
- Canonical lane-validity hardening 2026-03-24: added `platform/automation/lane_validity.py` to classify core lanes from canonical truth, not from tmux presence. The rule is now: if a role has actionable work on the active cycle, its `last_contract` must be fresh and cycle-aligned; otherwise `fc_doctor.py` degrades the lane and `auto_recover_tmux_roles.sh` can restart it automatically. Stale contracts with no actionable work remain advisory only.
- Verification governance 2026-03-24: main architect/admin verifier now owns recurring checks of admin-agent mission compliance, orchestration improvement, and independent delivery effectiveness; each verification must publish verdict, detected drift, and corrective guidance in `memory/agents/admin-agents.md`.
- Deep verification 2026-03-24: admin mission compliance is only partial. Real orchestration gains exist (`event_store_primary=true`, legacy bridges secondary-only, live stagnation scoreboard), but independent delivery is still ineffective. Canonical queue shows `BATCH-84` with `stagnation_alert.recent_classes=[reuse_only,reuse_only,reuse_only]` and `novelty_target_required=true`; planner still churns on repeated `BATCH-84-ANALYSIS` proofs; `BATCH-84-ADMIN-01` has a runtime proof from 09:42Z that was not consumed into a clean state transition by 22:48Z; and `parallel-workstreams.json` task rows for `BATCH-84` expose empty `status/owner/next_action/proof_count`, which makes projection-driven autonomy unreliable. Required admin focus: make stagnation a hard block, automate proof consumption / transition TTLs, repair projection fidelity, and stop counting control-plane motion as delivered value while app runtime stays degraded.
- Anti-churn enforcement 2026-03-24: `state_reconciler.py` now promotes active-cycle stagnation into a hard planner block, marks incomplete active-cycle workboard rows `decision_capable=false`, and finally counts `proof_transition_stalled` as a real reconcile fix/event instead of silent debt. `delivery_value_gate.py` now blocks strict delivery completion when local product runtime is degraded and upgrades repeated same-task proof retries into `PROOF_CHURN_NO_STATE_CHANGE`.
- Live reconcile result 2026-03-24T22:57Z: one reconcile pass produced `stagnation_hard_guarded=1` and `projection_decision_disabled=1` on the active cycle. Canonical queue meta now carries `planner_hard_guard.reason=stagnation_requires_novelty_target` for `BATCH-84`, and workboard meta carries `decision_capability_reason=projection_missing_operational_fields` with 16 missing operational fields. This means the system now surfaces the churn as an explicit orchestration fault instead of silently continuing.
- Supervision alignment hardening 2026-03-24: `fc_doctor.py` now degrades the projection surface when workboard decision capability is explicitly false, `planner_board_runtime.snapshot()` now exposes `planner_hard_guard`, `workboard_decision_capable`, and `active_canonical_task`, and `iteration_issue_report.py` now annotates `agent-iteration-issues-latest.json` with `active_cycle_batch_ids`, `active_cycle_roles`, and `stale_active_roles`. Goal: stop stale guardian/issue outputs from contradicting canonical active-task truth.
- Live supervision refresh 2026-03-24T23:19Z: `planner-guardian-latest.json` no longer recommends autobatch/ANALYSIS/new batch creation. It now surfaces `stagnation_requires_novelty_target`, `projection_not_decision_capable`, and the true canonical blocker on `BATCH-84-ADMIN-01`. `agent-iteration-issues-latest.json` is also refreshed on the active cycle and now exposes fresh `planner/admin/dev` role entries tied to `BATCH-84`, so stale supervision is visible as runtime state instead of hidden in March 20 artifacts.
- Deep verification refresh 2026-03-24 late check: delivery autonomy improved compared with the prior check because `product_runtime.status=ok` and canonical flow now clearly shows `BATCH-84` with 6 tasks done, 1 admin task in progress, 1 gov task waiting. But autonomy is still only partial: queue meta still reports `stagnation_alert` with three consecutive `reuse_only` classes and `novelty_target_required=true`; `BATCH-84-ANALYSIS` accumulated 88 proof artifacts (proof churn without clean transition discipline); `agent-iteration-issues-latest.json` is stale for admin/dev (still 2026-03-20); and `planner-guardian-latest.json` contradicts canonical truth by recommending ANALYSIS/batch creation while canonical work is `BATCH-84-ADMIN-01`. Main remaining admin mission: make orchestration supervision trustworthy enough to govern the automatic flow without human interpretation.
- Verification update at 2026-03-25T02:06Z: clear improvement. Admin/dev/planner structured supervision is now fresh, guardian is aligned with canonical truth, and `BATCH-84` advanced from admin `IN_PROGRESS` to `READY_PLANNER`. This means the admins did improve orchestration itself rather than manually rescuing the batch. Remaining bottlenecks are narrower: same-scope stagnation still needs an explicit novelty target, proof churn remains noisy (`BATCH-84-ANALYSIS` count=88), and monitor `status_lite` disagrees with doctor on product/backend health. Next admin focus: novelty-target workflow + health-signal consistency.
- Orchestration hardening 2026-03-24 late: canonical truth now publishes an explicit `novelty_target_workflow` whenever same-scope stagnation hard-blocks planner. The workflow is planner-owned, freezes new downstream work, and requires `novelty_target`, `user_value_delta`, `scope_delta`, and `success_metric` before the guard can clear.
- Health semantics normalization 2026-03-24 late: `fc_doctor` and `status_service.v3` now expose the same explicit split between `doctor.status` (overall control-plane), `product_runtime.status` (user-facing runtime), `app_runtime.backend_api.status` (backend runtime), and `agentic_runtime.status` (delivery control-plane). This removes the prior ambiguity where monitor-lite looked healthy while doctor was degraded for orchestration reasons.
- Proof churn suppression 2026-03-24 late: `parallel_workstream.complete_task()` now refuses replayed completion proofs once a task is already closed or has returned to planner with the same idempotent handoff. The intent is to stop future accumulation like `BATCH-84-ANALYSIS` without manual cleanup.
