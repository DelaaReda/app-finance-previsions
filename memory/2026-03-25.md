# Daily Memory 2026-03-25

- Admin/infra hardening 2026-03-25: `lane_validity.py` now treats tmux lanes rooted in deleted/out-of-workspace paths as canonical recovery failures (`invalid_deleted_workdir` / `deleted_workdir`), so auto-recovery can recycle those lanes instead of treating stale tmux presence as acceptable. `state_reconciler.py` now ignores planner graph rows stuck in `retryable` with `invalid_subagent_result:start_banner_only` once the canonical owner task is back in a READY state, preventing stale retryable residues from continuing to count as active owner-task pressure.

- Planner prompt hardening 2026-03-25: `scripts/cron_tmux_role_runner.sh` now tells planner explicitly to create canonical batches/tasks autonomously from product vision + queue/workboard truth, classify each batch before downstream work (`net_new|hardening|validation|reuse_only`), require a visible user delta, and use canonical `planner_runtime_actions.py novelty-target` before reopening same-scope work after stagnation.

- Verification-aligned admin correction 2026-03-25: live queue meta already publishes `novelty_target_workflow` for `BATCH-84`, so the remaining health inconsistency is narrowed to doctor semantics. `fc_doctor._app_runtime_surface()` was corrected so `product_runtime` follows backend runtime and treats monitor reachability as advisory-only, aligning doctor semantics with `status_service` instead of letting monitor degradation masquerade as app degradation.

- Admin/infra deep recheck 2026-03-25: the prior verification note was partly stale. The three requested corrections are now materially present in the live system:
  - `planner-guardian-latest.json` exposes an explicit `novelty_target_workflow` on the active cycle (`owner_role=planner`, `next_action=define_novelty_target`, required fields listed, policy `no_new_downstream_work`)
  - `status_lite` and `doctor` are now aligned on degraded product/app runtime (`primary_status=degraded`, `product_runtime.status=degraded`, `doctor_status=degraded`)
  - proof replay guard is in code: repeated same-idempotency completions on terminal or returned-to-ready tasks are now no-ops until a fresh claim starts a new cycle
- Canonical live truth on 2026-03-25 recheck:
  - `active_cycle.active_batch_ids=["BATCH-84"]`
  - queue item `BATCH-84` is `READY_PLANNER` with `blocked_reason=stagnation_requires_novelty_target`
  - guardian canonical task remains `BATCH-84-ADMIN-01`, with `novelty_target_workflow.status=required`
  - `agent-iteration-issues-latest.json` stays fresh for `planner`, `admin`, and `dev`
- Updated diagnosis 2026-03-25: the remaining blocker is no longer “missing admin/infra workflow”. The remaining blocker is that planner has not yet written the explicit `novelty_target` on the active cycle. Infra already enforces the stop, publishes the workflow, and aligns health semantics.
- Canonical workflow publication 2026-03-25: `state_reconciler.py` now writes the stagnation exit workflow into canonical queue truth itself (`meta.novelty_target_workflow`, `meta.novelty_target_required`, and active batch `next_action`/`novelty_target_required`), not only into guardian output. This makes the hard stop actionnable from queue/workboard alone.
- Downstream novelty gate 2026-03-25: `state_reconciler.py` now policy-blocks active-cycle downstream rows as `novelty_target_required_before_downstream` while the hard stagnation guard is unresolved, and restores their previous state automatically once `novelty_target` is present. This prevents downstream churn from restarting before planner has intentionally selected a new user-visible target.
- Next safe step 2026-03-25:
  - do not reopen downstream work
  - keep the hard guard active
  - require planner to use canonical `planner_runtime_actions.py novelty-target` before same-scope autobatch can resume intentionally
- Deep live verification 2026-03-25: confirmed that the admin team has materially improved orchestration supervision (fresh iteration issues, guardian aligned with canonical `BATCH-84-ADMIN-01`, hard stagnation workflow published in queue meta), but independent delivery is still only partial. The live blocker is now capability quality, not supervision freshness: doctor/runtime truth reports `BATCH-84-ADMIN-01` as `retryable` with `next_action=retry_capability` and `blocking_issue=invalid_subagent_result:start_banner_only`. Canonical queue/workboard still show the task as `IN_PROGRESS`, so there is a remaining translation gap between runtime-truth failure details and higher-level orchestration views. Admin mission remains: convert failing banner-only capability runs into valid structured outputs or explicit blockers, without reopening downstream churn.
- Stagnation exit hardening 2026-03-25 late: `planner_runtime_actions.py novelty-target` now rejects writes that omit `user_visible_delta`, and `product_priority_guard.py` no longer treats `novelty_target` alone as sufficient to reopen autobatch on a stagnant scope.
- Audit hardening 2026-03-25 late: `state_reconciler.py` now publishes `novelty_target_audit` with missing fields and overdue age when `stagnation_requires_novelty_target` persists without an effective write. `planner_guardian.py` surfaces that debt directly in recommendations instead of leaving it implicit.
- Verification refresh 2026-03-25: `BATCH-84` improved materially. Planner wrote the canonical novelty target, queue meta cleared the hard stagnation guard, and `BATCH-84-ADMIN-01` returned to `READY_PLANNER`. This confirms the novelty-target workflow is operational, not just documented. Remaining admin debt shifted again: tmux lanes regressed to `/home/venom/shared/analyse-financiere (deleted)`, product runtime is still degraded, and runtime-truth retains `retryable/start_banner_only` residue for admin-related tasks even after high-level queue/workboard moved on. Next admin focus: lane bootstrap auto-repair + cleanup/reconciliation of stale retryable runtime-truth rows.
- Admin/infra hardening 2026-03-25 late: lane validity now distinguishes `deleted_workdir`, `missing_workdir`, and `foreign_workdir`, and tmux auto-recovery records that concrete reason before recreating a lane. This is meant to stop `/home/venom/shared/analyse-financiere (deleted)` sessions from being counted as healthy transport.
- Runtime-truth quarantine 2026-03-25 late: `state_reconciler.py` now treats `retryable + invalid_subagent_result:start_banner_only` as stale transport residue once the canonical task is already back in `READY_PLANNER`/`READY_*`. The residue is kept as advisory metadata on the task row, while canonical `status`/`next_action` snap back to the ready-state path.
- Admin infra hardening 2026-03-25: tmux lane validity now treats deleted pane/process cwd as a hard failure, not just a stale string mismatch. `auto_recover_tmux_roles.sh` inspects both tmux pane path and `/proc/<pid>/cwd`, while `vm_resume_guard.sh` now escalates invalid role sessions into canonical lane recovery instead of leaving deleted sessions visible. Canonical dispatch also now quarantines non-active `retryable/start_banner_only` runtime-truth residues once the owner task has already returned to `READY_*`, so old banner-only failures stop polluting planner reasoning after the workboard has moved on.
