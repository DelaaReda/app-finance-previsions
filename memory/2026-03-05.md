# Daily Memory 2026-03-05


## Runtime unblock pass (VM-only) — 2026-03-05 early session
- Confirmed degraded snapshot root causes:
  - core lane drift (`dev` blocked by `ROLE_ARTIFACT_MISSING`),
  - transient `UNKNOWN/NO_DATA` contracts due incomplete payload normalization,
  - dependency plateau (`WAITING_DEP` high, low active runway).
- Applied guard hardening in `platform/policies/role_contract_guard.py`:
  - auto-fill role artifact for delivery/admin no-op or blocked-without-delivery payloads (`artifact_autofill_missing` issue marker),
  - strict delivery-action checks now trigger only on real `IN_PROGRESS` lane context (avoid false blocking when only WAITING_DEP backlog exists).
- Re-ran ticks (`planner`, `dev`, `admin`) manually with runner config v1.
- Post-fix runtime status via monitor API:
  - `health=OK`
  - `planner=IN_PROGRESS`, `dev=IN_PROGRESS`, `admin=IN_PROGRESS`
  - queue now shows active runway (`IN_PROGRESS=1`, `READY=0`, `WAITING_DEP=16`).
- Also aligned active docs/runtime path refs to VM canonical where operational:
  - `docs/ops/MCP_SSH_QUICK_REFERENCE.md`
  - `docs/ops/3DAY_MEMORY_DEPLOYMENT_STATUS.md`

## Runtime stabilization + monitor clarity (VM-only) — 2026-03-05 02:xx EST
- Confirmed live blocker chain from workboard: BATCH-27 currently gates BATCH-28; `BATCH-27-DEV-01` became active and is the immediate runway unlock point.
- Killed lingering legacy `qwen_{planner,dev,admin}_cron` sessions and re-applied codex-only core runtime via `scripts/enforce_core_codex_runtime.sh --apply`.
- Updated runner config v1 role models to reduce Spark quota stalls:
  - `dev.model -> gpt-5.2`
  - `scrum_master.model -> gpt-5.2`
- Improved monitor API visibility:
  - `/api/workboard` now exposes `deps`, `stream_id`, `batch_id` for each task (not only id/state/title).
- After restart and validation: `/api/status` returned `health=OK` with `dev=IN_PROGRESS` and queue showing `BATCH-27` runway active.

## Runtime hardening follow-up (VM-only) — 2026-03-05 08:xx EST
- Fixed `dev` intermittent runner crash (`tick: unbound variable`) in `platform/automation/cron_tmux_role_runner.sh`:
  - hardened `response_has_tick()` + `autofix_response_tick_suffix()` for missing tick args (`${2-}` + empty-tick guard).
- Fixed issue-publication continuity gap for `dev` on run-lock contention:
  - `acquire_role_lock()` now publishes monitoring + iteration-issue record even when returning `LOCK_SKIP`/`RUN_LOCK_BUSY`.
- Reduced admin false runtime alarm noise when probes are actually healthy:
  - on live probe success, reconciliation now rewrites evidence (`root_cause=runtime_restored_live_probe`, `verify=runtime_probe_8050_7779_ok`, sanitized `issues` list, normalized `run_note`).
- Enforced codex-only fallback default at runner level:
  - `RATE_LIMIT_QWEN_FALLBACK` default changed to `0` in `cron_tmux_role_runner.sh`.
  - Removed stray `qwen_dev_cron` tmux session.
- Validation (VM):
  - `bash -n platform/automation/cron_tmux_role_runner.sh` OK
  - tests: `test_role_contract_guard.py`, `test_role_runtime_context.py`, `test_runner_message_receipts.py` all PASS
  - `bash scripts/fc_health_check.sh --strict` now shows:
    - `Issue publication continuity OK (planner/dev/admin)`
    - `No qwen_* tmux legacy sessions`
    - backend/frontend/monitor contracts all `OK`.

## Monitor 503 mitigation (VM-only) — 2026-03-05 17:xx EST
- Incident: public monitor endpoint intermittently returned 503 Tunnel Unavailable while local monitor process stayed up.
- Root causes hardened:
  - duplicate-start race/noise around scripts/monitor_server.py (bind conflicts),
  - delayed tunnel recovery policy + stale URL capture from mixed tunnel logs.
- Fixes applied (on VM paths):
  - scripts/monitor_server.py:
    - added VM-only guard + single-instance lock (/tmp/fc-monitor-server.lock),
    - skip duplicate start when /api/status already healthy,
    - wait-on-lock behavior to avoid parallel bind attempts.
  - scripts/monitor_stack_guard.sh:
    - public failure threshold default tightened 3 -> 2,
    - one retry before counting a public failure,
    - robust start_tunnel() URL discovery from newly appended log lines (reduces stale URL persistence).
- Validation:
  - local VM monitor: http://127.0.0.1:7779/api/status -> 200,
  - public monitor: https://fc-monitor.loca.lt/api/status -> repeated 200 after guard cycle,
  - syntax checks: python3 -m py_compile scripts/monitor_server.py + bash -n scripts/monitor_stack_guard.sh pass.

## Queue/workboard desync fix (VM-only) — 2026-03-05 19:xx EST
- Symptom: `/api/status` reported `doctor=degraded` with `queue_workboard_mismatch` on `BATCH-27` and `BATCH-28`.
- Root cause: queue auto-sync in `parallel_workstream.py` only reconciled `WAITING_DEP|READY|IN_PROGRESS`, not `PLANNED`, while workboard had active stream states (`READY`, `IN_PROGRESS`).
- Fix:
  - `platform/automation/parallel_workstream.py`: extend reconciliation set to include `PLANNED` so active stream states propagate back to queue.
- Runtime action:
  - executed `python3 platform/automation/parallel_workstream.py sync-priority --queue docs/operations/orchestrator/priority-queue.json`.
- Validation:
  - `BATCH-27 queue=READY workboard=READY`
  - `BATCH-28 queue=IN_PROGRESS workboard=IN_PROGRESS`
  - `/api/status` => `doctor=ok`, `queue_workboard=ok`, `mismatch=[]`.

## Queue/workboard drift hardening v2 (VM-only) — 2026-03-05 19:xx EST
- Follow-up issue: intermittent mismatch persisted (`BATCH-28 queue=IN_PROGRESS` vs doctor-derived workboard `READY`) after ticks.
- Root cause: queue reconciliation in `_auto_advance_queue()` relied primarily on `streams[].state`, which can lag behind task-derived runtime truth.
- Fixes in `platform/automation/parallel_workstream.py`:
  - include `PLANNED` in queue reconciliation candidates,
  - derive stream state from task states first, fallback to stream metadata,
  - add local state helpers (`_normalize_state_token`, `_derive_stream_state_from_task_states`).
- Validation:
  - tests: `test_parallel_workstream_queue_sync.py` + `test_parallel_workstream_dependency_alias.py` PASS,
  - runtime: `sync-priority` successful,
  - monitor `/api/status`: `doctor=ok`, `queue_workboard=ok`, `mismatch=[]`.

## Monitor UI dedupe scrum vs po_scrum_master (VM-only) — 2026-03-05 19:xx EST
- Issue: Agents panel displayed duplicate advisory lane cards (`scrum_master` + `po_scrum_master`) with same content.
- Fix in `apps/monitor/server.py` (embedded JS):
  - normalize role selection with `_canonicalMonitorRole(po_scrum_master -> scrum_master)`,
  - use `_agentForRole()` payload fallback so `scrum_master` card reads `po_scrum_master` data when needed,
  - keep dedicated `PO Scrum Master (Advisory)` panel unchanged.
- Validation:
  - monitor restarted on VM (`scripts/monitor_stack_guard.sh`),
  - `/api/status` -> 200, `health=OK`, `doctor=ok`,
  - served HTML contains new dedupe helpers.
