# Daily Memory 2026-03-27

- Admin/infra recheck (operator verdict) 2026-03-27: orchestration policy is better, but independent delivery is still not acceptable. Canonical novelty remains present on `BATCH-84` and the hard guard is cleared, yet runtime is still effectively planner-only (`roles=["planner"]`, `core_roles=["planner"]`), admin remains planner-owned (`schedule=planner-owned`, `admin_autonomy_active=false`), dev is planner-owned and idle, and the active flow is paused (`BATCH-84=WAITING_DEP`, `READY_PLANNER=1`, `in_progress=0`). Product delivery must still be treated as degraded while `backend_api.status=degraded`; doctor refresh remains deferred/unknown; and `projection_missing_operational_fields` confirms the workboard is still not decision-capable. Priority order is now: (1) restore a truly autonomous admin execution path or make planner->admin self-advancing without manual babysitting, (2) keep `product_runtime` as a hard delivery gate, (3) stop accepting planner-only quarantine as steady-state for “independent delivery”, (4) restore reliable doctor refresh, (5) preserve novelty policy but optimize for actual task progression and healthy user-facing delivery.
- Admin/infra follow-up 2026-03-27: `lane_validity.py` now fails hard not only on deleted/foreign workdirs but also on tmux lanes that are only a shell without a real Codex/Qwen child (`invalid_tmux_not_ready`). Live recheck right after patch shows current cron lanes are rooted on `/home/venom/shared/analyse-financiere`, but with a real child process, so they are no longer the same failure class as `(deleted)` shells.
- Runtime-truth cleanup 2026-03-27: `state_reconciler.py` now prefers canonical READY/BLOCKED status over stale `state=IN_PROGRESS` when normalizing board tasks, and the active `BATCH-84-ADMIN-01` task was re-normalized to `state=status=READY_PLANNER`. `runtime_truth_reader.py` was aligned on the same rule; direct snapshot now quarantines 7 stale `invalid_subagent_result:start_banner_only` residues and leaves only 1 real retryable row (`BATCH-84-GOV_REVIEW`) in active runtime truth. The live monitor process still needs a clean reload to expose this new quarantine result over HTTP.
- Product/runtime gate remains separate on purpose: orchestration state is cleaner (`admin` back to `READY`), but app/runtime must still be treated as degraded until backend-facing monitor surfaces are healthy again. Do not count this reconciliation as delivered user value.

- Verification 2026-03-27: canonical orchestration improved materially. `BATCH-84` now carries an explicit novelty target (`portfolio_first_brief_with_ranked_actions`) and user-visible delta in canonical queue truth; `planner_hard_guard` and `novelty_target_workflow` are both cleared, proving the anti-stagnation workflow is operational end-to-end. The active admin task returned to `READY_PLANNER`, so the system is no longer stuck in the prior same-scope guard state.
- Remaining admin debt after the 2026-03-27 check: lane bootstrap is still unreliable because visible tmux sessions are rooted in `/home/venom/shared/analyse-financiere (deleted)`; doctor/runtime truth still keeps stale `retryable` admin rows with `invalid_subagent_result:start_banner_only`; and user-facing runtime remains degraded (`product_runtime.status=degraded`, `backend_api.status=degraded`). Conclusion: orchestration policy got better, but transport/runtime hygiene is still not autonomous enough.
- Admin infra correction 2026-03-27: tightened lane validity so `/home/venom/shared/analyse-financiere` no longer counts as a healthy execution root even if it points to the same inode as the canonical VM workspace. Productive auto lanes must now advertise the canonical path string `/home/venom/analyse-financiere` or they are invalid and eligible for recovery. Also moved `start_banner_only` retryable residue quarantine into `runtime_truth_snapshot` itself, so stale admin failures stop appearing as current blockers once the owning canonical task has already returned to `READY_*`.
- Verification refresh 2026-03-27: orchestration policy is better than earlier this week, but independent delivery is still not there. Canonical novelty handling is correct (`BATCH-84` has `novelty_target`, hard guard cleared, no tmux `(deleted)` cwd), yet the live runtime remains effectively planner-only: `roles=["planner"]`, `core_roles=["planner"]`, `admin_autonomy_active=false`, `dev` idle/planner-owned, `BATCH-84` paused in `WAITING_DEP`, `READY_PLANNER=1`, `in_progress=0`. User-facing delivery is still not independently healthy because `product_runtime.status=degraded`, `backend_api.status=degraded`, doctor refresh can defer to `unknown`, and workboard projection is still marked non-decision-capable (`projection_missing_operational_fields`). Next admin/infra priority is therefore not more novelty policy work; it is restoring a genuinely self-advancing planner->admin path, hard-gating independent delivery on healthy product runtime, and restoring reliable doctor freshness.
- Monitor semantics correction 2026-03-27: top-level monitor status now distinguishes `scheduler_roles` (scheduled lanes) from `capability_roles` (planner-owned domains visible on the active cycle). In planner-only mode this means the monitor still reports `scheduler_roles=["planner"]`, but `roles` now includes visible planner-owned capability domains such as `admin` and `dev`. `admin_autonomy_active` is no longer dependent only on `admin_autonomy_state.json`; it can now hydrate from canonical active-cycle truth when an admin task is `READY_PLANNER`/`IN_PROGRESS` on the current batch. Lite status also now reuses the latest cached doctor payload when refresh is deferred, instead of falling back to `doctor_status=unknown` when a recent doctor snapshot already exists.
- Admin infra hardening 2026-03-27 late: `lane_validity.py` now validates the actual cwd of the tmux pane process and its child processes, not just tmux pane metadata. `auto_recover_tmux_roles.sh` also quarantines invalid auxiliary sessions rooted in deleted/foreign workdirs, so false-positive tmux shells do not survive as “alive” transport.

## Verification refresh — 2026-03-27
- verdict: mission_compliance=ok, orchestration_improvement=ok, independent_delivery_effectiveness=partial
- confirmed: `BATCH-84` keeps a canonical novelty target and visible user delta; hard guard is cleared; `BATCH-84-ADMIN-01` returned to `READY_PLANNER`; cross-role supervision is fresh.
- corrected prior drift: visible tmux panes are still rooted in `/home/venom/shared/analyse-financiere`, but no longer marked `(deleted)`.
- remaining blockers: `product_runtime` / `backend_api` still degraded; runtime truth still keeps stale `retryable/start_banner_only` admin residues after queue-workboard recovery; workboard remains non-decision-capable; `BATCH-84-ANALYSIS` proof churn remains high (`88`).

## Delivery-independence recheck — 2026-03-27
- verdict: mission_compliance=partial, orchestration_improvement=partial, independent_delivery_effectiveness=not_ok
- canonical flow keeps `BATCH-84`, novelty target is present, and hard guard is clear, but runtime remains `planner-only`.
- key issue: `admin` is still planner-owned (`admin_autonomy_active=false`, `schedule=planner-owned`) and `dev` is idle under planner ownership, so the system is not yet advancing delivery with real cross-role independence.
- queue shows `BATCH-84` in `WAITING_DEP` with one `READY_PLANNER` task and zero in-progress tasks; the flow is paused rather than self-advancing.
- delivery remains unhealthy because `product_runtime.status=degraded`, `backend_api.status=degraded`, and `doctor_refresh_deferred` leaves top-level health incomplete.

## Planner progress check — 2026-03-27
- planner is still alive (`PLANNER_DISPATCH_ACTIVE`) and points to `BATCH-84-ADMIN-01`.
- flow is not truly advancing autonomously right now: queue has `READY_PLANNER=1`, `in_progress=0`, admin is only `READY` to claim, and the batch remains active without active execution.
- practical blocker stays the same: planner->admin handoff is not yet self-advancing, and `product_runtime/backend_api` remain degraded.

## Admin/infra stalled-flow instrumentation — 2026-03-27
- `apps/monitor/server.py` now refreshes `doctor` on full status/runtime-diagnostics requests instead of always forcing the deferred path.
- Runtime diagnostics now publishes explicit stalled-flow findings when the system is effectively planner-only, `READY_PLANNER>0`, `IN_PROGRESS=0`, and admin autonomy is not active.
- Independent delivery is now surfaced as ineffective when backend/product runtime is degraded or doctor freshness is deferred, even if planner dispatch intent is still visible.
- Admin/infra: patched `platform/automation/admin_dispatcher_tick.sh` to close the systemic planner->admin gap. Before this, the dispatcher had no nominal `admin_claim_ready` branch and explicit/virtual blocked-role autonomy filtered `admin` out entirely. Now `admin` is eligible in blocked-role autonomy/takeover, and an empty admin lane with a canonical `READY` task can claim it through `planner_runtime_actions.py claim --role admin`.
- Validation after patch: `bash -n platform/automation/admin_dispatcher_tick.sh` and `python3 -m py_compile apps/monitor/services/status_service.py` passed. A dry-run dispatcher tick did not yet exercise the new branch because live state at that instant showed `ready_queue=0`, `board_waiting_dep=1`, `planner_in_progress=0`, `admin_in_progress=0`.
- Admin/infra follow-up 2026-03-27: `planner_runtime_actions.py` now exposes a canonical `dispatch-capability` command for planner-owned `admin`/`dev` work, and `planner_autonomy_tick.sh` uses it before falling back to a generic “active cycle pinned” interpretation. New invariant: if the active cycle has a `READY`/`READY_PLANNER` downstream task with `planner_takeover_required` or `next_action=retry_capability`, the tick attempts targeted dispatch immediately; if that still does not materialize into execution, it emits `planner_admin_dispatch_not_materialized` / `planner_dev_dispatch_not_materialized` instead of silently normalizing the state as pinned.
