# Invariants - Copilot Domain

- Copilot endpoints remain non-blocking and return structured responses.
- Search and recommendations reuse shared service layer.
- No hard dependencies on frontend implementation files.
