from __future__ import annotations

from fastapi import APIRouter


def create_copilot_router() -> APIRouter:
    """Progressive extraction placeholder router.

    Endpoints remain in platform.main/register_routes until migrated in batches.
    """
    return APIRouter()
