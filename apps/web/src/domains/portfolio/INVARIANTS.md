# Invariants - Portfolio UI Domain

- Portfolio routes/components remain stateless between page renders unless explicit cache states.
- Only consumes shared API responses from backend services.
- No mocked production data in runtime mode.
