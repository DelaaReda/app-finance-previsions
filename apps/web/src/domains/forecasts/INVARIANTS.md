# Invariants - ForeCasts UI Domain

- UI consumes typed `contracts/*` data contracts only.
- Domain page composes reusable components under `components/`.
- No inline business logic that mirrors API business rules.
